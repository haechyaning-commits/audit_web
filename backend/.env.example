# 실제 값은 .env 파일(git에 커밋 안 됨, .gitignore 참고)에 넣고 사용하세요.

# Railway Postgres 서비스 → Connect 탭 → DATABASE_PUBLIC_URL
DATABASE_URL=postgresql://postgres:비밀번호@호스트:포트/railway

# OpenAI API 키 (상세페이지 4줄 요약 온디맨드 생성용, platform.openai.com에서 발급)
OPENAI_API_KEY=sk-...

# 배포된 프론트엔드(Vercel) origin (CORS 허용용). 로컬 개발(localhost:5173)은 기본으로 항상 허용되므로
# 로컬에서만 쓸 땐 비워둬도 됨.
FRONTEND_ORIGIN=https://your-app.vercel.app

# "데이터 오류 신고" 목록 조회(GET /admin/reports?token=...) 접근용 비밀 토큰. 비워두면
# 그 엔드포인트는 항상 403 — 로그인 시스템이 없는 프로젝트라 URL에 붙이는 공유 비밀
# 토큰 방식을 씀. 아무 무작위 문자열이나 넣고(예: openssl rand -hex 16), 이 값을 아는
# 사람만 /admin/reports?token=이값 으로 접속해서 신고 목록을 볼 수 있음.
ADMIN_TOKEN=

# 리랭커(bge-reranker-v2-m3, architecture.md §3.4) 켜기 — 기본 false. 켜기 전에
# §3.5(임베딩+리랭커 동시 로드 메모리 실측)를 먼저 끝낼 것, 안 그러면 배포 환경
# 메모리 상한을 넘겨 컨테이너가 죽을 수 있음. true로 하면 앱 시작 시 두 번째
# CPU 모델을 추가로 로드해서 시작 시간도 늘어남.
RERANKER_ENABLED=false

# 한국어 형태소 토큰화(kiwipiepy, architecture.md §3.6) 켜기 — 기본 false. 켜기 전에
# scripts/backfill_tsv_text.py + scripts/tsv_text_migration.sql로 chunks.tsv_text
# 배치 백필과 tsv 재색인을 먼저 끝낼 것 — 안 그러면 색인(원문 기준)과 쿼리(토큰화 기준)의
# 매칭 기준이 어긋나서 키워드 검색이 오히려 나빠짐(tokenizer.py 모듈 docstring 참고).
TOKENIZER_ENABLED=false
