[pytest]
# backend/tests/*가 "from app import main"으로 import할 수 있게 backend/ 자체를
# sys.path에 추가함(패키지를 pip install -e 하지 않고 리포 구조 그대로 테스트하기 위함,
# CI의 backend-test 잡도 이 디렉터리에서 pytest를 실행함).
pythonpath = .
