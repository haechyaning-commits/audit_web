# 진행 상황 로그

> 최신 항목이 맨 위. 날짜별로 그날 한 일만 압축 요약했음 — 서술형 배경설명·검증
> 과정·시행착오까지 담긴 원본 버전은 git 히스토리에 그대로 남아있어서 필요하면
> 언제든 꺼내볼 수 있음(`git log -- STATUS.md`).

## 2026-08-28

- ✅ 죽은 브랜치 2개(`initial-search-message-w39r8s`, `audit-report-text-backfill-yivu8a`)에서 `DetailPage.jsx` 렌더링 버그 5건 재발견·재이식 — 제목번호 헤딩 오탐, 표 참조 오탐, 접속부사 흡수, "관련자" 표 인식 실패, 검색 지연 문구 완화. 회귀 테스트 14→18건
- ✅ 리랭커 오프라인 eval 완료: MRR 0.337→0.260 악화로 미적용 확정(§3.4 종결). 또 다른 죽은 브랜치에서 부서/기관명 익명화 심볼 누출(24,920건/67,751건, 36.78%, 역대 최대)이 이미 프로덕션엔 반영돼 있었는데 main에 기록이 없었던 걸 발견 — `chunks` 잔여 60건 마저 정리, 관련 스크립트 뒤늦게 커밋

## 2026-08-27

- 🔧 리랭커 브랜치 main 병합 + §3.5 메모리 실측(3.8GB, Railway Hobby 48GB 한도의 8%, 안전) + `TOKENIZER_ENABLED=true` 배포 완료
- 🔴✅ tsv 재색인(STEP 2) 중 Railway DiskFull 장애(`/dev/shm` 한도로 추정) → GENERATED 컬럼 대신 트리거 방식으로 전환해 복구
- ✅ `chunks.tsv_text` 형태소 토큰화 백필 92,136건 완료(실패 0) + eval 스크립트가 저장된 `tsv` 대신 `tsv_text` 기준으로 재계산하도록 수정
- 🔧 리랭커·형태소 토큰화·오프라인 eval 하네스 코드 전부 준비(이 세션은 DB 없어 실행은 다음 세션 몫)
- ✅ 각주 흡수 버그 수정 + 이 프로젝트 첫 테스트/CI 인프라 도입(PR #46~#50: Vitest 14건 + pytest 18~21건 + CI 잡, `/reports` IP당 rate limiting, Sentry 연동)

## 2026-08-26

- ✅ 오류 신고를 GitHub 이슈 링크 → 자체 신고 모달 + `ADMIN_TOKEN` 보호 관리자 페이지로 교체(그 전엔 GitHub 이슈 링크 방식으로 먼저 추가했었음)
- 🔧 실배포 피드백 대응: 종합감사/성과감사 배지 색 겹침 수정, 법령 하이퍼링크가 기관 내부 지침까지 걸던 오탐을 law.go.kr이 실제로 다루는 법/시행령/조례로 좁힘
- 🔎 이 세션 프록시 환경에서 Playwright로 실제 렌더링 확인하는 우회법 확립(로컬 리버스 프록시). 새 버그 발견: 부서명 익명화 자리에 `[부서]` 대신 카드무늬 등 사설 심볼 노출(표본 7%) — 원인 불명, 수정은 다음 세션
- ✅ 베타테스트 20명 시뮬레이션(API 직접 호출): 크래시 0건, 이전 검색 지연 이슈 해소 확인(1.2~4.1초)

## 2026-08-25

- ✅ `DetailPage.jsx` 원문 구조 인식 버그 11건 수정(바레 라벨 헤딩, 가나다 소제목 24자 제한 확장, 괄호번호 헤딩, 각주 오분류 2건, 원문자 불릿, 들여쓰기 문단분리, 표 헤더 오분류, 익명화 placeholder 오분류) — 실 API 문서 1,894건 스캔 기반
- ✅ 홈 통계(전체 건수/연도별 차트) 하드코딩 → `/stats/years` 라이브 집계 전환
- ✅ 검색 결과 카드에 title 필드 표시, 관련성 하한선을 2자 미만 검색어 거부로 축소(점수 기반 필터링은 eval set 부재로 보류)
- 🔧 HWP 표 손실 복구 스크립트에 체크포인트/자가설치(hwp5txt·hwp5html) 보강
- ✅ HWP 표 손실 전수조사 완료: 33,089건 중 19,150건(57.9%) 영향 확정
- 🔴🔧 HWP 표 손실 "0건" 오탐이 반복됨 — 원인은 Colab 런타임마다 hwp5txt 재설치가 필요했던 것, 스크립트가 자체 설치하도록 고쳐 최종 해결. 진단용 체크포인트 재진단 스크립트도 신설
- 🚧 검색 첫 요청 15~17초 지연 대응: PyTorch 스레드 수 제한 + 예시 검색어 캐시 예열 + 지연 안내 문구 추가(효과는 가설이었으나 다음날 배포 후 실측으로 해소 확인)
- 🔧 `/documents/{id}/similar` 500 에러 발견·수정 — 1차는 embedding NULL 필터링, 2차에 진짜 원인(`pgvector.Vector`↔numpy 타입 불일치) 발견해 `.to_numpy()` 변환으로 최종 수정

## 2026-08-24

- ✅ 상세페이지 "유사 사례" 섹션 추가(문서 centroid로 벡터검색 재사용), 쿼리 임베딩 `lru_cache`로 필터 재검색 시 재인코딩 낭비 제거
- ✅ 이 프로젝트 첫 CI(GitHub Actions) 추가, 필터 연속 클릭 경합 조건(race condition) 수정
- ✅ robots.txt/sitemap.xml + 전체 사례 sitemap 생성 스크립트
- ✅ 웹페이지 피드백 반영(정렬 토글, 모바일 필터 접기, OG 메타태그, 관련도 막대, 최근검색 지우기, 필터 라디오화) — 별도 브랜치에만 있어 배포 안 되고 있던 걸 뒤늦게 발견해 main 병합
- 🔴 HWP 표 손실 전수조사 "0건"이 오탐으로 판명 — hwp5txt 런타임 전면 실패를 조용히 삼키던 버그, returncode 체크 추가
- ✅ 연도 필터 정렬을 빈도순 → 최신순으로 수정, 비주얼 톤 개편(기존 팔레트로 그림자/그라데이션만 추가)
- 🔍 Wingdings 심볼폰트 불릿 오염 조사·수정 시리즈: `v`(한국수력원자력)/`m`(서울대병원 등)/`q`(한국원자력통제기술원) 3개 글자가 실제 Wingdings 체크마크 불릿 오염으로 확정(`y`/`r`은 "Advisory"/"Leader" 같은 영단어 끝글자가 우연히 잡힌 가짜 경보로 최종 판명) → 249건/446건 반영. 반영 중 raw.githubusercontent.com 캐시로 구버전 스크립트가 실행돼 정상 표기("2.0m")가 잘못 지워지는 사고 발생 → 즉시 복구, 숫자 뒤는 매칭 제외하도록 정규식 보강. 재임베딩 1,758건까지 완료. 별개로 한국농어촌공사 문서에서 HWPX 태그 누출 신규 변종도 발견, 규모조사 스크립트만 작성
- ✅ 검색 필터를 고정 사이드바형으로 전면 교체 + 감사유형 필터·기관 자동완성 추가, 기관/연도 인덱스 추가, 기관/연도 필터(FR5) 로컬 PostgreSQL 실 SQL 15건 검증 후 main 머지
- ✅ HWP 표 손실 스크립트들의 체크포인트 폴더 누락 버그 일괄 수정, 파이프 셀 표를 진짜 HTML `<table>`로 렌더링, HWP 복구 표(파이프 셀)가 렌더링 안 되던 버그 발견·수정
- 🏁 각주 유실 576건 원본 PDF/HWP 10건 재추출 대조 — 전부 원본 자체에 없음 확정, 데이터 파이프라인 한계로 결론. HWPX 태그 누출 재확인 0건, "1. 제목:" 번호 유실도 이미 해결 확인. 단어 중간 줄바꿈(16,182건, 23.88%)은 형태소 분석 없인 안전한 자동수정 불가로 보류 확정
- ✅ 표지 페이지(뱃지/제목/날짜) 5줄이 한 줄로 뭉치던 버그 수정(`cover` 블록 타입 신설)

## 2026-08-21

같은 날 여러 세션이 이어서 진행한 "각주 렌더링 이상 전수조사" 시리즈: 초기 후보
4,137건 → 조치유형 코드((통보N)/(주의N)) 오탐 필터로 3,147건 → caption/field
직후 각주 인식 실패 버그 수정으로 3,102건 → 분류기 오판 수정으로 3,083건(각주
관련 렌더링 버그는 이 시점에 전부 해결 확인, "각주 참조는 있는데 블록 0건" 신호가
100% 원본 데이터 유실로 수렴) → cid 오염 캡션 처리 + 표 흡수 재분류 추가 수정으로
최종 **3,005건(4.44%, 전날 대비 27.4% 감소)**.

- 카테고리별 최종 결론: 긴 각주(1,247건) 대부분 정상(cid 오염 86건만 별도 이슈), 긴 불릿문단(885건) 89.6% 정상(표 흡수 84건 신규 발견, 수정은 리스크 대비 규모 작아 보류), heading 0건(411건) 대부분 정상/기존 설계, 긴 표잔여(74건) 전부 정상, 각주 참조인데 원본에 정의가 없는 576건은 기관/연도/파싱등급 무관하게 코퍼스 전반에 퍼진 텍스트 추출 유실로 결론(이 저장소 범위 밖)
- ✅ 코드 리뷰로 "붙임" 첨부목록 항목이 본문 각주 번호와 충돌해 각주로 잘못 떨어지던 버그 발견·수정

## 2026-08-20

- ✅ 사용자 원본↔웹 대조로 각주 15/16 미인식 버그 근본 원인(짧은 헤딩 직후 각주 흡수 실패) 발견·수정 + 대괄호 라벨([통보] 등) 뒤 문장이 통째로 볼드되던 버그 + '붙임' 첨부목록 라벨 미인식 버그, 총 3건 완전 해결(제보 5건 중 3건 — 남은 2건은 구조적으로 어려워 이월)
- 🔧 조치유형 분류 코드((통보N)/(주의N))가 각주 참조로 오인되던 스캐너 오탐 필터 추가
- 📋 `audit_render_anomalies.py` 첫 전수 실행(67,751건): 1차 스캔 13,601건 → 오탐 필터 후 4,997건. 표본 재확인으로 원인 3갈래 확정(헤딩/각주 번호 충돌, 표 블록에 각주 흡수, 스캐너 자체 오탐)
- 🐛 ㅇ(한글 자음) 불릿 미인식, 괄호라벨 직후 각주 오분류, 법령인용 아닌 나열식 목록까지 헤딩 오탐되던 버그 3건 발견·수정(부수적으로 각주 15/16 문제도 같이 해소됨)

## 2026-08-19

- ✅ HWP 내부 태그 누출(hc: 계열) 규모조사 — 0건, 이미 해소된 것 확인
- ✅ "1. 제목:" 번호 유실 DB 백필 완료(4,305건) — 재임베딩 입력 파일이 Colab GPU 런타임 전환 중 유실되는 사고 있었으나 전용 체크포인트로 재구성해 복구
- ✅ 구버전 HWP 표 손실 복구 DRY_RUN 검증 + 실반영 완료(1,559건, 표본 90.2% 커버) — Python 3.12+구버전 setuptools 조합에서 hwp5txt가 조용히 실패하던 문제 해결 후 재실행
- ✅ 원문 렌더링 버그 2건 추가 수정: 로마숫자 오탐(라틴 X가 장 제목으로 오인), 각주가 괄호 뒤 공백 없으면 인식 안 되고 인식돼도 한 줄만 잡히던 문제
- ✅ 제목 번호 유실 화면 표시 보정(`fixMissingTitleNumber`) — 같은 날 DB 백필 세션과 병렬 진행, 서로 보완 관계로 병합
- ✅ 제목 번호 유실(4,330건, 6.39%) + 단어 중간 줄바꿈(16,171건, 23.87%) 규모조사 완료

## 2026-08-18

- PDF 2단 레이아웃 재추출 1~3차: 컬럼 순서 뒤섞임 발견 → PyMuPDF 좌표 기반 재추출 프로토타입(컬럼 순서+띄어쓰기 동시 복구 성공) → 1차 722건, 2차(순서만 바뀐 케이스 구제) 702건, 3차(원문자 서식 버그 수정 후) 578건 touch-up — 최종 1,424건 + 578건 서식개선, 재청킹·재임베딩 전부 완료
- 구버전 HWP 표 내용 손실 원인 특정(hwp5txt가 `<표>` placeholder만 남기고 셀 내용 못 뽑음, hwp5html과 병합하면 복구 가능함을 검증) — 전수조사가 너무 느려 표본(3,426건)으로 전환, 영향률 50.5%(추정 16,709건) 확인, 복구 스크립트 작성
- HWP 속성명 누출("array"/"bList" 등 네임스페이스 없는 변종) 최소 11개 기관에서 발견, 수정 코드 작성+로컬 검증까지 완료(DB 반영은 다음 세션 몫)
- 제목/불릿 렌더링 버그 3건 수정(따옴표로 TITLE_RE 안 걸리던 버그, "•" 불릿 누락, "□" 단독줄 병합 안 되던 버그), 표 블록 줄바꿈 보존(CSS/JS 드리프트 수정)
- "2-2/2-3 내용 소실"의 정확한 메커니즘 확정: `build_final_dataset.py`가 제목이 우연히 같은 여러 사례를 매칭 키 충돌로 하나로 뭉갬(청킹 데이터엔 남아있어 완전 해결은 재청킹 필요, 저장소 밖 문제)
- 원문 렌더링 개선(필드 라벨 굵게 분리, 번호목록 헤딩에 딸린 인용문까지 굵어지던 버그, 연속 번호목록 각주 오분류 버그, "◯N" 헤딩 미인식, "◤" 장식 글리프 노출 등) + Railway 도메인이 여러 번 바뀌는 반복 문제 계속 확인

## 2026-08-14

- PDF 다단(2단) 레이아웃 전수조사 완료: 30,120건 중 2,111건(7%) 확인, 오탐 의심 패턴 직접 검증해서 전부 진짜로 판명
- PDF 재추출 프로토타입 완성: 컬럼 순서 복구(PyMuPDF 블록 좌표) + 띄어쓰기 손실 복구(문자 간격 실측 기반) 동시 해결, 실제 DB 원문과 글자 단위 비교 검증(24개 문서, 평균 유사도 93.5%)
- "선행 상용구 미제거" 문제 — 가설(제목 키워드 위치에서 문자열 슬라이스)은 찾았으나 다중 지적사항 보고서 스타일에서 표지/목차까지 삭제될 위험이 있어 구현 보류. 청킹 알고리즘 자체가 이 저장소에 없다는 것도 확인 — 재추출 DB 반영은 청킹 규칙 역산 전까지 보류
- HWP 원문 잘림(약 1,000자 캡) 대규모 발견: HWP 미리보기 텍스트를 본문으로 착각해 저장했던 근본 원인 확정, 5,405건 의심 중 5,389건 재추출·복구 성공. 반영 직후 자체 발견한 hwpx 재추출 정규식 버그 2종(XML 태그 누출, 표 내용 중복, 총 2,597건 영향)도 배포 전에 잡아서 재수정. 재청킹까지 완료, 재임베딩만 다음 세션으로 이월
- Railway 볼륨 77% 경고 대응(WAL 정리는 원인 불명으로 완전 해결 못함) + 원문 잘림/표 컬럼 뒤섞임/라벨 이어붙임 전수조사(원문 잘림 416건, 표 컬럼 뒤섞임 49건, 라벨 인식 실패 20개 키워드 확장) + 표 컬럼 뒤섞임 47/49건 PDF 재추출로 실반영
- 배포 설정 버그 발견(Vercel `VITE_API_BASE_URL`이 백엔드가 아니라 프론트 자신을 가리키고 있어 검색/상세 전부 안 되던 상태) + 원문 렌더링 개선 다수(각주 스타일, q/m→□/○ 글리프 치환, 보안문구 축소, 표 캡션 인식 확대) + "(N-M) [처분유형] 사항" 뒤 내용이 통째로 끊기는 문서 분리 경계 버그 최초 발견(저장소 밖 원인)

## 2026-08-13

- 원본 파일(GitHub/jsdelivr CDN) 링크 추가 — `source_file` 컬럼 백필(76,454건) + 상세페이지 "원본 파일 보기" 버튼. raw.githubusercontent.com이 강제 다운로드시켜서 jsdelivr로 교체(PDF는 새 탭에서 바로 보임)
- 표/각주 캡션 인식 확장(꺾쇠괄호, 하이픈 표기, 소관/조치기관 라벨 등) 다수 배포, 탭 타이틀에 대괄호 유실된 원문이 그대로 노출되던 버그 수정
- 숫자 중복 collapse 가드의 역설적 버그 발견(16~18자리급 극심한 중복일수록 오히려 안 고쳐짐) — 원인 진단만 하고 수정은 이월
- "[라벨] 한 줄 이어붙임" 문단 분리 안 되는 문제, 표 컬럼 뒤섞임 새 변종("2단 번호 리스트") — 기존 조사 스크립트가 못 잡는 것 확인, 별도 설계 필요로 이월

## 2026-08-12

- 재임베딩(34,409건) 마무리 + HNSW/GIN/document_id 인덱스 생성, Railway/Vercel 재배포
- 원문 구조화 렌더링(문단/헤딩/불릿/캡션/표 블록 분리) 최초 구현 — 여러 차례 실제 문서로 재현·검증하며 다듬음
- 감사종류(audit_type) 파싱 + 백필(67,751건) 및 카드/상세페이지 표시 추가
- 텍스트 오염 2차 수정(괄호 중복, 제목 콜론 통일 33,707건/34,764건 청크) + 단독 한글 글자 중복은 형태소 분석 없인 오탐 구분 불가로 자동수정 보류
- law.go.kr 스타일 UI 반영(KRDS 컬러 토큰, 목록형 카드, 목차 사이드바) 후 실사용 피드백으로 세리프체 제거 등 일부 되돌림

## 2026-08-11

- Railway DB를 실수로 완전 삭제하는 사고 발생(백엔드만 지우려다 Postgres까지 삭제) → 원본 소스 파일로 전체 파이프라인 재실행해 복구(문서 67,751건/청크 89,301건, 사고 전과 정확히 일치)
- 재임베딩 스크립트를 `!python` 서브프로세스로 실행해 Colab 커널 통신 실패로 두 번 날아간 뒤, 체크포인트 저장 기능 추가로 안정화
- 프론트엔드 UI 대규모 개선(카드 정리, 미리보기를 매치된 청크 기준으로 변경, 히어로 확대, 연도별 차트 컴포넌트 준비, 검색창 문장형 placeholder 등)

## 2026-08-10

- 4주차 배포 완료: Vercel 프론트 배포(GitHub 계정 연결 문제, SPA rewrite 설정 등 해결), Railway 백엔드 재배포(DB 연결 문자열/도메인 라우팅 문제 다수 해결) — curl로 최종 검증
- 검색/상세페이지 UI 대규모 개선: 헤더 상시 검색창, 결과 2열 그리드+페이지네이션, 상세페이지 원문 우선 표시(요약은 버튼 클릭 시 온디맨드), 자유형 4줄 요약 추가
- 데이터 품질 사고 대응 마무리 — 재추출 불가 문서 삭제 실행(documents 5,162건 + chunks 7,054건 제거, 최종 67,751건 확정), 원인 규명(버그 아니라 태그 노이즈였음)까지 완료

## 2026-08-07

- 텍스트 오염 발견(글자 중복, HWP XML 누출, 표 placeholder만 남은 문서 등) → 원인 진단 → 1차 자동 수정(1,516건 문서/4,770건 청크) → 실 검색으로 재확인 중 추가 이슈(숫자 중복, 표 placeholder 잡음) 발견 → 2차 수정(16,855건 청크 재임베딩) → 재추출 불가 문서 방침 확정(재추출 대신 삭제) 완료
- 2주차 목표(검색 API) 달성 — FastAPI 프로젝트 세팅, `/search`/`/documents/{id}` 구현, 실제 72,913건 데이터로 검증

## 2026-08-06

- 원본 데이터 정리(72,913건 확정) + DB 스키마 확정 + 적재 스크립트 준비
- BGE-m3 임베딩 완료(124,066개 청크) — 이 시점에 4줄 요약을 "배치 사전생성"에서 "상세 API 온디맨드 생성 + DB 캐싱"으로 설계 변경(비용/인프라 절감)
- `preflight_check.py`로 실전 배포 전 버그 발견(parse_tier 정규화 누락 등) 다수
- 검색 카드 미리보기가 요약 온디맨드 전환 후 깨져 있던 것 발견·수정(카드는 raw_text 발췌로 분리)
