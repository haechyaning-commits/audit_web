# 진행 상황 로그

> 대화창이 바뀌어도 여기부터 이어서 보면 됨. 최신 항목이 맨 위.

## 🔍 2026-08-21 (6차) — 남은 ① 20건은 버그 아니라 분류기 오판이었음 — DetailPage.jsx는 안 건드림

남은 ①(렌더링 버그 의심) 20건 중 표본 5건을 Colab에서 전체 트레이스(`trace_footnote_decisions.py`,
FOOTNOTE_DEF_RE 매치되는 모든 줄의 판정 과정을 한 줄씩 출력)로 추적한 결과 — **5건 전부 버그가
아니었음**.

**실제로 벌어진 일**: 한국광해광업공단 2023(`6b06a312a8a35d5a`)이 제일 명확한 사례 —
문서가 `"1) 업무개요 / 2) 판단근거 / 3) 감사결과 문제점"`처럼 감사보고서의 **진짜 최상위
구조 헤딩**을 번호로 매겼는데, `strict_footnote_nums`가 본문 어딘가의 무관한 "한글N)" 패턴을
이 헤딩과 같은 번호라는 이유로 "각주 참조"로 오인해서 이 문서를 신호② 후보로 잘못 뽑았음.
실제로는 `continuesHeadingList`가 의도대로 정확히 작동해서 이 번호목록을 각주로 오분류하지
않게 막았고, `DetailPage.jsx`는 `heading`으로 **올바르게** 렌더링하고 있었음. 나머지 4건도
전부 같은 패턴(정의 줄이 실제로 heading 블록으로 정상 렌더링됨, `1)→2)` 또는 `1)→2)`
연속 번호 시퀀스).

**결론**: 버그는 `DetailPage.jsx`가 아니라 어제 만든 `classify_footnote_zero_signal`(스캐너
분류 함수) 자체에 있었음 — "정의 줄이 원문에 있으면 무조건 ①"이 너무 단순했음.

**수정(스캐너 스크립트만, 프로덕션 코드는 이번엔 안 건드림)**: `classify_footnote_zero_signal`에
`blocks`를 추가로 넘겨받아, 정의 줄이 **실제로 heading 블록으로 렌더링됐는지**까지 확인 —
heading으로 잘 렌더링됐으면 "②번호목록 헤딩 오탐 의심"(진짜 렌더링 버그 아님)으로 재분류.
스캔 로직에서도 ②로 판정되면 애초에 candidates에 안 넣도록 필터 강화(strict_footnote_nums와
같은 급의 스캔 단계 오탐 제거). 합성 테스트(단위 테스트 3개 + 통합 재현 2개) 전부 통과,
기존 회귀 테스트 전부 그대로 통과.

**주의 — 아직 실 DB 재검증 못 함**: 이번 수정으로 신호②/전체 후보가 얼마나 더 줄어드는지
(② 오탐이 얼마나 있었는지)는 다음 Colab 세션에서 확인 필요. 남은 진짜 ①이 있다면 그것도
확인.

**다음에 할 일**:
1. Colab에서 재스캔 → 후보 건수 변화 확인, 신호② 내부 ①/②/③ 3갈래 재분포 확인
2. 남는 진짜 ①이 있으면 표본 재확인 — 없으면 각주 관련 렌더링 조사는 이 라운드에서 사실상
   마무리, ③(576건, 원본 데이터 유실)만 별도 파이프라인 조사로 남음
3. 표본 조사 전인 카테고리(긴 각주 1,247/긴 불릿문단 885/긴 표잔여 74/heading 0건 411)

## 📊 2026-08-21 (5차) — caption/field 수정 실 DB 확인: ① 85건 → 20건, 후보 3,147→3,102건

caption/field 수정 반영 후 Colab 재스캔. 자가 검증 2단계 그대로 통과
(9ddc6393057cc532=19, 4df12939e14a66c3=11 — 이번 수정과 무관한 케이스라 회귀 없음).

**전수 재스캔**: 후보 3,147건(4.64%) → **3,102건(4.58%)**, 45건 추가 감소.

**신호②(596건) 내부 태그 재분포**:
| 태그 | 이전(3차) | 이번(5차) |
|---|---|---|
| ③ 원본 각주 유실 의심 | 576건(87.1%) | 576건(96.6%, 그대로) |
| ① 렌더링 버그 의심 | 85건(12.9%) | **20건(3.4%)** |

caption/field 수정으로 ①이 85→20건(65건 해결)으로 크게 줄었고, ③은 프론트 수정과
무관하니 그대로 576건 유지 — 정확히 예상대로 움직임. 남은 ① 20건이 새 표본(전과
겹치지 않는 새 문서 5건: `6b06a312a8a35d5a`/`43982cb5cb6cb426`/`d96074185e74aa6f`/
`41e9a7cb98a73a1e`/`5e04ed57c91aa27c`)으로 계속 디버그 추적 중.

**다음에 할 일**:
1. 남은 ① 20건 표본 디버그 추적 결과 대기 — 또 다른 새 패턴이면 계속 수정, 개별
   잡음(1~2건씩 다른 사유)이면 이 라운드는 여기서 마무리하고 다음 카테고리로.
2. ③(576건, 원본 각주 유실) 규모 확정 — 텍스트 추출 파이프라인 별도 조사 착수(아직 미착수).
3. 표본 조사 전인 카테고리(긴 각주 1,247/긴 불릿문단 885/긴 표잔여 74/heading 0건 411).

## ✅ 2026-08-21 (4차) — caption/field 직후 각주 인식 실패 버그 수정(DetailPage.jsx)

①(렌더링 버그 의심) 85건 중 표본 5건(`0f233db923222242`/`a728ba6793fbd689`/
`311c70ac9b5bebb4`/`cae7766501a4fad0`/`49b74b6b3548c4a8`)을 Colab에서 디버그
추적한 결과 — **5건 전부 같은 새 버그**로 확인됨.

**원인**: 각주 판별 조건의 `effectivePrevType` 허용 목록(body/footnote/bullet/
table/heading)에 `caption`("자료:"/"출처:" 표 출처 표기)과 `field`("관계부서
의견" 같은 라벨+값 필드)가 없어서, 이 둘 바로 다음에 각주 정의 줄이 오면
그 각주 자신이 인식 안 되고(짧으면) heading으로 새어버림 — 8/20~8/21에 고친
bullet/table/heading 케이스와 완전히 같은 계열의 버그, 원인만 다른 두 타입.
`a728ba6793fbd689`(한국농어촌공사 2024)에서는 각주 1이 caption 직후 실패하며
`lastHeadingListNum`을 오염시켜 각주 2도 연쇄로 실패하는 것까지 확인(15/16과
동일한 연쇄 패턴).

**수정**: `effectivePrevType` 허용 목록에 `caption`/`field` 추가.
`DetailPage.jsx`/`audit_render_anomalies.py` 양쪽 동일 반영.

**검증**: 실 DB 표본 5건 패턴을 그대로 최소 재현(caption 단독/field 단독/
caption+연쇄 2건/caption 뒤 진짜 번호목록 회귀 없음)해서 Node+Python 양쪽
합성 테스트 전부 통과, 기존 회귀 테스트(15/16, bullet/table, 한국조폐공사 등)도
전부 그대로 통과. `npm run build`/`npm run lint` 통과.

**주의 — 아직 실 DB 재검증 못 함**: 이 세션은 DB 접근이 없어서 표본 5건 문서로
실제 footnote 블록 수가 늘어나는지는 확인 못함. **다음에 반드시**: Colab에서
`audit_render_anomalies.py` 재실행 → 전수 재스캔해서 신호②(661건)의 ①(85건)
비율이 얼마나 줄었는지 확인, 그리고 남는 ① 후보가 있으면 또 새 패턴인지
표본 재확인.

## 📊 2026-08-21 (3차) — Colab 재스캔: 후보 3,147건, 신호②의 87%가 원본 데이터 유실

## 📊 2026-08-21 (3차) — Colab 재스캔: 후보 3,147건, 신호②의 87%가 원본 데이터 유실

`(통보N)`/`(주의N)` 필터 + 신호② 3갈래 태깅을 넣은 `audit_render_anomalies.py`를 Colab에서
재실행.

**전수 재스캔 결과**: 후보 **4,083건(6.03%) → 3,147건(4.64%)**로 936건 대폭 감소 —
`(통보N)`/`(주의N)` 조치유형 코드 오탐 필터가 예상보다 훨씬 큰 효과(감사보고서 전반에
이런 괄호 코드가 흔했던 것으로 보임).

**신호별 재집계**(중복 허용): 긴 각주 1,227 / 긴 불릿문단 885 / 각주 참조 있는데 블록
0건(신호②) 661 / heading 0건 411 / 긴 표잔여 74 / 렌더링 글자수 손실 6.

**신호②(661건) 내부 태그 분포 — 중요한 발견**:
| 태그 | 건수 | 비율 |
|---|---|---|
| ③ 원본 각주 유실 의심 | 576건 | 87.1% |
| ① 렌더링 버그 의심 | 85건 | 12.9% |

어제 `988fef1135220d92` 1건으로 확인했던 "원본 raw_text 자체에 각주 설명이 없음" 패턴이
예외가 아니라 **신호②의 압도적 다수(576건)**였음. 이건 `DetailPage.jsx`를 아무리 고쳐도
해결 불가 — **텍스트 추출/전처리 파이프라인 쪽 별도 조사가 필요한 큰 이슈**로 격상.
남은 ①(렌더링 버그 의심) 85건이 계속 추적할 진짜 대상.

**다음에 할 일**:
1. ①(85건) 표본을 Colab에서 디버그 추적(`debug_footnote_docs.py`, 이번 세션에서
   전달함) — 각주 정의 줄이 실제로 어느 블록에 흡수됐는지 확인해서 아직 못 찾은
   새 렌더링 버그 패턴이 있는지 조사. 표본 5건(`0f233db923222242`,
   `a728ba6793fbd689`, `311c70ac9b5bebb4`, `cae7766501a4fad0`,
   `49b74b6b3548c4a8`) 결과 대기 중.
2. ③(576건) 규모 확정 — 이 576건이 특정 기관/연도/문서양식에 몰려있는지 분포 확인해서
   텍스트 추출 파이프라인의 어느 단계(어떤 PDF 레이아웃)에서 유실되는지 원인 추적
   착수(별도 작업, DetailPage.jsx 범위 밖).
3. 여전히 표본 조사 전인 카테고리(긴 각주 1,227/긴 불릿문단 885/긴 표잔여 74/heading
   0건 411)도 표본 뽑아서 실제 원인 확인 필요.

## 🔧 2026-08-21 (2차) — (통보N)/(주의N) 스캐너 오탐 필터 + 신호② 3갈래 자동 태깅 추가

각주 15/16 수정을 실 DB로 확인한 뒤, "다음 세션 할 일" 2번/1번을 이어서 진행
(`scripts/audit_render_anomalies.py`만 변경 — 프로덕션 `DetailPage.jsx`는 이번엔 안 건드림,
아래 둘 다 스캔 신호 정확도 개선용이라 처음부터 이 스크립트에만 있던 로직).

**1) `(통보N)`/`(주의N)` 조치유형 코드 오탐 제거**: 어제(18b48a4726dad247) 확인한 대로
`FOOTNOTE_REF_STRICT_RE`("한글 글자 뒤 숫자)")가 "(통보1)"/"(주의1)" 같은 조치유형 분류
괄호코드까지 각주 참조로 오인하고 있었음. 처음엔 음수 lookbehind(`(?<!\()`)로 시도했다가
합성 테스트에서 바로 실패함 — "통보"처럼 라벨이 2글자 이상이면 "보1)"만 보면 그 앞 글자가
"통"이라 괄호가 있는 걸 못 잡음(한 글자 lookbehind의 한계). **스팬 비교 방식으로 재설계**:
`ACTION_TYPE_CODE_RE`(`\([가-힣]{1,4}\d{1,2}\)`)로 "(라벨N)" 전체 스팬을 먼저 찾고, 그 스팬
안에 들어가는 `FOOTNOTE_REF_STRICT_RE` 매치만 제외하는 `strict_footnote_nums()` 헬퍼로
교체. 합성 테스트로 "(통보1)"/"(주의1)" 단독은 안 잡히고, 같은 줄에 진짜 각주("금액18)")가
같이 있으면 그건 여전히 잡히는 것 확인.

**2) 남은 신호② 3갈래 자동 분류(`classify_footnote_zero_signal`) 추가**: 위 필터로 원인②
(스캐너 오탐)는 이제 스캔 단계에서부터 후보 목록에 안 들어감(footnote_nums_in_text가
빈 집합이 되므로). 남는 후보는 원인①(렌더링 버그)/③(원본 각주 유실) 둘 중 하나 — 참조된
각주 번호 중 하나라도 원문에 "N) ..." 형태 정의 줄이 실제로 있으면 "①렌더링 버그 의심",
하나도 없으면 "③원본 각주 유실 의심"으로 태깅해서 후보 목록의 reason 문자열에 그대로
붙게 함(사람이 표본 확인할 때 우선순위 좁히는 용도, 최종 원인은 여전히 표본 확인 필요).
988fef1135220d92(원본 유실 확인된 실제 사례)류와 9ddc6393057cc532(렌더링 흡수 확인된
실제 사례)류를 최소 재현한 합성 테스트로 분류 로직 자체는 검증함.

**검증**: 합성 자가 검증(1단계, DB 불필요) 새 케이스 포함 전부 통과. **이 세션은 DB
접근이 없어서 실제 4,083건 후보 중 ①/③ 비율이 얼마나 되는지, (통보N)/(주의N) 필터로
후보가 실제로 몇 건 더 줄어드는지는 아직 확인 못함** — 다음 Colab 세션에서 전수 재스캔
필요.

**다음에 확인할 것**:
1. Colab에서 재스캔 → 후보 건수 추가 감소분 확인((통보N)/(주의N) 필터 효과)
2. 신호② 후보들의 ①/③ 태그 분포 확인 — ③(원본 유실)이 많으면 텍스트 추출 파이프라인
   쪽 별도 조사로 넘기고, ①(렌더링 버그)이 많으면 그 표본들 실제 원문 대조해서 아직
   못 찾은 새 버그 패턴이 있는지 확인
3. `988fef1135220d92`류(원본 각주 유실) 정확한 규모는 이제 ③ 태그 개수로 바로 집계 가능

## ✅ 2026-08-21 — 각주 15/16 버그 수정(DetailPage.jsx) — 실 DB로 최종 확인 완료

어제(8/20) 특정한 원인 그대로 수정: 짧은(24자 이하) 각주 정의 줄이 `classifyLine`의
"짧은 번호 헤딩" 규칙에 걸려 heading으로 잘못 승격되는데, 그 직전 줄이 진짜 heading이면
`effectivePrevType`이 "heading"이 돼서 각주 판별 조건(허용 목록: body/footnote/bullet/
table)에 안 걸림 — 그래서 각주 자신이 각주로 인식조차 안 되고 heading으로 새어버리고,
그 뒤 이어지는 각주도 직전이 "heading"이라 연쇄로 계속 heading이 됨(각주 15/16이 이 경로로
실패).

**수정**: 각주 판별 조건의 `effectivePrevType` 허용 목록에 `"heading"` 추가 —
8/20에 bullet/table을 추가했던 것과 같은 이유. `continuesHeadingList`(연속 번호목록
판별)가 여전히 진짜 번호목록 헤딩과의 오탐을 막아줌. `DetailPage.jsx`와
`scripts/audit_render_anomalies.py` 양쪽 동일 반영.

**검증**: `DetailPage.jsx`의 관련 함수(TITLE_RE~splitIntoBlocks)를 Node로 직접 떼어내
1) 수정 전 재현(heading 바로 다음 짧은 각주 15/16이 실제로 둘 다 heading으로 새는 것
확인) 2) 수정 후 둘 다 footnote로 인식되는 것 확인 3) 기존에 검증됐던 케이스들(한국조폐공사
각주 1/2, 표/불릿 흡수 중 각주 6/9, 일반 표/불릿 회귀, **연속 번호목록이 heading 뒤에
와도 여전히 heading으로 유지되는 것**) 전부 회귀 없음 확인. `npm run build`/`npm run lint`
통과. `scripts/audit_render_anomalies.py`의 합성 자가 검증(1단계, DB 불필요)도 같은
케이스로 전부 통과.

**실 DB로 최종 확인(Colab)**: 자가 검증 2단계에서 `9ddc6393057cc532`가 정확히 기대치대로
**footnote 블록 19건**(기존 17건에서 예측한 대로 +2) 나옴 — 각주 15/16 버그가 실제
데이터로도 해결됐음이 확정됨. `4df12939e14a66c3`도 여전히 11건으로 회귀 없음 확인.

**전수 재스캔 결과**: 후보 **4,137건(6.11%) → 4,083건(6.03%)**로 54건 추가 감소(8/20
불릿/표 흡수 수정 이후 다시 돌린 것 대비). 기관별 상위는 한국산업안전보건공단(273)/
한국자산관리공사(271)/한국지역난방공사(251)/국립공원공단(222)/한국보훈복지의료공단(191)
순으로 어제와 대체로 비슷한 분포. 새 후보 목록:
`/content/drive/MyDrive/audit_project/render_anomaly_candidates.jsonl`(4,083건).

**다음 세션 할 일** (8/20 항목에서 이어짐, 아직 유효):
1. 남은 신호②(각주 참조는 있는데 블록 0건)를 3갈래 원인(①아직 못 고친 렌더링 버그
   ②스캐너 오탐 ③원본 데이터 자체 각주 유실)으로 실제 분류 — 오늘 샘플에도
   `35bb4593636ed16f`/`045bcfdcaf44e763`/`18b48a4726dad247`/`ce73bc695513e89a` 등
   신호② 사례가 여전히 다수 남아있음, 원인별 태깅 로직 작성 필요.
2. `(통보N)`/`(주의N)` 조치유형 코드를 `FOOTNOTE_REF_STRICT_RE`에서 제외하는 필터 추가.
3. `988fef1135220d92`류(원본 각주 유실) 규모 파악.
4. 아직 표본 조사 안 한 카테고리(긴 각주/긴 불릿문단/긴 표잔여/heading 0건) 표본 확인.

## 📋 2026-08-20 마무리 — 재스캔 결과(4,137건) + 남은 문제 3갈래 재분류 + 새 원인(데이터 유실) 발견

바로 아래 "각주 헤딩 오분류 버그 부분 수정" 항목(불릿/표 흡수 수정, PR #33)이
main에 반영된 뒤 `audit_render_anomalies.py`를 다시 전수 실행.

**결과**: 후보 4,997건(7.38%) → **4,137건(6.11%)**. 신호②("각주 참조는 있는데
블록 0건")가 오늘 수정 덕분에 **1,751건 → 실질적으로 대폭 감소**(1차 스캔
11,932건 대비). 카테고리별 재집계:

| 신호 | 건수 | 비율 |
|---|---|---|
| ② 각주 참조는 있는데 블록 0건 | 1,751건 | 42.3% |
| ① 긴 각주 | 1,139건 | 27.5% |
| ① 긴 불릿문단 | 890건 | 21.5% |
| ④ 문서는 긴데 heading 0건 | 411건 | 9.9% |
| ① 긴 표잔여 | 74건 | 1.8% |
| ③ 렌더링 글자수 손실 의심 | 6건 | 0.1% |

(신호 하나가 여러 카테고리에 겹칠 수 있어 합계가 전체 건수보다 큼)

**신호② 표본 재조사로 새 원인 발견**: 남은 신호②(1,751건) 중 `988fef1135220d92`
(한국전력공사 2024, 복잡한 표 위주 문서)를 재확인했더니 — 오늘 고친 불릿/표
흡수 버그와 무관하게, **원본 raw_text 자체에 각주 설명(예: "1) ...")이 어디에도
없음**(문서 전체 grep으로 확인, 렌더링 로직 문제가 아님). "수송요율1)"/
"수송요율2)"처럼 참조 위첨자만 표 안에 남아있고, 그 각주 본문 자체가 텍스트
추출 단계에서 유실된 것으로 추정 — `DetailPage.jsx`를 아무리 고쳐도 원본
데이터에 없는 내용을 보여줄 수 없음. **이건 프론트 렌더링이 아니라 텍스트
추출/전처리 파이프라인 문제**(과거 STATUS.md의 "2-2/2-3 내용 소실"류 데이터
유실 이슈와 같은 계열).

**결론 — 남은 신호②(1,751건)는 최소 3가지 다른 원인이 섞여 있는 것으로 추정**
(정확한 비율은 아직 미분류, 표본 소수만 확인함):
1. 아직 못 고친 렌더링 버그(예: 오늘 못 고친 각주 15/16 — 짧은 숫자 헤딩
   바로 뒤에 각주가 오는 경우, `effectivePrevType==="heading"`에 막힘)
2. 스캐너 신호 자체의 오탐 — `(통보1)`/`(주의1)` 같은 조치유형 코드를
   `FOOTNOTE_REF_STRICT_RE`가 여전히 각주로 오인(`18b48a4726dad247`에서 확인,
   8/20 오전 항목 참고) — 렌더링은 정상, 신호만 더 정밀화하면 됨
3. **[신규]** 원본 데이터 자체에 각주 설명이 없음(`988fef1135220d92`) — 프론트
   수정 불가, 텍스트 추출 파이프라인 조사 필요(별도 작업)

**다음 세션 할 일**:
1. 남은 신호② 1,751건을 위 3갈래로 실제 분류(각 원인에 해당하는 판별
   로직을 짜서 후보를 태깅) — 원인②(스캐너 오탐)·③(데이터 유실)부터
   걸러내면 진짜 렌더링 버그①의 정확한 규모가 나옴.
2. 원인①(각주 15/16류, 짧은 헤딩 직후 각주가 막히는 문제) — `DetailPage.jsx`
   프로덕션 수정 필요, 실제 문서 다수로 회귀 검증하며 진행.
3. `(통보N)`/`(주의N)` 같은 조치유형 코드 패턴을 `FOOTNOTE_REF_STRICT_RE`에서
   제외하는 필터 추가 검토.
4. `988fef1135220d92`류(원본 각주 유실)의 실제 규모 파악 — 신호②에서 "정말
   raw_text에 각주 정의가 없는" 문서가 몇 건인지 별도 스크립트로 확인, 어느
   텍스트 추출 단계에서 유실됐는지 원인 추적(다중 지적사항 보고서/복잡한 표
   레이아웃과 관련 있을 가능성 — 8/13 "선행 상용구 미제거"류 이슈와 함께
   조사).
5. 카테고리 ①(긴 각주 1,139건 + 긴 불릿문단 890건 + 긴 표잔여 74건, 합쳐서
   전체 후보의 절반 가까이)과 ④(heading 0건 411건)는 아직 표본 조사 전 —
   다음 세션에서 각각 몇 건 샘플 뽑아서 실제 원인 확인 필요.
6. 위 작업들 진행 후 self-test 기대값(현재 17/11)을 필요시 재조정하고
   전수 재스캔.

## ✅ 2026-08-20 — 각주 헤딩 오분류 버그 부분 수정(불릿/표 흡수) — DB로 실증 완료

바로 아래 "각주 헤딩 오분류 연쇄 버그" 항목의 근본 원인을 실제로 고침. 처음엔
"헤딩 번호와 각주 번호 충돌(continuesHeadingList)"이 원인이라고 가설을
세웠는데(LIST_HEADING_NUM_RE를 괄호 전용으로 좁힘 + splitLawCitationHeading이
번호 접두어 자신의 괄호를 오인하지 않게 수정), **실제 DB로 검증하니 9ddc639의
footnote 개수가 12에서 전혀 안 바뀜** — 가설이 부분적으로 틀렸던 것으로 판명.

**실제 원인 재규명(Colab에서 `_DEBUG_NUMS` 임시 계측으로 매 줄 effectivePrevType
값을 직접 출력해서 확인, 가설 아님)**:
1. `"* 당시 관련자는..."`처럼 `*`로 시작하는 줄이 불릿 문단을 열면, 그 뒤
   이어지는 평문들이 계속 같은 "bullet" 문단으로 흡수되다가 진짜 각주 정의
   줄(6, 7)까지 삼켜버림 — `effectivePrevType==="bullet"`라서 각주 판별 조건
   자체를 못 탐.
2. `"[표 1] ..."` 캡션 뒤 표 데이터를 흡수하는 중에 각주 정의 줄(9)이 나오면
   마찬가지로 `effectivePrevType==="table"`이라 실패.
3. 6·9가 실패하며 classifyLine의 "80자 이하+문장종결없음" 휴리스틱에 걸려
   잘못 heading으로 승격됨 → `lastHeadingListNum`이 오염돼 8·10이 연쇄로 실패.

**수정**: 각주 판별 조건의 `effectivePrevType` 허용 목록을 `("body","footnote")`
에서 `("body","footnote","bullet","table")`로 확장 — `SOURCE_NOTE_RE`("자료:"/
"출처:")가 표 블록을 강제로 끝내는 경계로 이미 쓰이는 것과 같은 이유로, 진짜
각주 정의 줄(번호가 footnoteNums에 있음)도 불릿/표 문단을 끝내는 경계로 인정함.
`DetailPage.jsx`와 `audit_render_anomalies.py` 양쪽 동일 반영.

**검증**: 실제 원문(사용자가 Colab 디버그 추적으로 확인해준 L61~L109 그대로)으로
Node 통합 테스트 + 최소 격리 단위 테스트(표/불릿 각각) + 회귀 테스트(일반 표/
불릿은 여전히 정상 흡수) 전부 통과 → **실제 DB로 최종 확인: 9ddc639 footnote
12건 → 17건**(예측했던 6/7/8/9/10 다섯 건이 정확히 늘어남). `npm run build`/
`npm run lint` 통과, Python 포팅 Node/Python 13개 케이스 완전 일치.

**남은 것 — 각주 15/16은 여전히 미해결**: 원인은 이미 특정됨 — 짧은 숫자
헤딩(예: "15) 과잉금지의 원칙...")이 24자 이하라 그 자체로 heading이 되고,
그 바로 다음 줄(각주 16)이 `effectivePrevType==="heading"`(직전 heading push
때 para가 비어서)이라 막힘. `continuesHeadingList`와는 무관한, "헤딩 바로
다음에 진짜 각주가 연달아 나오는" 케이스 — 오늘은 여기서 멈춤(사용자 판단:
효과 대비 복잡도가 더 큰 케이스, 다음 세션에서 이어서 진단).

**교훈**: 실제 검증 없이 "이거일 것 같다"는 가설로 self-test 기대값이나
"원인 특정 완료"를 단정하지 말 것 — 이번에 두 번이나(9ddc639, f35fdc468543c358)
가설이 실제와 달랐음. 반드시 Colab에서 직접 debug print로 확인 후 결론 낼 것.

## 📋 2026-08-20 — audit_render_anomalies.py 첫 전수 실행 완료 + 원인 3갈래 확인

`scripts/audit_render_anomalies.py`(PR #29/#30/#31로 최신 `DetailPage.jsx`에
맞춰 재포팅 + self-test 수정 + `FOOTNOTE_REF_STRICT_RE` 추가 완료된 버전)를
Colab에서 **처음으로 전체 67,751건 대상 실행 완료**.

**1차 스캔(오탐 있는 상태)**: 후보 13,601건(20.07%). 신호②("각주 참조는
있는데 블록 0건")가 87.7%(11,932건)로 압도적 — 샘플 5건을 실제 원문으로
직접 확인해보니 **전부 진짜 각주가 아니라 스캐너 신호 자체의 오탐**이었음
(날짜 표기 `'24.06.24)`, 괄호 열거번호 `(10) 그런데...`, 익명화 마스킹
`+0+0+0)` — 전부 `FOOTNOTE_REF_RE`가 너무 느슨해서 생긴 오탐). `FOOTNOTE_REF_STRICT_RE`
(한글 글자 뒤에 붙는 경우만 인정)를 신호② 계산에만 추가해서 반영(PR #31,
`split_into_blocks()` 내부의 프로덕션 미러링 로직 자체는 안 건드림).

**2차 스캔(오탐 필터 반영 후)**: 후보 **4,997건(7.38%)**으로 대폭 감소.
결과: `/content/drive/MyDrive/audit_project/render_anomaly_candidates.jsonl`.
기관별 상위: 한국자산관리공사(454)/한국산업안전보건공단(274)/한국보훈복지의료공단(259)/
국립공원공단(255)/한국지역난방공사(255)/한국가스공사(226) 등.

**남은 4,997건 중 신호② 표본 5건을 추가로 직접 원문 대조 → 원인이 최소
3갈래로 갈림**:
1. **이미 진단한 헤딩/각주 번호 충돌 버그**(위 8/20 첫 항목 참고) —
   `f35fdc468543c358`(한국조폐공사 2016)에서 재확인. 헤딩이 "1."/"2."로
   번호 매겨지고 각주도 "1)"/"2)"라 `lastHeadingListNum`이 오염됨.
2. **[신규 발견] 표(table) 블록에 각주가 흡수됨** — `988fef1135220d92`
   (한국전력공사 2024, 표 위주 문서)에서 확인. 각주 정의 줄이
   `effectivePrevType === "table"`일 때 나오면 `("body"|"footnote")`
   조건에 안 걸려서 표 블록에 그냥 흡수됨. 위 1번과 완전히 다른 코드
   경로 — `splitIntoBlocks`의 표 처리 분기 쪽 별도 버그.
3. **[신규 발견] 스캐너 신호 자체의 오탐 잔존** — `18b48a4726dad247`에서
   `(통보1)`/`(주의1)`(각주가 아니라 조치유형 분류 코드)를 `FOOTNOTE_REF_STRICT_RE`가
   여전히 각주 참조로 오인함("통보"/"주의" 둘 다 한글이라 필터를 통과함).
   `045bcfdcaf44e763`/`35bb4593636ed16f`는 앞부분만으론 원인 미확정.

**사용자 결정**: 오늘은 여기까지 — 3갈래 원인 전부 다음 세션(DB 접근 있는
Colab, 실제 문서로 회귀 검증 가능한 환경)에서 진행.

**다음 세션 할 일**:
1. `render_anomaly_candidates.jsonl`(4,997건)을 위 3갈래 원인별로 실제
   분류(각 원인에 해당하는 구체적 판별 로직을 짜서 후보를 태깅) — 원인③
   (스캐너 오탐)부터 걸러내면 진짜 렌더링 버그 규모가 더 좁혀질 것.
2. 원인①(헤딩/각주 번호 충돌, `continuesHeadingList`)과 원인②(표 흡수)
   둘 다 `DetailPage.jsx` 프로덕션 코드 수정 필요 — 각각 실제 문서 다수로
   회귀 없는지 검증하며 신중히 진행(기존에 여러 라운드 튜닝된 로직이라
   섣부른 수정은 다른 케이스를 깰 위험 있음).
3. `(통보N)`/`(주의N)` 같은 조치유형 코드 패턴을 `FOOTNOTE_REF_STRICT_RE`에서
   제외하는 추가 필터 검토(한글이면 다 인정하는 대신, 조치유형 키워드
   뒤는 제외하는 식) — 스캐너 신호만 더 정밀화.
4. 위 수정들 반영 후 `9ddc6393057cc532`의 self-test 기대값을 12 → 16으로
   복원하고(원인① 수정 시), 전수 재스캔으로 최종 규모 확정.

## 🐛 각주 헤딩 오분류 연쇄 버그 발견 — DetailPage.jsx 실제 프로덕션 버그 (2026-08-20)

`audit_render_anomalies.py`를 최신 `DetailPage.jsx`(PR #21 병합분)에 맞춰
재포팅(PR #29)한 뒤 Colab에서 처음 실행하니, 자가 검증 2단계(실제 문서)에서
한국자산관리공사 2021(`9ddc6393057cc532`) 문서의 footnote 블록이 기존 기대값
16건이 아니라 **12건**으로 나와서 실패함. Node로 실제 `DetailPage.jsx`와
포팅본을 나란히 대조해서 확인한 결과 — **포팅 오류가 아니라 main에 이미
병합된 프로덕션 코드 자체의 실제 렌더링 버그**임을 확인.

**근본 원인** (`frontend/src/pages/DetailPage.jsx`):
1. `splitLawCitationHeading()`이 "따옴표 앞에 `)`가 하나라도 있으면 법령인용
   라벨"로 판단하는데, 이 조건이 너무 느슨해서 **각주 번호 자체의 괄호**
   (`7) ... '블라인드'의 ...`처럼 `7)`의 `)`)까지 "법령 인용 괄호"로 오인함.
   그 결과 각주 정의 줄이 heading으로 잘못 쪼개짐(라벨/인용문 분리).
2. `classifyLine()`의 "80자 이하 + 문장종결 없음" 휴리스틱(원래 "1) 공사
   「행동강령」 제12조(...) 제3항"처럼 짧은 법령 라벨을 위해 만든 예외)도,
   PDF 줄바꿈으로 문장 중간에 끊긴 긴 각주 정의 줄에 오발동해서 heading으로
   오분류시킴.
3. 위 두 오분류로 `lastHeadingListNum`이 각주 번호(예: 7)로 잘못 세팅되면,
   `continuesHeadingList`(번호목록 연속 판정) 가드가 **그 다음 각주**(예: 8)를
   "목록 연속"으로 오판해서 각주로 인식을 아예 안 시킴 — 한 번 어긋나면 뒤
   각주들까지 연쇄로 body에 흡수됨. 실제 문서에서 각주 6~10, 15~16(총 7건)이
   이렇게 새어나감. 8/19 시점 self-test 주석의 "6/7/8은 별도 각주 정의 없이
   실제로 존재 안 함"이라는 설명 자체가 틀렸었음(실제론 다 존재, 렌더링이
   흡수한 것) — 아무도 실제 각주 개수를 다시 세어본 적이 없었던 것으로 보임.

**임시 조치**: 근본 수정(`continuesHeadingList`가 "헤딩 번호"와 "각주 번호"를
구분 못 하는 문제)은 실제 문서 전수로 회귀 검증이 필요해서 이번엔 보류 —
DB 접근 없는 세션이라 안전하게 검증 못 함. 대신 `audit_render_anomalies.py`의
self-test 기대값만 현재(버그 있는) 동작에 맞춰 12로 수정해서 전수 스캔을
막지 않게 함(주석에 이 버그 설명 남겨둠, PR #29 이후 커밋).

**다음 세션(DB 접근 있는 Colab) 할 일**:
1. `splitLawCitationHeading()`의 괄호 판정을 "번호 접두어(`^\d{1,2}[.)]`) 이후
   구간에서만" 찾도록 좁히기 — 각주 번호 자체의 괄호를 법령인용 괄호로
   오인하는 문제(위 1번)는 이걸로 해결 가능, 비교적 안전한 수정.
2. `classifyLine()`의 80자 휴리스틱과 `continuesHeadingList`는 더 근본적 —
   "각주 번호"와 "헤딩/목록 번호"가 같은 패턴(`N)`/`N.`)을 공유해서 생기는
   구조적 모호함이라, 실제 문서 다수로 회귀 없는지 검증하며 신중히 접근할 것.
3. 위 수정 후 `audit_render_anomalies.py`의 `9ddc6393057cc532` 기대값을
   16으로 되돌리고, 전수 스캔을 다시 돌려서 같은 패턴(각주 번호 근처에
   인용부호나 법령조항이 있는 문서)이 몇 건이나 더 있는지 규모 파악.
4. 이 버그 자체가 "구조 이상 전수조사"의 4개 탐지 신호(긴 블록/각주참조인데
   0건/글자수 손실/heading 0건) 중 어느 것에도 안 걸릴 수 있음(각주가
   완전히 0건이 되는 게 아니라 일부만 샘) — 새 신호("각주 참조 개수 대비
   실제 각주 블록 개수 비율이 크게 낮은 경우") 추가를 검토할 것.

## 📋 2026-08-19 마무리 정리 — 오늘 한 일 + 내일 할 일

### ✅ 오늘 완료
1. **2026-08-18 "내일 할 일" 3개 항목 전부 완료**: HWP 표 손실 복구(1,559건),
   "1. 제목:" 번호 유실 DB 수정(4,305건, 재임베딩 누락 발견·복구까지), HWP
   태그 누출 규모조사(0건, 문제 없음 확인).
2. **원문 렌더링 버그 4건**을 사용자 스크린샷 제보로 발견·수정·배포 완료
   (`main`에 머지되어 실서비스 반영됨 — PR #26, #27):
   - 표 셀 값 "X"가 로마숫자 장 제목으로 오인 (`HEADING_LABEL_PATTERNS` 라틴 IVX 제거)
   - 각주가 괄호 뒤 공백 없으면 인식 안 됨 (`FOOTNOTE_DEF_RE` `\s+`→`\s*`)
   - 각주가 인식돼도 한 줄만 잡히고 이어지는 줄이 새어나감 (문단 누적 방식으로 변경)
   - 각주 누적 중 불릿 줄("- 행위요건 : ...")을 만나면 각주 인식이 끊겨서 뒤이은
     각주 번호들이 전부 그 불릿 문단에 흡수됨 (각주 중 불릿은 흡수하도록 예외 추가)
   - 가독성 CSS: 문단 첫 줄 들여쓰기 추가, 검색어 하이라이트 굵기 완화(700→600)
3. **같은 날 다른 세션의 PR #24**(제목 번호 유실 화면 표시 보정)를 병합해서
   통합 — DB 수정과 보완 관계임을 확인, 충돌 없음.
4. **"스크린샷으로 하나씩 찾는 방식은 안 지속가능하다"는 지적**에 따라
   `scripts/audit_render_anomalies.py`(신규, 읽기 전용) 작성 — `DetailPage.jsx`의
   렌더링 로직을 Python으로 포팅해서 전체 문서를 스캔, "구조가 이상해 보이는"
   후보를 자동으로 뽑아냄(비정상적으로 긴 각주/불릿/표 블록, 각주 참조는
   있는데 각주 블록이 0건, 렌더링 글자수와 원본 글자수 큰 차이, 긴 문서인데
   heading 0건). 로컬에서 오늘 실제 검증한 문서 2건(9ddc6393057cc532 —
   각주 16건, 4df12939e14a66c3 — 각주 11건)으로 포팅 정확성 자가검증 완료
   (실행 맨 앞에서 자동으로 재확인 후 안 맞으면 중단하도록 설계) — **아직
   Colab에서 전체 67,751건 대상으로 실제 실행은 안 함, 다음 세션 할 일**.

### 📋 내일(다음 세션) 할 일
1. **[최우선] `scripts/audit_render_anomalies.py`를 Colab에서 실제 실행** —
   전체 문서 대상 렌더링 구조 이상 후보 목록 확보, 기관/패턴별로 분류해서
   오늘 고친 3가지 각주 버그 외에 또 다른 반복 패턴이 있는지 확인. 이게
   오늘 사용자가 지적한 "스크린샷 하나씩 찾는 건 지속 불가능"의 실질적 해법.
2. 운영 사이트에서 오늘 반영된 렌더링 수정사항들 실제 브라우저로 최종 확인
   (이 세션은 프록시 문제로 헤드리스 브라우저 스크린샷이 안 됐음 — 대신
   실제 raw_text로 Node 시뮬레이션 검증만 함).
3. HWP 표 손실 타임아웃 30건 재시도, 나머지 모집단(표본 밖 ~29,663건) 확대
   적용 여부 결정.
4. 단어 중간 줄바꿈 문제(16,171건, 23.87%) — 여전히 미해결, 정탐률 표본
   확인부터 필요.
5. 제목 번호 유실 수정 중 상위 10개 기관 스코프 밖 잔여 ~32건 재임베딩
   커버리지 확인.
6. 검색결과 카드/상세페이지 레이아웃/정렬·필터링 UX 개편 — 아키텍처 검토
   문서(Artifact) 작성 완료, 방향 결정 대기 중.
7. 검색 랭킹 이슈(기관명으로 검색해도 다른 기관 문서가 위에 뜨는 경우) —
   원인은 규명함(RRF가 청크 내용만 보고 institution 필드는 랭킹에 관여 안
   함), 아직 수정 안 함 — 기관명 매치 가중치 부여 여부 결정 필요.

## 🔀 병합 메모 — 제목 번호 유실, 같은 날 두 세션이 다른 각도로 고침 (2026-08-19)

이 세션(DB 접근 가능, Colab)은 `documents.raw_text`/`chunks.text`를 직접
고쳤고(바로 아래 "✅ '1. 제목:' 번호 유실 수정 완료" 항목), 별도 세션(DB
접근 없음, 프론트만)은 같은 문제를 화면 표시 보정(`fixMissingTitleNumber()`,
PR #24)으로 고쳐서 `main`에 먼저 병합함 — 두 수정이 서로 충돌하지 않고
보완 관계임을 확인 후 병합함:
- DB가 이미 고쳐진 4,305건은 `fixMissingTitleNumber()`의
  `TITLE_BLOCK_LEADING_NUM_RE.test()` 가드에 걸려 보정을 건너뜀(이미 "1. "이
  있으므로) — 중복 적용 없음.
- DB 수정이 못 미친 나머지(상위 10개 기관 스코프 밖 ~32건 등)는 이
  프론트 보정이 안전망 역할을 함.
- `frontend/src/pages/DetailPage.jsx`는 두 세션의 변경(각주 처리 수정 +
  isTitle/fixMissingTitleNumber)이 자동 병합(git merge)됨, 충돌 없음 —
  `npm run build` 정상 확인.

## ✅ HWP 내부 태그 누출(hc: 계열 등) 규모조사 — 0건, 문제 없음 확인 (2026-08-19)

`scripts/audit_hwp_tag_leak.py`(8/13 작성, 지금까지 실행 기록이 STATUS.md에
한 번도 없었음)를 처음으로 실제 실행. 전체 문서 67,751건 대상 —
`hp:run`/`hp:linesegarray`(기존에 고쳤다고 알던 것), `hc:도형/차트 속성`
(8/13 신규 발견), `textpos=`/`vertpos=`, `linkListNextIDRef=`, `outlineS`
5개 패턴 계열 전부 **0건**. 8/13 당시 실제 문서(한국수목원정원관리원 등)에서
누출을 발견했었는데, 그 사이(8/14~8/18) 여러 재청킹/재임베딩 라운드를
거치면서 관련 문서들이 이미 재처리되어 해소된 것으로 추정(정확한 원인은
추적 안 함, 결과만 확인). **추가 조치 불필요 — 오늘 할 일 3번 항목 완료,
더 이상 손볼 것 없음.**

이걸로 2026-08-18 "내일 할 일" 3개 항목(HWP 표 손실 복구, 제목 번호 유실
수정, HWP 태그 누출 조사) 전부 마무리됨.

## ✅ "1. 제목:" 번호 유실 수정 완료 + Drive 동기화로 인한 재임베딩 누락 복구 (2026-08-19)

`scripts/audit_title_number_and_wordbreak.py` 규모조사(4,305건, 6.35%,
한국보훈복지의료공단/한국자산관리공사/한국토지주택공사에 96.3% 집중)에
이어 실제 수정 스크립트 `scripts/fix_title_number_loss.py`(신규)를 작성해
반영. `fix_text_corruption.py`와 같은 "정규식으로 그 자리에서 패치" 패턴
(구조 변경 없음, 재청킹 불필요) — 로컬에서 실제 샘플 3건 + 엣지케이스로
`fix_title_number()` 검증 후 DRY_RUN=True→False로 진행.

**DRY_RUN 결과(정상)**: 수정 대상 4,305건, 샘플 3건 다 의도대로 "1. " 삽입
확인. **실반영**: `documents.raw_text` 4,305건 UPDATE, 관련
`chunks.text` 4,377건 UPDATE(첫 청크가 raw_text 맨 앞과 같은 줄로 시작하는
경우만 매칭), 백업(`title_number_fix_backup_*.jsonl`) + 재임베딩 입력
(`reembed_input.jsonl`) 저장까지 스크립트 자체 로그로는 정상 완료.

**막혔던 것 — GPU 런타임 전환 시 Drive FUSE 마운트 미동기화로 재임베딩
입력/백업 파일이 통째로 유실**: `documents`/`chunks` UPDATE는 Postgres에
직접 반영되는 거라 안전했지만, 그 직후 Drive에 쓴 `reembed_input.jsonl`
(4,377건)과 백업 파일은 GPU 런타임 전환 중 컨테이너가 교체되면서 실제
Drive 클라우드에 동기화되기 전에 유실된 것으로 추정됨(로컬 버퍼는 성공
응답을 줬지만 실제 파일 mtime 확인 결과 8/13 — 오늘 쓴 게 아니었음).
그 결과 `reembed_changed_chunks.py`(공용 재임베딩 스크립트)가 완전히
무관한 8/13자 옛 파일(259건, 다른 작업 잔재)을 읽었고, 그 259건마저
**공유 체크포인트(`reembed_checkpoint.jsonl`, 여러 수정 작업이 공유하는
35,022건 누적 파일)에 이미 들어있어서 실제로는 아무것도 새로 계산되지
않고 옛 벡터를 그대로 재기록**함 — `chunks.text`는 고쳐졌는데
`chunks.embedding`은 안 바뀐 상태로 남을 뻔했음.

**복구**: 백업 파일이 유실돼서 정확한 chunk_id 목록을 재구성할 수 없어,
상위 10개 기관(전체 4,305건의 99.3% 커버) 범위로 좁혀 "1. 제목" 패턴이
이미 붙어있는(=수정 완료된) 문서/청크를 다시 스캔해서 4,380건 재구성 →
**공유 체크포인트를 쓰지 않는 전용 체크포인트 파일**로 강제 재계산 →
벡터 norm 평균 0.999994(표준편차 0.000172), 4,380건 전부 DB 반영 확인.
문서 텍스트 수정 자체는 재조사(`audit_title_number_and_wordbreak.py`
재실행)로 0건(완전 반영) 확인 완료.

**교훈 — 다음에 비슷한 작업할 때 참고**:
1. Colab에서 **런타임 유형을 바꾸면(CPU↔GPU) 완전히 새 컨테이너가
   배정되며 Drive FUSE 마운트가 끊기고, 직전 로컬 쓰기가 실제 Drive
   클라우드에 동기화됐는지 보장이 안 됨** — 중요한 파일(백업, 재임베딩
   입력)을 쓴 직후 런타임을 전환해야 한다면, 전환 전에 파일 존재/크기를
   한 번 더 확인하거나 잠시 대기 후 전환할 것.
2. `reembed_changed_chunks.py`의 체크포인트(`reembed_checkpoint.jsonl`)는
   **여러 수정 작업이 공유하는 파일이라 chunk_id만 보고 "이미 완료"로
   판단** — 같은 chunk_id를 다른 이유로 이미 임베딩한 적이 있으면(텍스트가
   그 사이 바뀌었어도) 옛 벡터를 그대로 재사용해버림. 텍스트만 바뀌고
   구조(청크 경계)는 안 바뀌는 종류의 수정(이번 건처럼)을 재임베딩할 땐
   **전용 체크포인트 파일명을 쓰는 게 안전**함.

**알려진 작은 공백**: 상위 10개 기관 스코프 밖의 나머지 ~32건(전체
4,305건의 0.7%)은 이번 재임베딩 범위에서 빠졌을 수 있음 — "1. " 세 글자
차이라 임베딩 벡터에 미치는 영향은 미미할 것으로 추정되지만, 완벽을
기하려면 다음 세션에서 이 기관 롱테일만 별도로 확인 필요.

**남은 항목 3**: HWP 내부 태그 누출(hc: 계열 등) 규모조사
(`scripts/audit_hwp_tag_leak.py`)는 아직 실행 안 함 — 다음 세션 할 일.

## ✅ 구버전 HWP 표 손실 복구 — DRY_RUN 검증 + 실제 반영 완료 (2026-08-19)

어제(8/18) 표본조사에서 확인된 영향 문서(표본 1,730건)를 대상으로
`scripts/rechunk_reembed_hwp_table_fix.py`를 Colab에서 실제로 돌림.

**막혔던 것 — Python 3.12 + 구버전 setuptools 조합에서 `hwp5txt`가 전부 조용히
실패**: 1차 DRY_RUN 결과가 자동반영 0건/수동검토 1730건(전부
`low_similarity_after_merge`, `n_markers=0`)으로 나와서 이상 신호로 보고 원인
추적. `hwp5txt` 서브프로세스가 매번 `AttributeError: module 'pkgutil' has no
attribute 'ImpImporter'`로 죽고 있었음(Python 3.12에서 제거된 API를 pyhwp
빌드용으로 강제 설치한 `setuptools<60`의 옛 `pkg_resources`가 임포트 시점에
호출) — 스크립트가 `hwp5txt`의 리턴코드를 확인 안 하고 빈 stdout을 그대로
"표 없음"으로 처리해서 에러 없이 조용히 전부 실패했던 것.
- **해결**: `pyhwp` 빌드 후 `pip install -U "setuptools<82"`로 setuptools를
  다시 최신(82 미만, torch 요구사항과도 호환)으로 복구 — `pkg_resources`가
  최신 버전으로 바뀌면서 `pkgutil.ImpImporter` 참조가 사라져 정상화.
  (참고: 같은 런타임 내에서 pip로 setuptools를 여러 번 갈아끼우면 `Ignoring
  invalid distribution ~etuptools` 형태로 pip 메타데이터가 깨질 수 있음 —
  이땐 "세션 다시 시작"이 아니라 "런타임 연결 해제 및 삭제"로 디스크까지
  완전히 새로 받아야 해결됨.)
- 다음번엔 설치 순서를 `pip install "setuptools<60" pyhwp` →
  `pip install -U "setuptools<82"` → 나머지 패키지 순으로 **한 셀에서 한번에**
  실행할 것 — 스크립트 상단 주석에도 이 순서 반영 권장.

**재실행 DRY_RUN 결과(정상)**: 자동반영 1,560건 / 수동검토 170건
(`marker_table_count_mismatch` 26건, `low_similarity_after_merge` 114건,
타임아웃 30건). 수동검토 사유 표본 검증:
- 표/마커 개수 불일치(26건) 상위 사례는 표 8~13개짜리 문서 — 안전장치가
  의도대로 작동(8/18에 우려했던 표 62·66개 outlier는 이번 표본엔 없었음).
- 유사도 낮음(114건) 중 임계값(0.90)에 가장 가까운 사례(0.8955,
  `3416ed0276ec540d`)를 DB 원문과 직접 대조 — 표 제외 본문은 한 글자도
  안 틀리고 일치, 차이는 전부 "현지조치 요구서" 표지 상용구 + "1. 제목"의
  "1." 번호 접두어뿐이었음. 즉 8/14(9차)부터 알려진 "선행 상용구 미제거" /
  "제목 번호 유실" 한계가 이 게이트에 그대로 걸린 것으로, 병합 로직 자체의
  결함이 아님 — 이번 범위에서 손대지 않기로 함(다중 지적사항 보고서
  표지/목차까지 날아갈 위험 때문에 계속 보류 중인 사안, 아래 항목 참고).
- 타임아웃 30건은 전부 `hwp5html --output` 90초(1건은 `hwp5txt` 60초) 초과
  — 병합 로직과 무관, 표 많은 문서 처리 속도 문제로 추정.

**실반영(DRY_RUN=False) 완료**: `documents.raw_text` UPDATE **1,559건**
(DRY_RUN 재현 시 1,560건과 1건 차이 — 정상 범위, 타임아웃 경계 편차로 추정),
청크 3,744개 재생성, GPU 재임베딩 3,744건 전부 완료(벡터 norm 평균
1.000000, 표준편차 0.000175, 임베딩 누락 0건 — 검증 통과). 백업:
`hwp_table_fix_backup_20260819_015525.jsonl`(1,559건, 원본 raw_text+chunks
보존).

표본(3,426건, affected 1,730건) 기준 90.2%를 이번에 복구. 전체 모집단
33,089건(추정 영향 16,709건)에 대한 전수 반영은 아직 안 함 — 이번엔
8/18 표본조사 체크포인트 대상만 처리.

**다음 세션에서 할 일**:
1. 운영 사이트(Vercel)에서 실제 렌더링으로 몇 건 눈으로 확인 — 표가
   `"셀 | 셀"` 형태로 저장되는데 `DetailPage.jsx`가 이 파이프 구분자를
   표답게 렌더링하는지 아직 미확인(스크립트 주석에도 명시된 리스크).
2. 수동검토 170건 중 타임아웃 30건은 `hwp5html` 타임아웃 값을 늘려서
   (예: 90s → 180s) 재시도해볼 만함 — 병합 로직 문제 아님.
3. 표본(3,426건) 밖의 나머지 모집단(약 29,663건)에 대해서도 이 복구를
   확대 적용할지 결정 — 표본 기반 추정 영향률(50.5%)을 적용하면 추가로
   1만건 이상 영향받았을 가능성.
4. "선행 상용구 미제거" 문제(8/14 9차부터 보류 중)는 여전히 미해결 —
   이번에도 다시 확인됨(같은 패턴). 언젠가 다중 지적사항 보고서와 개별
   처분요구서 스타일을 구분하는 조건부 로직으로 접근 필요.

## ✅ 원문 렌더링 버그 2건 추가 수정 — 로마숫자 오탐(X) + 각주 공백/한줄 제한 (2026-08-19)

사용자가 실제 배포 화면 스크린샷으로 제보한 렌더링 버그 2건을 원인
특정 후 수정. 둘 다 라이브 API(`auditweb-production-4482.up.railway.app`)로
실제 문서 원문을 받아서 Node로 `splitIntoBlocks`/`classifyLine` 로직을
그대로 재현·검증함(이 세션은 DB 접근 없어서 이 방법으로 검증).

1. **표 셀 값 "X"가 로마숫자 장 제목으로 오인**: `HEADING_LABEL_PATTERNS`의
   로마숫자 패턴이 유니코드 로마숫자(Ⅰ,Ⅱ,Ⅲ...)뿐 아니라 라틴 알파벳
   I/V/X까지 문자 클래스에 포함해서, 표의 "인증여부" 셀 값 "X"로 시작하는
   줄이 장 제목으로 오인됨(제보: [표 3] 웹 접근성 품질 미인증 현황).
   라틴 알파벳 버전이 실제 검증된 사례가 이 저장소에 없어서 제거.
2. **각주가 괄호 뒤 공백 없으면 인식 안 되고, 인식돼도 한 줄만 잡힘**:
   실제 문서(한국자산관리공사 2021, `9ddc6393057cc532`)로 확인 — ①
   `FOOTNOTE_DEF_RE`가 "숫자)" 뒤 공백을 필수(`\s+`)로 요구해서
   `18)「감사규정」...`처럼 공백 없이 바로 이어지는 각주가 아예 인식 안
   되고 직전 문단에 흡수됨(`\s*`로 완화), ② 각주로 인식돼도 그 줄 하나만
   push하고 끝나서 PDF 줄바꿈으로 이어진 다음 줄이 별도 "body" 문단으로
   새고 있었음(bullet/table과 같은 문단 누적 방식으로 변경).
   각주 1~33개짜리 실제 문서 여러 건으로 회귀 없음 확인(엉뚱한 내용 삼킴 없음).

**병합**: 같은 날 다른 세션이 만든 `fixMissingTitleNumber()`/PR #24와
`FOOTNOTE_DEF_RE`/heading push 부분이 겹쳐서 `git merge origin/main` 시
`frontend/src/pages/DetailPage.jsx`는 자동 병합 성공(충돌 없음), 위
"🔀 병합 메모" 항목 참고. `npm run build` 정상.

## ✅ 제목 번호 유실 — 화면 표시 보정 반영 (2026-08-19)

DB(raw_text) 백필은 이 세션에서 DB 쓰기 접근이 없어 불가능해서, 대신
`frontend/src/pages/DetailPage.jsx`의 `splitIntoBlocks()`가 프론트 렌더링
단계에서만 보정하도록 수정(`fixMissingTitleNumber()` 추가). 원문 데이터는
그대로 두고 화면에만 "1. "을 붙여서 보여줌.

- 조건: 제목 블록에 번호가 없는데 바로 다음 헤딩이 "2."(이상)로 시작할 때만
  보정(조사 스크립트와 동일 조건이라 오탐 위험 낮음, 아래 케이스3 확인).
- 기존에 `TITLE_RE`를 렌더링/목차 두 곳에서 텍스트 재검사하던 방식이라, 보정
  텍스트에 "1. "을 붙이면 재검사가 깨져서(제목 스타일/목차 제외 오작동) 블록
  생성 시점에 `isTitle` 플래그를 한 번만 계산해 저장하고 그 플래그로만
  판단하도록 같이 리팩터링함.
- 검증: 노드로 `splitIntoBlocks`/`buildToc`만 떼어내서 3가지 케이스 확인 —
  ①실제 버그 재현(번호 유실 + "1. " 보정 + isTitle/목차 정상 유지),
  ②이미 번호 있는 정상 문서(회귀 없음 — 원래도 "1. 제목..." 형태는 isTitle
  판정 안 되는 기존 동작 그대로, 이번 수정과 무관한 별개 사안),
  ③번호 체계 자체가 없는 문서(오탐 없이 보정 안 함).
- `npm run lint`(oxlint), `npm run build`(vite) 통과.
- 실제 배포 화면으로는 미확인 — 병합 후 브라우저 재확인 필요.
- **다음**: 줄바꿈 의심(16,171건, 23.87%) 수정은 아직 미착수 — 정탐률 표본
  확인부터 필요.

## ✅ 제목 번호 유실 + 단어 중간 줄바꿈 규모조사 완료 (2026-08-19)

`scripts/audit_title_number_and_wordbreak.py`를 Colab에서 실행(읽기 전용,
DB 미반영). `documents` 테이블 전체 **67,751건** 전수 스캔.

**결과**:

1) **"1. 제목:" 번호 유실** — **4,330건 / 67,751건 (6.39%)**
   - 기관별 상위: 한국보훈복지의료공단(2,885), 한국자산관리공사(767),
     한국토지주택공사(516), 해양환경공단(81), 코레일관광개발(12),
     울산항만공사(11) 등 — 상위 3개 기관에 4,168건(전체 유실분의 96.2%)이
     쏠려있고 나머지는 소수 기관에 산발적으로 분포.
   - 샘플로 확인한 패턴: 원본이 "1. 제 목 : ..." / "2. 관계기관 : ..."
     구조인데 raw_text엔 제목 줄 맨 앞 "1. "만 사라진 채 저장돼 있음
     (2026-08-18 한국보훈복지의료공단 사례와 동일 패턴).

2) **단어 중간 줄바꿈 의심**(근사 휴리스틱 — 오탐/누락 있을 수 있음) —
   의심 문서 **16,171건 / 67,751건 (23.87%)**, 의심 지점 총합
   **39,426건**(문서당 평균 2.4건).
   - 특정 기관 쏠림 없이 광범위(샘플에 한국산업기술시험원·한국보훈복지의료공단·
     한국석유공사·한국수력원자력·한국가스공사·한국문화예술위원회 등 다양하게 등장).
   - 샘플 확인 결과 실제로 단어 중간에서 끊긴 사례로 보임(예: "...주의의무"
     → "를 지며..."는 "주의의무를 지며"가 두 줄로 쪼개진 것).

**판단/다음 계획**:
- 번호 유실(4,330건)은 전체 대비 규모는 작지만 상위 3개 기관에 집중돼 있어
  손보는 효율이 좋음 — raw_text 자체가 적재 시점부터 "1. " 없이 저장된
  상태라 프론트(`DetailPage.jsx`)에서 보정할지, DB 백필로 채워넣을지 방법
  검토 필요.
- 줄바꿈 의심(16,171건, 23.87%)은 규모가 커서 무시하기 어려운데, 휴리스틱
  기반이라 정탐률 확인이 먼저 필요 — 표본을 더 뽑아 오탐 비율 가늠 후,
  `splitIntoBlocks()`에서 병합 시 조사/어미 앞 공백을 스마트하게 제거하는
  방향을 검토(단, 정상적으로 조사로 시작하는 문장을 훼손할 부작용 위험
  있어 신중히 접근할 것).
- 다음 세션: 이 두 건 수정 우선순위/방법 논의 + 1번(HWP 표 손실 DRY_RUN),
  3번(hc: 태그 누출 조사) 진행 상황 확인.

## 🔀 병합 메모 — 2026-08-18 두 세션이 병렬로 남긴 요약 두 건 (PR #21 병합, 2026-08-20)

같은 날(8/18) 서로 다른 트랙에서 작업한 두 세션이 각자 STATUS.md에 "오늘 한
일 + 내일 할 일" 요약을 남김: 이 세션(DB 접근 가능, Colab — HWP 표 손실/제목
번호 유실 등)은 바로 아래 "📋 내일 할 일 (2026-08-18 마무리 정리)"를, 별도
세션(DB 접근 없음, 프론트 렌더링 버그 수정 — PR #21)은 자체 브랜치에 "📌
2026-08-18 하루 요약 + 내일 할 일"을 남김. 8/19의 "제목 번호 유실" 건과
마찬가지로(위 "🔀 병합 메모" 참고) 두 트랙은 서로 겹치지 않는 별개 작업이라
충돌 없이 병렬 진행됐음 — PR #21을 8/20에 `main`에 병합하면서 두 요약을
시간순으로 나란히 배치함. PR #21 요약 안의 "내일 할 일" 항목 중
`DATABASE_URL` 등록/2-2·2-3 내용 소실 등은 이후 다른 세션에서 실제로
진행됐는지 위 8/19 항목들과 대조해 확인이 필요할 수 있음(예: 2-2/2-3 내용
소실은 8/19 이후로도 STATUS.md에 재등장하지 않아 여전히 미해결로 보임).

## 📋 내일 할 일 (2026-08-18 마무리 정리)

오늘 하루 HWP 표 손실 문제 하나를 원인 특정 → 규모조사(전수 시도 → 표본으로
전환) → 복구 스크립트 작성까지 끝냄. 우선순위 순으로 정리:

1. **[최우선] HWP 표 손실 복구 DRY_RUN 실행** — 원인·규모(전체 33,089건 중
   약 50.5%, 추정 16,709건) 다 확인됐고 복구 스크립트(`scripts/rechunk_reembed_hwp_table_fix.py`)도
   작성 완료된 상태. Colab에서 `DRY_RUN=True`로 돌려서 자동반영/수동검토 갈림
   비율, 병합 샘플, 표 개수 많은 outlier(최대 66개) 문서가 마커/표 개수
   불일치로 안전하게 걸러지는지 확인 → 이상 없으면 `DRY_RUN=False`로 반영
   (백업 자동 생성됨). 이 스크립트 자체는 이 세션(DB 접근 없는 컨테이너)에서
   합성 데이터로만 단위테스트했고 실제 문서 검증은 아직 없었음 — DRY_RUN
   결과를 꼼꼼히 볼 것.
2. ~~**제목 번호 유실 + 단어 중간 줄바꿈 규모조사 실행**~~ — **완료(2026-08-19)**,
   위 섹션 참고. 번호 유실 4,330건(6.39%)은 화면 표시 보정까지 반영 완료
   (위 "화면 표시 보정 반영" 섹션). 줄바꿈 의심 16,171건(23.87%)은 아직
   미착수 — 휴리스틱 정탐률 표본 확인부터 필요, 다음 할 일로 남음.
3. **HWP 내부 태그 누출(hc: 계열 등) 규모조사 결과 확인** — `scripts/audit_hwp_tag_leak.py`
   (2026-08-13 작성)가 지금까지 STATUS.md에 실행 결과가 한 번도 기록된 적
   없음 — 이미 돌려봤는데 기록만 안 남긴 건지, 아예 안 돌려본 건지부터
   확인하고 필요하면 재실행.

**(할 일 목록에서 제외함)** Railway 백엔드 공개 도메인이 반복적으로 바뀌던
문제 — 코드/인프라 결함이 아니라 GitHub 연동을 계속 지웠다 다시 연결해서
생기는 것으로 확인됨(사용자 확인). 매번 새 도메인이 발급되는 게 원인이라
연동을 다시 지우지만 않으면 재발 안 함 — 커스텀 도메인 설정 자체는 여전히
안 해뒀지만 더 이상 급한 할 일은 아님.

## 📌 2026-08-18 하루 요약 + 내일 할 일 (PR #21 세션 — 프론트 렌더링 트랙)

오늘 하루 두 트랙이 동시에 진행됨: ①이 세션(Claude Code, 아래 1~6차) — 사용자가 실제
배포 사이트 스크린샷을 계속 보내주면서 실시간으로 프론트 렌더링 버그를 검증하며 수정
②**사용자가 Colab에서 병행 진행한 PDF 2단 레이아웃 재추출 작업(PR #20, `main`에 이미
병합 완료)** — 바로 아래에 그 상세 기록이 있음. 이 세션이 "컬럼 순서 뒤섞임(한국철도공사
2020, `dec56dc84bfe3a6c`)은 raw_text 자체에선 재현 안 된다"고 판단했던 게 실은 **이
PR #20 작업이 같은 시간대에 이미 DB에 반영해놓은 상태를 본 것**이었음(1,424건 반영
완료) — 두 트랙이 우연히 같은 문제를 다른 경로로 다뤘던 것으로 정리됨.

**병합 작업**: PR #21(이 세션의 프론트 커밋들)이 `main`(PR #20 병합 후)과 `STATUS.md`/
`DetailPage.jsx` 둘 다에서 충돌 발생 → `main`을 이 브랜치로 머지해서 직접 해결함
(`DetailPage.jsx`는 두 브랜치가 독립적으로 발견한 "번호목록 헤딩 과다 굵게" 버그를 각자
다른 방식으로 고쳐서 생긴 로직 충돌 — main의 24자 길이 상한 + 이 세션의 법령인용
분리 로직을 함께 살리는 조건으로 병합함, 아래 6차 이후 항목 뒤에 병합 상세 기록 예정).

### 오늘 한 일 (이 세션 기준, 1~6차 — 전부 실제 raw_text/스크린샷으로 검증, `vite build`/`oxlint` 매번 통과)
1. **프론트 렌더링 버그 다수 수정** (`frontend/src/pages/DetailPage.jsx`) — 아래 1~5차 참고:
   - 소관부서/조치부서 등 필드 라벨만 굵게(값은 보통체)로 분리
   - 번호목록 헤딩에 딸려온 인용문까지 과굵게 되던 버그, 연속 번호목록 각주 오분류 버그
   - "◯1"/"◤"/"•" 등 원문 글리프 인식 누락, "□" 단독줄이 다음 줄과 안 합쳐지던 버그
   - "제목" 뒤 따옴표 때문에 TITLE_RE 안 걸리던 버그
   - 워터마크 추정 노이즈 줄 캡션 처리, "C-/D-" 인물목록 줄바꿈, 표 블록 줄바꿈 보존
2. **"2-2/2-3 내용 소실" 정확한 원인 처음 특정** — `build_final_dataset.py`가 제목이
   같은 여러 사례(원본 PDF 한 장에 (2-1)/(2-2)/(2-3) 같이 들어있는 경우)를 매칭 단계에서
   하나로 뭉갬. `chunks`엔 다 남아있고 `documents.raw_text`만 한 사례로 뭉개짐 확인.
   **코드 수정은 아직 — case_number를 매칭 키에 추가하는 방향 검토 필요(3차 참고).**
   이 문제는 아래 PR #20의 컬럼 순서 문제와는 **별개**(원인이 다름) — 계속 미해결.
3. **HWP 내부 속성명 누출("array"/"bList"/"hasNumRef=" 등) — 기존에 알려진 것보다 훨씬
   넓게 퍼져있는 것 확인, 수정 코드까지 작성·로컬 검증 완료**(`scripts/
   fix_text_corruption.py`) — **DB 미반영**(쓰기 권한 없음, 5~6차 참고). PR #20의
   "구버전 HWP 표 내용 손실" 조사와도 별개 문제(하나는 표 내용이 통째로 빠진 경우,
   이건 속성명 텍스트가 잘못 섞여 들어온 경우).
4. **DB 접근 문제 확인** — 이 세션(Claude Code)엔 DB 접근 가능한 환경이 따로 없고,
   사용자가 이 환경("Default")에 `DATABASE_URL` 환경변수를 직접 등록하기로 함 —
   **아직 등록 전**. (참고로 PR #20 작업은 이 세션이 아니라 사용자가 직접 Colab에서
   진행한 것 — 이 프로젝트의 기존 DB 작업 방식 그대로.)

### ⏭️ 내일 할 일 (우선순위 순)
1. **`DATABASE_URL` 등록 확인** — 사용자가 claude.ai/code 환경 설정에 등록하기로 함
   (Railway Postgres `DATABASE_PUBLIC_URL`, Public Networking 켜져 있어야 함). 등록
   후엔 **새 세션에서 시작해야 인식됨**(환경변수는 컨테이너 시작 시점에 주입됨 — 등록
   전에 떠 있던 세션은 재확인해도 안 잡힐 수 있음). 새 세션 시작하자마자
   `echo $DATABASE_URL` 또는 `psycopg2.connect()`로 먼저 확인.
2. **DB 접근 되면 — `fix_text_corruption.py` HWP 누출 수정 반영** (6차에서 코드+로컬
   검증까지 끝남, 아래 순서만 남음):
   a. `documents_backup_20260818`/`chunks_backup_20260818` 백업 테이블 생성
   b. **훨씬 넓은 실제 샘플(수백~수천 건)로 로컬 재검증** — 이번 세션은 5건만 검증함,
      특히 카멜케이스 패턴(`CAMEL_CASE_TOKEN_RE`)이 진짜 한글 내용을 잘못 지우는 오탐이
      있는지 전수에 가깝게 확인 필요(6차 "다음 세션 할 일" 참고)
   c. 문제없으면 `documents.raw_text`/`chunks.text` 반영
   d. 바뀐 chunk만 골라 BGE-m3 재임베딩(Colab GPU) → `chunks.embedding` UPDATE
   e. HNSW 인덱스는 대량 UPDATE 전에 임시 DROP → 작업 → VACUUM → 재생성(이 프로젝트
      반복된 교훈, 안 하면 볼륨 부족/느려짐)
3. **"2-2/2-3 내용 소실" 규모 파악 + 수정 방향 확정** — DB 접근 있으면 같은
   (source_file, title) 조합이 여러 case_number에 걸쳐 나오는 문서가 몇 건인지 SQL로
   집계(3차 항목 참고). `build_final_dataset.py` 매칭 키에 case_number 추가하는 안
   검토.
4. **PR #21 병합 + Vercel 재배포 확인** — `main` 병합해서 충돌 해결까지 끝냄, 이제 PR
   자체는 병합 가능한 상태. 병합 후 실제 화면에서 오늘 고친 프론트 버그들(특히
   한국철도공사 2020 `dec56dc84bfe3a6c` — PR #20으로 컬럼 순서도 고쳐지고 이 세션
   수정으로 "◯N" 헤딩 스타일도 고쳐져서 이중으로 나아졌을 것, 한국가스공사 2021
   `7a766550f0f873b7`, 한국임업진흥원 2026 `bf1dbb1bfa7043dd`)이 스크린샷대로
   반영됐는지 확인.
5. **PR #20의 미완 작업 이어가기** — `scripts/audit_hwp_table_loss.py`(구버전 HWP
   표 내용 손실 규모조사) 아직 실행 완료 안 됨, 수동검토 큐 713건(선행 상용구/표
   뒤섞임)도 미반영 상태로 남아있음(아래 PR #20 상세 기록 참고).
6. *(여유 있으면)* "참고" 캡션(작은글씨+굵게+번호) 스타일 — 아직 실제 예시를 못 찾음,
   사용자가 스크린샷/문서 주면 그때 진행.

## ✅ 구버전 HWP 표 손실 규모조사 완료 — 표본조사 결과 (2026-08-18)

`audit_hwp_table_loss.py`를 표본조사(3,000건 목표, 실제로는 이미 처리된 것까지
합쳐 **3,426건** 처리됨 — 이전 전수조사 실행분 3,426건이 체크포인트에 이미 다
쌓여 있어서 표본이 그대로 그 크기가 됨)로 완주.

**결과 — 예상보다 훨씬 큰 규모로 확인됨**:
- 대상 `.hwp` 문서 전체: **33,089건**
- 표본(3,426건) 내 영향받음: **1,730건 (50.5%)**
- 전체 환산 추정: 약 **16,709건** (오차범위 대략 ±2%p, 95% 신뢰수준)
- 기관별 상위: 한국국토정보공사(162), 한국산업안전보건공단(140), 한국지역난방공사(136),
  한국동서발전(91), 주택도시보증공사(88), 한국수자원공사(86) 등 — 특정 기관에 쏠리지
  않고 광범위하게 분포.
- 표 개수 분포: 대부분 1~5개(495/384/301/197/131건)이지만, 표 62개·66개짜리 문서도
  1건씩 있는 등 롱테일 존재 — 이런 outlier는 `rechunk_reembed_hwp_table_fix.py`의
  마커/표 개수 일치 검증이 제대로 걸러내는지 DRY_RUN에서 특히 확인 필요.

**결론**: 절반이 영향받는 규모라 복구 스크립트(`rechunk_reembed_hwp_table_fix.py`,
이미 작성해둠)를 실제로 돌릴 가치가 명확히 있음.
- **다음 세션에서 할 일**: `rechunk_reembed_hwp_table_fix.py`를 DRY_RUN=True로
  실행 — 자동반영/수동검토 갈림 비율, 병합 샘플, 특히 표 개수 많은 outlier 문서의
  마커/표 개수 일치 여부 확인 후 DRY_RUN=False로 반영.

## 🚧 구버전 HWP 표 손실 규모조사 — 전수조사가 너무 느려서 표본조사로 전환 (2026-08-18)

`audit_hwp_table_loss.py`를 실제 3만건 규모로 돌려보니(연결 재사용 패치 전)
25분에 2,000건 — 완주까지 5시간 이상 걸릴 속도. 이 조사의 목적 자체가 "복구
스크립트를 만들 가치가 있는지 판단할 영향 비율"이라 전수조사가 필요 없다고
판단, `RANDOM_SAMPLE_SIZE = 3000` 무작위 표본조사로 전환함(이미 처리된 문서는
버리지 않고 표본에 우선 포함해 재사용, `random.seed(42)`로 재현 가능하게 고정).
3,000건 표본이면 모집단 3만 기준 비율 추정 오차범위 대략 ±2%p(95% 신뢰수준)로
규모 판단용으로 충분. 결과 리포트도 "표본 내 비율 → 전체 환산 추정치"로 출력하게
고침. 정확한 전수 집계가 필요해지면 `RANDOM_SAMPLE_SIZE = None`으로 되돌리면 됨.
- **다음 세션에서 할 일**: 표본조사 결과(추정 영향 건수) 확인하고, 복구 스크립트
  (`rechunk_reembed_hwp_table_fix.py`) DRY_RUN 진행할지 결정.

## 🚧 구버전 HWP 표 내용 손실 — 복구(merge) 스크립트 작성 (2026-08-18, 이어서)

바로 아래 항목(원인 특정 + `audit_hwp_table_loss.py` 규모조사)에 이어서, 실제
반영용 복구 스크립트 `scripts/rechunk_reembed_hwp_table_fix.py`(신규)를 미리
작성해둠 — `audit_hwp_table_loss.py`를 Colab에서 돌려 체크포인트를 만드는 동안
병행 준비.

- **이 세션(audit_web 컨테이너)은 DB/jsdelivr 접근이 없어서**(egress 정책상
  차단, `DATABASE_URL`도 미설정) 실제 문서로 끝까지 검증은 못 함. `pip install
  pyhwp`로 `hwp5txt`/`hwp5html` CLI 자체는 설치·실행 확인했고, `<table>` 파싱
  +병합 로직은 합성(가짜) HTML/텍스트로 단위 테스트해서 정상 동작 확인함(스크립트
  헤더에 이 한계와 확인된 범위를 명시해둠).
- **구조**: 기존 `rechunk_reembed_pdf_column_fix*.py`와 동일한 패턴(DRY_RUN 게이트
  → 백업 → UPDATE → DELETE/INSERT 청크 → GPU 재임베딩, `split_into_chunks`도 그대로
  복사해서 재사용).
- **병합 방식**: `hwp5txt`의 `<표>` 마커와 `hwp5html`의 `<table>` 등장 순서가 1:1
  대응한다는 이미 검증된 사실(한전KPS 2018 문서, 6개 표)을 이용 — 마커 개수와 실제
  뽑힌 표 개수가 다르면 자동반영 안 하고 수동검토 큐로 뺌. 표 셀은 `"셀 | 셀"`로
  펼침(파이프 구분자는 `audit_hwp_table_loss.py`의 `db_has_table_trace` 휴리스틱과
  일부러 맞춘 것 — 정착된 컨벤션은 아니라서 실제 반영 전 상세페이지 렌더링과
  어울리는지 확인 권장).
- **안전장치**: `<표>`를 그냥 제거만 한 "마커제거 기준텍스트"(기존 파이프라인이
  DB에 저장해온 방식 그대로 재현)를 DB 옛 `raw_text`와 비교해서 유사도
  `REMOVE_TABLE_SIMILARITY_THRESHOLD=0.90` 미만이면 자동반영 안 함 — 표 내용이
  아니라 "표 이외 부분까지 이 재추출이 원본과 다르게 뽑았는가"만 걸러내는 게이트.
- **다음 세션에서 할 일**: 1) `audit_hwp_table_loss.py` Colab 실행 결과(영향 규모)
  확인, 2) 이 스크립트를 실제로 DRY_RUN=True로 돌려서 자동반영/수동검토 갈림과
  병합 샘플이 말이 되는지 확인(특히 마커/표 개수 불일치 비율 — hwp5html 출력
  파일 구성이 문서마다 다를 수 있어 실측 필요), 3) 문제없으면 DRY_RUN=False로
  반영.

## 🚧 구버전 HWP 표 내용 손실 — 원인 특정 + 규모조사 스크립트 착수 (2026-08-18)

PDF 3차 반영 후 운영 사이트 실사용 확인 중, 한전KPS 2018(cb8dcfbd7983ba43)에서
"3. 관련자 현황", "Ⅲ. 신분상 조치 요구" 같은 섹션 제목 다음에 내용이 완전히
비어있는 걸 발견 — PDF 작업과 무관한 별개의 HWP(구버전 바이너리) 이슈.

**원인 특정**: 이 저장소 pyhwp 설치(`pip install pyhwp`, setuptools<60 필요 —
최신 setuptools에서 빌드 실패함) 후 실제 파일로 직접 검증:
- `hwp5txt`(현재 파이프라인이 쓰는 도구)는 표가 있던 자리에 `<표>` placeholder만
  남기고 실제 셀 내용은 애초에 텍스트로 못 뽑음(도구 자체의 한계).
- DB `raw_text`엔 그 `<표>` placeholder마저 어딘가에서 필터링돼서 표가
  있었다는 흔적조차 안 남음(`<그림>`은 "그림"으로 괄호만 벗겨져 남아있는 것과
  대조적).
- `hwp5html`로 같은 파일을 열어보면 표 내용이 실제로 복구됨(소속/직급/직무,
  징계양정/조치근거 등). `hwp5txt`의 `<표>` 등장 순서와 `hwp5html`의 `<table>`
  등장 순서가 1:1로 정확히 매칭되는 것도 직접 검증(6개 표, 6개 마커 정확히
  일치) — 두 출력을 합치면 온전한 문서 재구성 가능함을 확인.

**`scripts/audit_hwp_table_loss.py`(신규, 읽기 전용)**: 전체 `.hwp` 문서
(`.hwpx` 제외 — XML 기반이라 다른 이슈) 대상으로 이 문제가 몇 건이나 되는지
조사만 함(DB 미반영). 체크포인트 지원. 동시성 8로 시작했다가 실측(`hwp5txt`
1건 ~0.29초, GIL 안 잡음)해서 24로 상향.
- **아직 실행 완료 안 됨** — 다음 세션에서 결과 확인하고, 영향 규모 보고 실제
  반영 스크립트(hwp5txt+hwp5html 병합) 만들지 결정할 것.

## ✅ PDF 2단 레이아웃 재추출 3차 — 원문자 서식 버그 수정 + 578건 touch-up (2026-08-18)

1차+2차로 반영된 1,424건을 운영 사이트(Vercel)에서 실제로 눈으로 확인하다가 새
버그 2개 발견, 둘 다 수정 완료:

1. **원문자(◯1, ◯2...) 숫자가 다음 줄로 분리되는 버그** — 실제 PDF 좌표 확인:
   "◯"와 본문은 y0=134.8인데, 원 안에 겹쳐 그려지는 작은 숫자(폰트 9.2pt vs
   6.5pt)는 y0=136.5로 1.7pt 차이 나서 `_merge_same_row_fragments()`의
   `y_tol=1.0` 허용오차를 넘어가 별도 줄로 분리되고 있었음. `y_tol=2.0`으로
   수정(`scripts/reextract_pdf_text.py`) — 한국공항공사 2단 인쇄 변종 문서로
   회귀 없음 확인.
2. **번호 항목(1. 2. 3.) 헤딩 오탐** — `frontend/src/pages/DetailPage.jsx`의
   헤딩 판정 정규식(`/^\d{1,2}[.)]\s+\S/`)이 줄 길이를 안 봐서, "1. 관계 법령
   및 규정"처럼 짧은 소제목 뒤에 줄바꿈 없이 긴 본문이 바로 이어붙은 경우(원본
   PDF부터 그렇게 한 줄, 한국문화예술위원회 2019 e6bae4491398a6b2로 확인) 문단
   전체가 헤딩(진하게)으로 잘못 렌더링됨. 가/나/다 소제목 판정과 동일한 방식으로
   길이 상한(24자) 추가.

**`scripts/rechunk_reembed_pdf_column_fix_round3.py`(신규)**: 1번 수정을
반영한 최신 추출 로직으로 이미 반영된 1,424건을 다시 돌려 서식만 touch-up.
이미 순서는 맞아서 1차와 동일한 단순 유사도 게이트만 사용.
- DRY_RUN 결과: 서식개선 578건, 완전동일(스킵) 789건, 유사도 낮음(재확인)
  57건 → `pdf_reextract_manual_review_round3.jsonl`.
- 반영 완료: `documents.raw_text` 578건 UPDATE, 청크 1,638개 재생성 +
  재임베딩 완료(벡터 norm 평균 0.999995, 임베딩 누락 0건). 백업:
  `pdf_reextract_backup_round3_20260818_043559.jsonl`.
- 운영 사이트 API로 직접 재확인: `dec56dc84bfe3a6c` raw_text에 "◯1 음주관리..."
  정상 병합 확인.

**그 외 이번 검증 중 발견했지만 스코프 밖이라 손 안 댄 것(다음 세션용)**:
- 일부 문서 raw_text에서 "1. 제목:"의 "1."이 원본 데이터 적재 시점부터 유실됨
  (한국보훈복지의료공단 2022 01ff5cfbb6fafddd로 확인, 이 저장소 코드가 지운 게
  아니라 원본 raw_text 자체에 없음) — 전수 규모 조사용 읽기 전용 스크립트
  `scripts/audit_title_number_and_wordbreak.py` 작성해둠(아직 실행 안 함).
- 단어 중간 줄바꿈("복귀하였음에 도" 등) — DetailPage.jsx가 원문 줄들을 공백
  으로 이어붙이는데, PDF가 원래 단어 중간에서 줄을 끊은 경우까지 공백이 들어감
  (2026-08-12부터 알려진 한계, 완벽한 해결은 원문 재구조화 필요, 코퍼스 전체
  영향 규모 미확인).
- 포스터/인증서 스타일 일부 문서에서 제목이 원본 PDF 자체에 디자인 효과로
  2~3번 겹쳐 그려져 있어 추출 결과에도 그대로 중복 등장(강원개발공사 2023
  91a76bca37e56cf3로 확인) — 정보 손실은 아니고 코스메틱 이슈, 우선순위 낮음.
- Railway 백엔드 공개 도메인이 이 세션 중 3번 이상 바뀜(`-cb0e` → `-0b13`)
  — 매번 Vercel `VITE_API_BASE_URL` + Railway `FRONTEND_ORIGIN` 재설정
  필요했던 반복 문제, 커스텀 도메인 설정 재권장.

## ✅ PDF 2단 레이아웃 재추출 2차 — 순서만 바뀐 문서 702건 추가 구제 (2026-08-18)

1차(아래 항목) 반영 후 실제 웹사이트(운영 Vercel)에서 눈으로 확인하다가, 이
프로젝트의 원조 검증 기준 문서였던 한국철도공사 2020(`dec56dc84bfe3a6c`)조차
여전히 옛(뒤섞인) 원문 그대로인 걸 발견 — 직접 재현 검증해서 원인 특정:

- **핵심 문제(설계 결함)**: 1차의 유사도 게이트(`SIMILARITY_THRESHOLD=0.90`,
  difflib 순서기반)는 "옛 텍스트와 얼마나 비슷한가"를 재는데, 이 프로젝트
  목적 자체가 "뒤섞인 옛 텍스트를 순서대로 고치는 것"이라 **가장 성공적으로
  고쳐진 문서일수록(뒤섞임이 심했을수록) 옛 텍스트와 제일 달라져서 게이트에
  걸리는 역설**이 있었음.
- 실제 재현: `dec56dc84bfe3a6c`를 재추출하면 "◯1...◯5..." 뒤섞인 목록이
  1→2→...→8 정확한 순서로 복구되지만, 옛 DB 원문과의 순서기반 유사도는
  **0.535**(임계값 미달). 반면 글자(공백 제외) 멀티셋 유사도는 **1.0**, 길이
  비율도 **1.013** — 내용 손실/추가 없는 순수 재정렬임이 수치로 확인됨.
- **`scripts/rechunk_reembed_pdf_column_fix_round2.py`(신규)**: 1차 수동검토
  큐(1,415건)를 재검사 — 순서기반 유사도 미달이어도 순서 무시 멀티셋 유사도
  ≥0.97 + 길이 비율 0.95~1.05면 "순서만 바뀐 안전한 케이스"로 추가 반영.
  표 뒤섞임/선행 상용구 문제(실제 글자 내용이 달라짐)는 이 기준에서도 자연히
  계속 걸러짐.
  - DRY_RUN 결과: 추가 반영 **702건**(전부 순서무시/재정렬 기준 통과, 순서기반
    통과는 0건 — 즉 702건 전부가 1차 게이트의 역설에 걸렸던 케이스), 여전히
    수동검토 필요 **713건**.
  - 반영 완료: `documents.raw_text` 702건 UPDATE, 청크 1,458개 재생성 +
    재임베딩 전부 완료(벡터 norm 평균 0.999994, 임베딩 누락 0건).
  - 백업: `pdf_reextract_backup_round2_*.jsonl`(Drive).
- **결과**: 1차+2차 합산 **1,424건**(722+702) 반영 완료, 수동검토 남은 건
  **713건**(선행 상용구 미제거 / 표 뒤섞임 등 실제 내용이 다른 문서들 — 이건
  글자 유사도 방식으로는 못 구제함, 별도 접근 필요).
- **참고**: Railway 백엔드 공개 도메인이 이 세션 중에도 또 바뀌어서(`-cb0e`도
  이미 죽음) 반복 문제가 재확인됨 — 아래 여러 항목에 이미 기록된 고질적
  이슈, 커스텀 도메인 설정 강력 권장.

## ✅ PDF 2단 레이아웃 재추출 반영 완료 — 722건 (2026-08-18)

`scripts/reextract_pdf_text.py`(2단 순서+띄어쓰기 복구 프로토타입)를 실제로 DB에
반영. 새 스크립트 두 개 추가:

- **`scripts/audit_pdf_column_layout.py` 재실행 + Drive 저장 버그 수정**: 원래
  체크포인트를 Colab 로컬(`/content/`) 상대경로에 저장해서 세션 재시작 시 유실되는
  문제가 있었음 → Drive 경로로 변경. 전체 30,120건 PDF 문서 재검사, **2,137건**
  2단 레이아웃 후보 발견(이전 세션의 "약 2,111건" 추정과 거의 일치).
- **`scripts/rechunk_reembed_pdf_column_fix.py`(신규)**: `claude/data-preprocessing-next-steps-wksl8h`
  브랜치의 `split_into_chunks()`를 그대로 재사용(별도로 새로 설계하지 않음). 신구
  raw_text 유사도(difflib) 게이트(임계값 0.90)를 걸어서 재추출 결과가 미덥지 않은
  문서는 자동 반영 안 하고 수동검토 큐로 분리하는 안전장치 포함.
  - 1차 DRY_RUN(2,137건 대상): 자동반영 674건 / 수동검토 1,463건(68.5%).
    탈락률이 예상보다 높아서 샘플 4건 실제 DB원문 대조로 원인 특정:
    1) 마스킹 기호 정규화 누락(☆☆병원, ▽▽과처럼 기존 5종 `○◎□●■` 밖의 기호) —
       **고침**(별/삼각형 계열 `☆★△▲▽▼◇◆`까지 확장, `reextract_pdf_text.py`도 동기화).
    2) 선행 상용구 미제거("현지조치 요구서..." 표지 문구) — 기존에 알려진 미해결
       한계, 그대로 둠.
    3) [신규 발견, 미해결] 폰트/서브셋 경계에서 숫자·괄호 조각이 엉뚱한 위치로
       밀려나는 경우(`_merge_same_row_fragments()` 엣지케이스, 예: "제4조(감사의
       구분)에" → "제 조 감사의 구분 에 4 ( )"). 원인은 특정했지만 고치면 다른
       정상 케이스를 깨뜨릴 위험이 있어 보류 — 유사도 게이트가 이미 정확히
       걸러내고 있어 급하지 않음.
    → 결론: 68.5% 탈락은 게이트 오작동이 아니라 정상 필터링으로 확인됨.
  - 1번만 고친 뒤 재실행: 자동반영 **722건**(+48) / 수동검토 1,415건.
  - **DB 반영 완료**: UPDATE 직전 대상 문서의 raw_text+chunks를 Drive에 타임스탬프
    백업(`pdf_reextract_backup_*.jsonl`, 이 저장소엔 `backup_before_fix.py`류가
    없어서 인라인으로 추가) → `documents.raw_text` UPDATE 722건 → 옛 청크 DELETE →
    새 청크 1,593개 INSERT → GPU(T4) 임베딩 1,593건 전부 완료(벡터 norm 평균
    0.999996, 임베딩 안 된 청크 0건으로 검증 완료).
- **수동검토 큐 1,415건**(`pdf_reextract_manual_review.jsonl`)은 이번에 DB
  미반영 — 선행 상용구 제거 로직 추가하거나, 표 뒤섞임 문서를 별도 방식으로
  다루기로 결정한 뒤 다음에 처리. 급하지 않음(DB 오염 없이 안전하게 남겨둔 상태).
## ✅ HWP 속성명 누출 수정 코드 작성 + 로컬 검증 완료 (2026-08-18, 6차)

바로 아래 5차에서 "코드 수정은 이번에도 안 함"이라고 남겼는데, 사용자가 "고쳐줘야지"라고
재요청 → 다시 생각해보니 **DB에 직접 반영은 못 해도(쓰기 권한 없음), `scripts/
fix_text_corruption.py` 코드 자체는 이 세션에서 작성·검증까지 할 수 있었음**(이 프로젝트가
그동안 여러 번 써온 패턴 — "권한 없는 세션은 스크립트만 작성해두고 다음 세션이 실행"과
동일). 실제로 다음 라운드까지 진행함.

### 한 것 — `scripts/fix_text_corruption.py`의 `strip_hwpml_leak()` 확장
1. **`HWP_LEAK_MARKER` 가드를 대폭 확장** — 기존 `hp:run|hp:lineseg|hp:sz|hp:pos`에
   2026-08-13 조사 결과(`hc:\w+`/`textpos=`/`vertpos=`/`linkListNextIDRef=`/`outlineS`)와
   이번에 발견한 것(`hasNumRef=`/`hasTextRef=`/`linkListIDRef=`/`width=`/`bottom=`/
   `merged=`) 추가.
2. **카멜케이스 토큰 일괄 제거(`CAMEL_CASE_TOKEN_RE`)** — 접두어(hp:/hc:)도 없고
   `="값"`도 안 붙은 "맨 토큰"(`array`, `bList`, `hasN`, `hasT`, `linkLi`, `styleIDRef`
   등)은 하나하나 나열하는 대신, "소문자로 시작해서 중간에 대문자가 나오는" 형태
   (`\b[a-z]{1,10}[A-Z][A-Za-z0-9]*\b`)를 통째로 제거하는 방식 채택 — 이 말뭉치(한글
   전용, 대문자 약어만 드물게 섞임)엔 이런 형태가 자연 발생할 수 없다는 전제. "array"/
   "para"처럼 카멜케이스가 아닌 순수 소문자 토큰만 별도로 정확히 나열(`BARE_LOWER_TOKEN_RE`,
   오탐 위험 있어서 좁게).
3. **줄바꿈으로 갈라진 속성("id=\n\"2147483648\"")과 이름 없이 값만 남은 속성
   ("=\"16\"")도 추가로 처리** — `TAG_ATTR_SPLIT_RE`/`ORPHAN_TAG_VALUE_RE`.

### 로컬 검증 (DB 없이, 이 세션이 이번 대화에서 실제 API로 받아뒀던 raw_text 5건으로)
- **정상 문서 3건(가스공사/철도공사/토지주택공사)**: `full_fix()` 적용 전후 **완전히
  바이트 단위로 동일** — 가드가 안 열려서 손도 안 댐, 회귀 없음 확인.
- **오염 문서 2건(대구경북/임업진흥원)**: `array`/`bList`/`hasN`/`hasT`/`linkLi`/
  `styleIDRef`/`id="..."`/`="16"` 전부 깔끔하게 제거되고 나머지 실제 내용(한글 문장,
  표 셀 숫자/대시)은 그대로 보존되는 것을 직접 눈으로 확인.
- **오탐 방지 회귀**: "TFT 소속으로 KOVIS 시스템을 이용해 BSC 평가를 수행함
  hasNumRef=\"1\""같은 합성 문장으로, 진짜 대문자 약어(TFT/KOVIS/BSC)는 안 지워지고
  가짜 속성(hasNumRef=)만 지워지는 것 확인.
- `python3 -m py_compile` 문법 검사 통과.

### 여전히 DB 미반영 — 다음 세션(DB 접근 있을 때) 할 일
1. **반영 전 필수 절차(이 프로젝트 기존 원칙)**: `documents_backup_<날짜>`/
   `chunks_backup_<날짜>` 백업 → 훨씬 넓은 실제 샘플(수백~수천 건)로 로컬 CSV 재검증
   (이번 5건은 표본이 너무 작음, 특히 카멜케이스 오탐이 실제로 없는지 대량 검증 필요)
   → `documents.raw_text`/`chunks.text` 반영 → 바뀐 chunk만 재임베딩(Colab GPU) →
   `chunks.embedding` UPDATE.
2. 영향 문서 수는 여전히 미확인 — `scripts/audit_hwp_tag_leak.py` 스타일로 새
   `HWP_LEAK_MARKER`(카멜케이스 포함) 기준 전수조사 먼저 하면 정확한 규모 파악 가능.
3. `id=`/`="16"`류(줄바꿈으로 갈라진 속성, 이름 없는 속성)는 이번에 딱 1건(임업진흥원)
   으로만 확인함 — 더 넓은 샘플에서 이 패턴이 실제로 흔한지도 같이 확인.

## ✅ 제목/불릿 렌더링 버그 3건 수정 + 🔍 HWP 속성명 누출 재발 확인(범위 확대) (2026-08-18, 5차)

### 렌더링 버그 3건 수정 (`DetailPage.jsx`, 실제 raw_text로 검증)
1. **`제목" 가정의달 공직기강...` 처럼 "목" 바로 뒤에 따옴표가 남아서 TITLE_RE가
   전혀 매칭 안 되던 버그** — 한국임업진흥원 2026(`bf1dbb1bfa7043dd`)으로 확인
   ("제목이 눈에 안 띈다"는 피드백). 2026-08-14에 대괄호를 선택으로 만든 것과 같은
   이유로, 따옴표(스트레이트/커브)도 선택 문자에 추가.
2. **"•"(U+2022) 불릿이 BULLET_RE 문자 집합에 빠져있던 버그** — 한전KPS 2018
   특정감사로 확인. "• '12~'16년...) / - 144,346,500원을...) 처럼 "•"/"-"가 같은
   목록의 상위/하위 항목인데 "•"만 body로 분류돼 CSS 여백이 "-" 줄들과 달라서
   "문단 나누는 게 이상하다"는 피드백으로 이어짐 — "•" 추가로 통일.
3. **"□" 기호 혼자만 있는 줄이 다음 줄(진짜 소제목 텍스트)과 안 합쳐지던 버그** —
   같은 한국임업진흥원 문서에서 "□\n점검 개요"처럼 기호와 글자가 줄바꿈으로 갈라져
   있어서, 기호 줄도 뒤에 오는 글자가 없어 BULLET_RE에 안 걸리고, "점검 개요" 줄도
   다른 패턴이 없어서 둘 다 그냥 body로 앞 문단(제목 문단)에 흡수되고 있었음(위 1번
   버그와 겹쳐서 더 눈에 띔). `pendingGlyph`로 기호 단독 줄을 보류했다가 다음 줄과
   합쳐서 "□ 점검 개요"로 재구성 — 못 합쳐지고 문단이 끊기면(빈 줄 등) 기호를 잃지
   않고 독립 조각으로 살려둠.

**검증**: 위 3건 다 한국임업진흥원 실제 raw_text로 Node 검증(제목 헤딩 분리 확인,
"□ 점검 개요"/"□ 점검 결과(총괄)..." 정상 병합 확인) + 한전KPS는 스크린샷 텍스트
재구성으로 검증 + 기존 5개 실제 문서(가스공사/철도공사/토지주택공사/대구경북/
임업진흥원) 전체 회귀 확인. `vite build`/`oxlint` 통과.

### 🔍 HWP 내부 속성명 누출 — 기존에 알려진 것보다 훨씬 넓은 변종 확인(코드 수정 안 함)
사용자가 대구경북첨단의료산업진흥재단 2025 복무감사(`7ae6b5f5cecf06ae`)에서
"array"/"bList"/"hasN" 같은 원문에 없는 이상한 영단어를 제보 → 조사해보니 **완전히
새로운 문제가 아니라, 2026-08-07(1차)·2026-08-13(2차, `scripts/audit_hwp_tag_leak.py`)에
이미 발견했던 "HWP XML 누출" 문제의 세 번째 변종**이었음(2026-08-07엔 `hp:run` 같은
네임스페이스 태그, 2026-08-13엔 `hc:` 도형태그/`textpos=`/`linkListNextIDRef=`/
`outlineS` 발견). 이번엔 **네임스페이스 접두어(hp:/hc:)조차 없는 맨 속성명**
(`bList`, `hasNumRef=`, `hasTextRef=`, `linkListIDRef=`, `width=`, `bottom=`,
`merged=`, `array`, `para`) 형태를 실제 라이브 검색으로 확인 — 짧은 검색 몇 번만으로도
한국원자력의학원/한전KDN/성남도시개발공사/대구경북첨단의료산업진흥재단/한국수목원
정원관리원/신용보증기금/사립학교교직원연금공단/축산물품질평가원/한국토지주택공사/
울산시설공단/한국문화예술위원회 등 **최소 11개 기관 문서에서 발견** — 기존에 알려진
것보다 훨씬 넓게 퍼져있는 것으로 보임.

**정확한 원인(코드로 확인)**: `scripts/fix_text_corruption.py`의 `strip_hwpml_leak()`은
`HWP_LEAK_MARKER`(= `hp:run|hp:lineseg|hp:sz|hp:pos` 중 하나가 문서 어딘가에 있어야
함) 가드를 통과해야만 동작함(전체 72,511건에 영향 없는 문서까지 건드리는 사고를
막으려고 2026-08-07에 일부러 넣은 안전장치). **대구경북 문서 raw_text를 직접 검사해보니
`hp:`가 단 한 글자도 없었음** — 즉 이 가드가 아예 안 열려서, 안에 있는 `TAG_ATTR_RE`
(`\b[\w:]+="[^"]*"`, 이미 `width="..."` 같은 건 잡을 만큼 넓음)조차 시도가 안 되고
있었음. 게다가 `bList`/`array`/`para`/`hasN`처럼 `="값"`이 안 붙은 맨 토큰은
`TAG_ATTR_RE`로도 못 잡음(`TAG_NAME_RE`도 `hp:`로 시작하는 것만 잡음) — 가드를 없애도
이 세 번째 변종은 어차피 못 잡았을 것.

**코드 수정은 이번에도 안 함** — 이유: ①이 스크립트는 `documents.raw_text`/
`chunks.text`를 직접 UPDATE하는 스크립트라 DB 쓰기 권한 있는 세션에서 로컬 CSV로
먼저 재검증하는 이 프로젝트의 기존 절차(2026-08-07 원칙, "반영 전 로컬 검증"이
실제로 대형 사고를 막은 전례 있음)를 거쳐야 하는데 이 세션은 DB 접근이 없어서 검증
자체가 불가능함 ②"맨 토큰"(bList, array, para, hasN 등) 매칭 규칙은 아직 전체
변종 목록을 다 못 봐서(이번에 찾은 것도 우연히 검색으로 걸린 일부일 뿐) 섣불리
정규식을 짜면 진짜 한글 내용 속 영단어를 잘못 지울 위험이 있음(예: "TFT"/"KOVIS"
같은 진짜 약어와 구분 필요).

**다음 세션(DB 접근 있을 때)을 위한 방향 메모**:
1. `HWP_LEAK_MARKER` 가드를 `hp:run|hp:lineseg|hp:sz|hp:pos|hc:\w|textpos=|vertpos=|
   linkListNextIDRef=|outlineS`로 넓히면(2026-08-13 조사 결과 반영) 최소한 이미 넓은
   `TAG_ATTR_RE`가 더 많은 문서에서 시도라도 됨 — 1차 확장으로 시도해볼 만함.
2. "맨 토큰"(접두어/값 없는 `bList`/`array`/`para`/`hasN`류)은 전체 corpus에서
   `scripts/audit_hwp_tag_leak.py` 스타일로 먼저 **읽기 전용 전수조사** 해서 실제
   나오는 토큰 종류를 다 모은 다음, 화이트리스트(진짜 자주 쓰이는 한글 문서 속
   영단어: TFT, KOVIS 등)와 겹치지 않는지 확인 후 블록리스트 방식으로 접근하는 게
   안전해 보임(오탐 위험이 %"닫는 괄호+따옴표" 같은 구조적 신호가 없어서 지금까지 쓴
   방식보다 큼).
3. 이 세션에서 검색으로 우연히 찾은 것 외에 **정확한 영향 문서 수는 미확인** — 위
   1번처럼 가드만 넓혀도 되는 문서와, "맨 토큰"까지 있어서 못 잡는 문서를 구분해서
   집계 필요.

## ✅ 표 블록 줄바꿈 보존 (2026-08-18, 4차)

### 사소하지만 확실한 개선 — CSS와 JS 사이 드리프트 수정
`splitIntoBlocks`가 표/그림 조각(`table` 타입) 블록도 다른 문단(body/bullet)과 똑같이
`para.join(" ")`로 모든 줄을 공백 하나로 이어붙이고 있었음 — 그런데 CSS
(`.raw-line-table > div { white-space: pre-wrap }`)는 애초부터 줄바꿈이 살아있는 걸
전제로 만들어져 있었음(2026-08-12 4차, 표 블록 처음 도입 때부터). 표는 PDF 줄바꿈이
"문장이 길어서 끊긴 자리"가 아니라 "표의 서로 다른 셀/행"일 가능성이 높아서, 공백으로
합치면 뜻 없는 단어 나열이 되지만 줄바꿈을 살리면 최소한 원래 줄 단위 구분은 남길 수
있음 — `table` 타입만 `para.join("\n")`으로 바꿈.

**실제 데이터로 검증**: 한국토지주택공사 "시간외근무 부적절"(`c1b8ac66979ce69b`) 문서의
[표 1]로 확인 — 완전한 표 재구성은 아니지만(원본 PDF 컬럼 순서 자체가 이미 뒤섞여
추출된 상태라 여전히 표처럼 보이진 않음), 최소한 "보고서/점검/점검일 부서 관련자...",
"주택성능연구", "2021.01.07. T 18:20 - 18:50 근무지 무단이탈 기 승인 취소" 등 원래
줄 단위가 살아나서 이전(모든 게 한 줄로 뭉개짐)보다는 눈에 띄게 나아짐.

`vite build`/`oxlint` 통과.

## 🔍 "2-2/2-3 소실" 정확한 원인 확정 + 렌더링 버그 2건 추가 수정 (2026-08-18, 3차)

### 🎯 핵심 발견 — "2-2/2-3 내용 소실"의 정확한 메커니즘을 처음으로 특정함
사용자가 문제의 실제 문서 ID(`c1b8ac66979ce69b`, 한국토지주택공사 2020 "시간외근무
부적절")와 **원본 PDF 링크(jsdelivr CDN)를 직접 줘서** 처음으로 원본과 DB를 나란히
비교할 수 있었음. 원본 PDF(`한국토지주택공사_2020년 복무감사(3).pdf`)를 열어보니
**한 파일에 (2-1)/(2-2)/(2-3) 세 건의 서로 다른 사례가 들어있었고 제목이 셋 다
"시간외근무 부적절"로 동일**(관련자만 T·V / U / W로 다름).

- `/documents/{id}`(상세페이지, `documents.raw_text`) → **(2-1) T·V 사례만** 담겨있고
  "(2-2) [ 경고 ] 사항"(다음 사례 제목 한 줄)만 남긴 채 끊김 — 8/14 세션이 이미 확인한
  증상 그대로.
- **그런데 `/search`로 확인해보니, 같은 문서 ID로 (2-2) U 사례, (2-3) W 사례 내용도
  검색됨** — 즉 **`chunks`에는 세 사례 내용이 전부 들어있는데 `documents.raw_text`엔
  (2-1)만 남아있는 상태**. 8/14 세션의 "원인도 특정 못함" 결론에서 한 걸음 더 나가서,
  정확히 어느 스크립트의 어느 로직인지까지 찾음.
- **원인**: `scripts/build_final_dataset.py`의 `match_with_chunks()`가 문서를
  `(source_file, title)` 키로만 매칭함(스크립트 상단 주석에 이미 "title이 여러 개
  섞인 document_id는 제외"라는 안전장치가 있지만, 이 안전장치는 **여러 사례가 서로
  다른 제목으로 섞였을 때만** 잡아냄 — (2-1)/(2-2)/(2-3)처럼 **제목이 우연히 전부
  똑같은 경우**엔 안 걸림). `lookup = {(source_file, title): d for d in clean_docs...}`
  딕셔너리 컴프리헨션이 같은 키를 마지막 값으로 덮어써서, 세 사례 중 하나(raw_text
  기준으로는 2-1)만 최종 `documents_final.jsonl`에 남고 나머지 둘의 raw_text는 유실됨
  — 반면 애초에 청킹 단계(`embed_ready_v2.jsonl`, 이 저장소에 코드가 없음)에서 세
  사례가 이미 하나의 `doc_id`로 묶여 있었던 것으로 보여서 `chunks`엔 셋 다 남음.

### 왜 이번에도 코드 수정까지는 못 감
근본 원인의 일부(청킹 단계에서 애초에 세 사례가 한 `doc_id`로 묶인 것)는 여전히 이
저장소 밖(유실된 1회성 Colab 청킹 코드) 소관이라, `build_final_dataset.py`만 고쳐도
(예: 매칭 키에 case_number 추가) chunks 쪽 데이터가 이미 섞여있어서 완전한 해결은
안 됨 — 청킹을 다시 해야 함. 게다가 이 세션은 Drive/Colab 접근도 DB 쓰기 권한도 없어서
스크립트를 고쳐도 재실행/검증/반영을 할 수가 없음. **다음 세션(Colab/DB 접근 있는
세션)에서 이어갈 것 — 청킹 재현이 안 되면 최소한 `build_final_dataset.py`의 매칭
키에 case_number를 추가해서 "제목 같은 사례가 하나로 뭉개지는" 것 자체는 막을 수
있을지 검토.**

### 렌더링 버그 2건 추가 수정 (`DetailPage.jsx`, 다른 사용자 스크린샷 2장 기반)
1. **워터마크로 추정되는 노이즈 줄**: "INSIDabcdef_:MS_0001MS_0001 1000_SM1000_SM:_
   fedcbaDlSNI"처럼 한글 없이 영문+숫자+밑줄만 뒤섞인 줄이 본문 중간에 낀 것을 실제
   스크린샷으로 확인(원본 PDF엔 없음, "abcdef"/"fedcba"처럼 거울상 대칭 패턴이 있어서
   문서유출 추적용 숨김 워터마크 텍스트 레이어가 잘못 추출된 것으로 추정). 다른
   반복 상용구(SECURITY_NOTICE_RE)와 같은 급으로 캡션(작게) 처리 — `WATERMARK_NOISE_RE`.
2. **"C - .../D - ..." 인물별 목록 항목이 한 문단으로 뭉쳐 보이던 문제**: "* 폭행
   피해사실 : C - B가 휘드른 손에 머리를 2회 맞음" 다음 줄 "D - 왼쪽눈썹..."이 원본
   PDF에서는 줄바꿈으로 나뉜 별도 목록 항목인데, 어떤 기존 불릿 문자로도 안 시작해서
   그냥 이어 붙고 있었음("문단 나누는 게 아쉬워" 피드백). 이 말뭉치의 인물 마스킹
   표기(대문자 라틴 1~3글자)가 "- "로 시작하는 패턴을 새 불릿 신호로 인식하도록
   `PERSON_LIST_ITEM_RE` 추가.

### 검증
- 두 문제 다 스크린샷 텍스트를 최대한 그대로 재구성한 합성 텍스트로 Node 검증(이
  문서의 실제 doc id를 못 받아서 — 스크린샷에 URL이 없었음). `vite build`/`oxlint` 통과.

### ⏭️ 다음 세션은 여기부터
1. **`c1b8ac66979ce69b`류(제목 동일한 여러 사례가 한 doc_id로 뭉개짐) 영향 범위
   전수조사** — Railway DB 접근 있는 세션에서, 같은 (source_file, title) 조합이
   여러 case_number에 걸쳐 나오는 문서가 몇 건인지 SQL/스크립트로 집계 권장. 이번
   사례 하나로는 전체 규모를 알 수 없음.
2. `build_final_dataset.py` 매칭 키에 case_number 추가하는 것으로 "최소한 향후
   재실행 시 안 뭉개지게"는 가능한지 검토(단, 청킹 자체가 안 됐던 과거 데이터는
   재청킹 없이는 완전 해결 안 됨 — 위 "왜 이번에도" 항목 참고).
3. 위 렌더링 버그 2건, 실제 배포 화면으로 재확인.

## ✅ 원문 렌더링 개선 2차 — 실제 API로 재검증 + "◯N" 헤딩/장식 글리프 버그 추가 수정 (2026-08-18, 2차)

### Railway 백엔드가 살아있는 걸 발견 — 이번엔 진짜 raw_text로 검증
같은 날 1차 세션 때는 알려진 모든 Railway 도메인이 죽어있어서 합성 텍스트로만
검증했는데, 이번엔 사용자가 준 라이브 사이트 URL의 번들(JS)에서 새 도메인
(`auditweb-production-0b13.up.railway.app`, `/health` 정상)을 찾아내서 **두 문서의
실제 raw_text를 API로 직접 받아 그걸로 재검증**함 — 한국가스공사 2021(`7a766550f0f873b7`,
품위유지의무 위반 등), 한국철도공사 2020(`dec56dc84bfe3a6c`, 복무감사, 첫 대화의
원래 스크린샷 문서).

### 1차의 INLINE_LAW_CITATION_SPLIT_RE가 실제 데이터로 재현한 반례에 걸림 — 함수로 재작성
1차에서 "괄호 바로 뒤에 공백+따옴표"로 잘랐던 정규식이, 실제 raw_text의 "2) 공사
｢취업규칙｣ 제7조(성실의무) 제1항‘직원은..."(괄호 뒤에 "제1항"이 공백 없이 하나 더
붙고 나서야 따옴표가 옴 — 사용자가 "2) 공사 취업규칙도 마찬가지"라고 후속 제보한 바로
그 줄)에서 안 잘렸음. 정규식 한 방 대신 `splitLawCitationHeading()` 함수로 바꿔서
"줄에서 처음 나오는 인용부호 앞에 닫는 괄호가 하나라도 있었는지"로 조건을 완화 —
두 문서 실제 raw_text 전체(가스공사 34블록, 철도공사 46블록)를 Node로 돌려서 전부
의도대로 분리되는 것 확인(라벨/인용문 각주/필드 전부 정상).

### 새로 발견 — 한국철도공사 문서에서 실제로 확인한 버그 2건 (원래 스크린샷 문서!)
raw_text를 실제로 받아보니, 사용자가 처음 제보했던 "1 뒤에 이상한 기호"/"2 뒤에도
마찬가지, 새로 시작해야 하는데 본문처럼 보임"의 정체를 찾음 — **컬럼 순서 뒤섞임이
아니라 다른 두 가지 렌더링 버그였음**(이 문서의 raw_text 자체는 순서가 정상이었음,
◯1~◯18이 처음부터 끝까지 올바른 순서로 저장되어 있었음 — 8/14 세션이 봤던 다단
스크램블 예시와는 다른 문제였던 것으로 보임):
1. **"◯1 음주관리 및 근무에 관한 사항 (신분상・행정상조치)"처럼 "◯"(큰 동그라미,
   U+25EF)+숫자가 붙어서 항목 번호를 나타내는데, 원문자(①②③)도 숫자패턴(1.)도
   아니라서 지금까지 그냥 body로 흘러들어가 안 굵은 문단으로 보이고 있었음** —
   "새로 시작해야 하는데 그냥 본문처럼 보인다"는 피드백 그대로. 헤딩 패턴에
   `/^◯\d{1,2}\s+\S/` 추가해서 굵게 나오도록 수정. "○"(BULLET_RE의 흰 동그라미,
   마스킹 표시 "○○○"에도 쓰이는 그 글자)와는 다른 코드포인트라 충돌 없음 확인.
2. **"Ⅰ 감사실시 개요" 헤딩과 "1. 감사배경 및 목적" 헤딩 사이에 "◤"(검은 삼각형)
   한 글자만 있는 줄이 홀로 끼어서, 뜻 없는 한 글자짜리 body 문단으로 렌더링되고
   있었음** — PDF의 장식용 코너 표시가 텍스트로 잘못 추출된 것으로 보임. "1 뒤에
   이상한 기호"가 바로 이거였을 가능성이 높음(정확히는 1 앞이지만, 시각적으로
   바로 붙어 있어서 그렇게 보였을 것). 빈 줄과 똑같이 취급해서 화면에서 사라지게
   함(`ORNAMENT_GLYPH_LINE_RE`, 같은 계열 ◢◣◥도 장식용일 가능성 높아 함께 제외).

### 검증
- Node로 실제 raw_text 두 건 전체를 `splitIntoBlocks`에 통과시켜 블록 목록을 눈으로
  전수 확인(스크래치패드에 저장) — 의도한 대로 나옴, 회귀 없음.
- `npm run build`(vite)/`npm run lint`(oxlint) 재확인 통과.
- **여전히 실제 배포 페이지(브라우저)로는 확인 못 함** — 코드 검증은 API 원문
  기준으로 했지만 CSS 적용된 실제 화면은 다음 세션에서 확인 필요.

### 이번에 다시 확인한 것 — 컬럼 순서 뒤섞임은 이 두 문서에선 안 보였음
8/14 세션이 발견했던 "◯1 음주관리...◯5 조작판..." 한 줄에 붙어버리는 다단 스크램블이,
이번에 API로 받은 한국철도공사 문서의 raw_text에서는 재현되지 않음(◯1~◯18 전부
정상 순서, 한 줄에 안 붙어있음). DB가 그새 고쳐졌다는 근거는 없음(10차 결론대로
청킹 알고리즘 문제로 반영 자체가 막혀있었음) — 그보다는 원래 스크린샷이 보여준
문제가 사실 위 1·2번(◯N 헤딩 미인식 + ◤ 장식 글리프)이었고, "컬럼 순서" 진단은
8/14 세션이 이 문서의 다른 페이지/버전을 보고 내렸을 가능성이 있음. 결론: **이
문서에 한해서는 DB 재추출 없이 이번 프론트 수정만으로 원래 스크린샷 문제가 상당
부분 해소됐을 가능성이 있음** — 실제 화면으로 재확인 필요(위 검증 항목 참고).

### ⏭️ 다음 세션은 여기부터
1. Vercel 재배포 확인 후 `dec56dc84bfe3a6c`, `7a766550f0f873b7` 둘 다 실제 화면으로
   열어서 이번 2차 수정까지 포함해 스크린샷과 비교 재확인.
2. 위에서 "컬럼 순서 뒤섞임은 이 문서에서 재현 안 됨"이라고 결론 내렸지만, 8/14
   세션 기록과 배치되므로 100% 확신은 아님 — 재확인 후에도 실제로 순서가 이상해
   보이면 그때 다시 8/14 방향(재추출)으로 돌아갈 것.
3. "참고" 캡션(작은글씨+굵게+번호), 2-2/2-3 소실(다른 문서, `c1b8ac66979ce69b`),
   표 셀 복원은 여전히 미해결 — 아래 1차 항목 참고.

## ✅ 원문 렌더링 개선(필드 라벨 굵게 분리 + 번호목록 헤딩 과굵게/각주오분류 버그) (2026-08-18)

### 배경 — 이번엔 실제 라이브 사이트 스크린샷으로 검증하며 진행
이 세션은 Railway 백엔드가 다운 상태(모든 알려진 도메인 404 — 8/14 마지막 세션에서
"비용 관리를 위해 서비스 삭제, 코드는 그대로라 재배포는 언제든 가능"이라고 한 것과 일치)라
API로 raw_text를 직접 받아올 순 없었음. 대신 사용자가 실제 배포된 프론트(`audit-web-phi.
vercel.app`, 살아있음)의 원문 ↔ 원본 PDF 스크린샷을 두 문서(한국철도공사 2020 복무감사,
한국가스공사 2021 품위유지의무위반)로 여러 장 보내줘서, 그 실제 텍스트를 근거로 회귀
테스트(Node로 `splitIntoBlocks`만 떼어내 합성 텍스트 실행)까지 거쳐 수정함 — API 없이도
"실제 문서로 확인 후에만 정규식 수정"이라는 이 프로젝트의 기존 원칙을 지킴.

### 고친 것 (`frontend/src/pages/DetailPage.jsx`, `index.css`)
1. **번호목록 헤딩에 붙은 인용문까지 통째로 굵게 나오던 버그**: "1) 법령 「공직자
   윤리법」 제3조의 2. 제2항(공기업) '공직유관단체인 우리 공사는..." — 원본 PDF는
   "제2항(공기업)"까지만 굵고 그 뒤 인용문(따옴표 시작)은 보통체의 새 문단인데, 텍스트
   추출 시 둘 사이 줄바꿈이 사라져서 번호목록 헤딩 패턴이 줄 전체를 삼켜버리고 있었음.
   "괄호로 끝나는 라벨 + 공백 + 인용부호"에서 자르는 `INLINE_LAW_CITATION_SPLIT_RE`
   추가 — 라벨만 heading 블록, 인용문은 새 body 문단으로.
2. **연속 번호목록 항목이 각주로 오분류되던 버그**: 위 1번처럼 "1) 법령...(설명 문단)
   2) 법령..." 구조에서, 두 번째 항목("2)")은 항상 heading/bullet 바로 다음이 아니라
   body 문단 바로 다음에 옴 — 8/14에 넣은 각주 판별 조건("직전이 body면 각주 후보")에
   걸려서 실제 목록 헤딩인데 작게 각주 스타일로 나오던 문제(사용자가 "2) 법령은 왜
   작은 글씨인지" 로 제보). 직전 번호목록 헤딩 번호를 추적(`lastHeadingListNum`)해서
   그 값+1과 정확히 같으면 각주 판정에서 제외하도록 수정 — 실제 각주(참조번호가 딴 데서
   인용된 것)는 이 조건에 안 걸리는 것도 합성 텍스트로 회귀 확인함.
3. **상단 필드 라벨(소관부서/조치부서/관계기관/조치기한/감사명/관련자/일련번호)이
   값까지 통째로 굵게 나오던 문제**: 사용자가 "소관부서/조치부서 라벨뿐 아니라 값까지
   다 굵어서 여러 필드를 나란히 비교하기 어렵다"고 제보(원본 PDF는 라벨만 굵음). 이
   라벨들을 `HEADING_LABEL_PATTERNS`(줄 전체 굵게)에서 빼서 캡처그룹 있는
   `FIELD_LABEL_PATTERNS`로 옮기고, 새 "field" 블록 타입으로 라벨만 `<b>`, 값은 보통
   굵기로 렌더링(`raw-line-field` CSS). 부수 효과로 이 필드들이 목차(TOC)에서도 빠짐
   (buildToc는 "heading" 타입만 모으므로) — 안 그래도 항상 문서 맨 위에 있어서 목차
   항목으로서 의미가 적었던 것들이라 오히려 목차가 깔끔해짐.

### 검증
- Node로 `splitIntoBlocks`/`classifyLine`/`matchFieldLabel`만 떼어내 위 두 실제 문서
  스크린샷 텍스트 + 회귀용 합성 텍스트(일반 목록 헤딩, 짧은 가/나/다 라벨, 진짜 각주)로
  실행 — 의도한 대로 분리되고 기존 케이스는 안 깨짐 확인.
- `npm run build`(vite) 정상 빌드, `npm run lint`(oxlint) 경고 없음 확인.
- **실제 배포 페이지로는 확인 못 함** — Vercel 재배포 후 스크린샷 속 두 문서(특히
  `7a766550f0f873b7`)를 실제로 열어서 눈으로 재확인 권장.

### 사용자가 같은 대화에서 제보한 12개 항목 중 이번에 손 못 댄 것 (다음 세션 참고)
- **컬럼 순서 뒤섞임/문장 중간 끼어듦(한국철도공사 2020, `dec56dc84bfe3a6c`)**: 이미
  8/14 6~10차에서 원인 규명 + 프로토타입(`scripts/reextract_pdf_text.py`)까지 끝났지만
  DB 반영은 "청킹 알고리즘이 저장소에 없음"으로 막혀있음(10차 항목 그대로 유효, 이번
  세션은 DB 접근 자체가 없어서 진전 없음).
  - 참고로 raw_text만 놓고 보면 **재청킹 없이 `documents.raw_text`만 갱신해도 상세페이지
    표시는 즉시 고쳐짐** — `chunks`(검색/임베딩 전용)는 옛 텍스트 기준으로 남아 검색
    정확도만 약간 stale해질 뿐, 상세페이지 표시가 깨지는 것도 검색 자체가 안 되는 것도
    아님. 10차가 "재청킹까지 필요하다"고 본 건 데이터 정합성 관점에서는 맞지만, "일단
    표시만이라도 정확하게" 목표라면 훨씬 작은 스코프로 갈 수 있음 — 다음 세션에서 이
    분리된 접근을 사용자와 상의해볼 만함(단, DB 쓰기 권한이 있는 세션에서만 가능).
- **(N-M) [처분유형] 사항 뒤 내용 소실("2-2/2-3 안 보임")**: 8/14 세션에서 이미
  `c1b8ac66979ce69b`(한국토지주택공사) 사례로 원인 확인 — raw_text 자체가 다음 사례
  제목 한 줄만 남기고 끊겨 있음, 이 저장소보다 앞선 원본 파싱 단계(저장소 밖) 문제라
  이 저장소 코드로는 수정 불가. 한국철도공사 문서에서도 같은 패턴인지는 미확인.
- **표 셀 복원**: 여전히 셀 구분이 추출 단계에서 소실된 상태라 접어서(펼쳐보기) 보여주는
  것 이상은 이 저장소 코드로 불가(8/14 세션과 같은 결론).
- **"참고" 캡션을 원본처럼 작은 글씨+굵게+번호로**: 실제 예시 텍스트를 못 받아서
  손 안 댐 — 지금은 다른 대괄호 캡션(표/그림/별표 등)과 똑같이 처리됨. 다음에 사용자가
  "[참고]" 관련 스크린샷/원문을 준다면 위와 같은 방식(Node 합성 텍스트 회귀 검증)으로
  진행하면 됨.
- **q/m → □/○ 글리프 치환**: 8/14에 이미 고쳐서 코드엔 있음(`GLYPH_BULLET_RE`) — 이번에
  사용자가 다시 "네모가 q로" 언급 안 한 걸 보면 배포로 이미 해결됐을 가능성 있음
  (8/14 같은 날 배포 설정 버그로 한 번 배포가 안 먹었던 전례가 있어서 재확인 권장).

### ⏭️ 다음 세션은 여기부터
1. **이번 커밋 배포 확인** — Vercel 자동배포(main 병합 트리거인지 확인) 여부 확인 후,
   한국가스공사 2021(`7a766550f0f873b7`)과 한국철도공사 2020(`dec56dc84bfe3a6c`)을
   실제로 열어서 이번 수정 4가지가 스크린샷대로 반영됐는지 확인.
2. 위 "손 못 댄 것" 중 "raw_text만 갱신"(재청킹 생략) 옵션을 사용자와 상의 — 진행하기로
   하면 DB 쓰기 권한 있는 세션에서 `dec56dc84bfe3a6c` 등 소수 문서로 먼저 시범 적용 후
   범위 넓히는 걸 권장(2,111건 전체 일괄 적용 전).
3. "참고" 캡션 관련 실제 원문 확보되면 작은글씨+번호 스타일 추가.

## 🚧 DB 반영 스크립트 착수 전 확인 — 청킹 알고리즘이 이 저장소에 없음 (2026-08-14, 10차)

`scripts/reextract_pdf_text.py`를 실제로 DB에 반영하는 스크립트를 쓰려고
기존 반영 스크립트들(`fix_text_corruption.py`, `reembed_changed_chunks.py`,
`embed_chunks.py`, `build_final_dataset.py`)의 패턴을 확인함.

### 왜 단순하지 않은가 — raw_text 재추출 = 재청킹 필요
`fix_text_corruption.py`는 **기존 `chunks.text`를 정규식으로 그 자리에서
패치**하는 방식이라(청크 경계는 그대로 두고 내용만 살짝 고침) 재임베딩만
하면 끝났음. 하지만 `reextract_pdf_text.py`는 문서 전체를 처음부터 다시
파싱하는 거라 **문단/줄 경계가 완전히 달라짐** — 기존 `chunks` 행들은 옛
`raw_text` 기준으로 잘려 있어서 그대로 둘 수 없고, 새 raw_text로 처음부터
다시 청킹해야 함.

### 문제: 원래 청킹 알고리즘이 이 저장소 어디에도 없음
`embed_chunks.py`는 이미 청킹된 `embed_ready_v2.jsonl`(chunk_id+text)을
읽어 임베딩만 함. `build_final_dataset.py`도 마찬가지로 이미 만들어진
`embed_ready_v2.jsonl`을 documents와 매칭만 함. **실제로 raw_text를 청크로
자르는 코드 자체가 이 레포에 없음** — 원래 코퍼스 빌드 때 레포 밖(1회성
Colab 노트북)에서 만들어졌고 보존되지 않은 것으로 보임(이 세션에서 이미
여러 번 확인된 패턴: "[부서]" 마스킹 정규화도 마찬가지로 못 찾음).

### 그래서 지금 반영 스크립트를 쓰지 않기로 함
청킹 규칙(청크 크기, 겹침, 분할 기준)을 모른 채로 재추출 문서만 새로 짜면
독자적인 청킹 방식이 돼서 코퍼스 나머지 부분과 일관성이 깨질 위험이 있음
(검색 품질에 미묘한 영향 가능) — 근거 없이 새로 짜지 않기로 함(이번 세션에
반복된 "확실하지 않으면 구현 보류, 근거만 기록" 원칙과 동일).

### 다음 세션은 여기부터
1. 기존 `chunks` 테이블에서 (재추출 대상이 아닌) 문서 하나를 골라
   `raw_text` vs `chunks.text` 여러 건을 비교해서 청킹 규칙을 역산 시도
   (청크 길이 분포, 겹침 존재 여부, 문단/문장 경계 기준인지 등) — 안 되면
   사용자에게 원래 청킹 스크립트가 어디 있는지(다른 Colab 노트북?) 직접
   문의.
2. 청킹 규칙 확보되면 그때 `apply_reextraction.py`류 작성 —
   `remove_unrecoverable_docs.py`와 같은 2단계 안전 패턴(1단계: 읽기 전용
   manifest 저장, 2단계: 사람이 확인 후 수동으로 주석 해제해서 반영) 따를 것.
3. 8~9차에서 남긴 "선행 상용구"/표 구조 차이 문제도 같이 해결 필요.

## 🔍 "선행 상용구" 문제 — 그럴듯한 원인 가설 찾음, 구현은 보류 (2026-08-14, 9차)

8차에서 남긴 "제목 앞 상용구 미제거" 문제를 API 없이 로컬 자료로 더 파봄.

### 가설: 줄 단위 삭제가 아니라 "제목" 키워드 위치에서의 문자열 슬라이스
9개 검증 문서 전부 `raw_text`가 **"제 목"으로 시작**함(브래킷/콜론 표기는
문서마다 다름). 재추출 결과와 비교해보면 단순히 "제목 줄 앞의 줄들을
삭제"가 아니라 **"제" 글자가 처음 나타나는 위치에서 문자열을 그대로 자르는
것**처럼 보임 — 예를 들어 "1. 제 목 : ..." → "제 목 : ..." (줄바꿈 포함 앞부분
전부 삭제), "[제 목] ..." → "제 목] ..." (여는 대괄호까지 잘려나감, 이게
바로 이전에 TITLE_RE를 느슨하게 고치게 만들었던 "여는 괄호 소실" 버그의
정체였을 가능성이 높음 — PDF 추출 손상이 아니라 이 슬라이스 로직 때문이었던
것으로 보임).

### 왜 아직 구현 안 함 — 전체 감사보고서(다중 지적사항)에서 위험 확인
한국공항공사 문서(`airport2023.pdf`)로 이 규칙을 테스트해보니 "제목" 키워드가
문서 15번째 줄에서 처음 매치되는데, 그 앞에 **처분요구 사항 일람표(목차),
표지, 기관명, "감사실" 섹션 헤더**가 있음 — 규칙을 그대로 적용하면 이 전체가
삭제됨. 이 문서는 여러 지적사항을 담은 전체 감사보고서 스타일(한국철도공사
`railway2020.pdf`와 같은 유형)이라, 검증에 쓴 9개 문서(개별 처분요구서 1건당
1문서 스타일)와 근본적으로 다른 템플릿일 가능성이 높음 — 목차/표지가 정말
DB 원문에서도 삭제되는지 이 문서 자체의 raw_text(API 없이는 못 받음)로
확인 전까지는 이 규칙을 재추출 로직에 넣지 않기로 함(잘못 넣으면 문서
전체 앞부분을 통째로 날리는 큰 회귀가 될 수 있음).

### 다음 세션은 여기부터 (API 복구 후)
1. `airport2023.pdf`, `railway2020.pdf`처럼 다중 지적사항 스타일 문서의
   실제 raw_text를 API로 받아서, "제목"이 문서 앞부분(표지/목차 포함)에서
   삭제되는지 아니면 그대로 살아있는지 확인.
2. 확인되면 문서 스타일(개별 처분요구서 vs 전체보고서)을 구분하는 조건을
   찾아 `strip_leading_boilerplate()`류 함수를 조건부로만 적용하도록 구현.

## ✅ PDF 재추출 프로토타입 — DB 원문과 9개 문서 글자 단위 비교 + 마스킹 정규화 추가 (2026-08-14, 8차)

### API 없이 검증한 방법
Railway 백엔드가 계속 다운(`auditweb-production-1bc4` `/health` 404)이라 새
문서를 API로 못 받아왔음. 대신 이전 세션이 `DetailPage.jsx` 검증용으로 이미
받아둔 문서 JSON(`raw_text` 포함) 19개와, 스크래치패드에 있던 PDF 24개를
문서 ID로 대조해보니 **9개가 우연히 겹침** — 이 9건으로 API 없이도 DB 원문과
직접 글자 단위 비교 검증을 할 수 있었음.

### 결과: 평균 유사도 93.5%, 새 버그 발견/수정
공백 정규화 후 difflib 유사도 평균 **93.5%**, 9건 중 7건 96%+ (2건은 99.9%대로
사실상 일치). 비교 중 DB 원문과의 체계적 차이 2가지 발견:
1. **마스킹 기호 정규화 누락(이번에 고침)**: "○○○", "◎◎◎◎", "□□" 같은 기호
   반복(기관별로 다른 익명화 표기)이 DB 원문에선 이미 통일된 "[부서]"로
   저장돼 있는데, 재추출 로직은 원본 그대로 뽑아내고 있었음. 어디서 이
   정규화를 하는지 저장소 스크립트에서 못 찾음(1회성 스크립트로 유실된
   듯) — `normalize_masked_glyphs()`로 재현. 페이지 번호 푸터("- 1 -")도
   같이 제거하는 `strip_page_number_footers()` 추가.
2. **선행 상용구 미제거(아직 미해결)**: "권 고 사 항 1.", "지적연번 2017-",
   "한 국 소 비 자 원 주 의 요 구" 같이 "제 목" 줄 앞에 붙는 문서분류/기관명
   헤더가 DB 원문엔 없는데 재추출 결과엔 포함됨. 표가 있는 문서는 셀 구조
   차이로 유사도가 더 낮음(두 건 76~79%). 이번 라운드 핵심 타겟(2단 순서+
   띄어쓰기)과는 무관한 별개 이슈라 이번엔 손대지 않음 — 다음 세션에서
   확인 필요.

### 다음 세션은 여기부터
1. Railway 백엔드 복구되는 대로 2,111건 중 더 넓은 무작위 샘플로 같은 방식
   (원문과 글자 단위 비교) 재검증.
2. 선행 상용구 미제거 문제와 표 구조 차이 문제 원인 조사 — 특히 DB 원문을
   만든 원래 파이프라인이 이 저장소 어디에도 없다는 게 확인됐으므로, 이
   재추출 로직이 그 파이프라인을 완전히 대체할 수 있는지, 아니면 기존
   파이프라인에 컬럼/띄어쓰기 로직만 패치해 넣는 게 나을지 판단 필요.
3. 검증 통과하면 DB 반영 스크립트 작성 — 백업 → 반영 → 재임베딩 순서.

## ✅ PDF 재추출 프로토타입 — 24개 문서 확대 검증 + 같은-줄 오분할 버그 발견/수정 (2026-08-14, 7차)

### 확대 검증 결과
`scripts/reextract_pdf_text.py`를 스크래치패드에 이미 받아둔 24개 PDF(다단 후보
8건 + 이전 세션에서 받은 임의 16건)에 돌려 기존 PyMuPDF 기본 추출과 비교함.
- **크래시 0건.**
- "15자 이상 붙어버린 한글 어절" 개수를 세어 띄어쓰기 손실을 정량 비교한 결과,
  기본 추출은 **24건 중 8건(33%)** 에서 최대 63개까지 발생했는데, 새 로직은
  **24건 전부 0개**. 띄어쓰기 손실이 애초 2,111건(다단 의심)보다 훨씬 넓게
  퍼져 있고, 이 재추출 로직이 다단 여부와 무관하게 일반적으로 더 안전하다는
  뜻으로 보임(→ 적용 범위를 다단 2,111건 한정이 아니라 더 넓게 볼지도 고려
  필요).

### 이번에 새로 찾아 고친 버그
검증 중 건강보험심사평가원 문서(`ef6b966955b95184`)에서 "【제 목】"이 "【제" /
"목】"로 두 줄에 걸쳐 쪼개지는 걸 발견 — PyMuPDF가 같은 시각적 줄도 폰트/서브셋
전환 경계(글자 사이 간격이 아주 넓은 라벨성 텍스트 등)에서 별도 line 객체로
잘못 쪼개는 게 원인이었음(이 문서는 원래 TITLE_RE 정규식을 느슨하게 고치게
만든 그 문서 — 이번엔 추출 단계에서 근본적으로 고침). `_merge_same_row_fragments()`
를 추가해 y0가 거의 같은 fragment를 합치도록 했는데, 처음 버전은 **2단 페이지의
좌/우 컬럼 첫 줄이 같은 y0라서 서로 다른 컬럼 내용이 한 줄로 잘못 합쳐지는
회귀**를 스스로 발견함(한국철도공사 "◯1 음주관리...◯5 조작판..." 좌우 컬럼
헤더가 한 줄로 섞여버림) → x 간격 조건(가로로 인접, <=50pt — 2단 컬럼 판정
최소 간격인 페이지폭 15%보다 훨씬 작음)을 추가해 해결. 8개 2단 후보 문서
전체에서 병합줄이 페이지 폭의 70% 넘는 의심 사례 없음, 한국철도공사 5개 항목
순서 정확함 재확인 완료.

### 여전히 프로토타입 — DB 반영 전 남은 일은 6차와 동일 (원문 글자 단위 비교는
아직 1건뿐, 백업/재임베딩 계획 필요)

## ✅ PDF 재추출 프로토타입 — 다단 순서 + 띄어쓰기 동시 복구 성공 (2026-08-14, 6차)

### 돌파구 — 띄어쓰기 소실 원인 규명 + 해결
8/14 3차에서 발견한 트레이드오프("PyMuPDF가 컬럼 순서는 잘 잡지만 문서에 따라
띄어쓰기가 깨짐")의 원인을 문자 좌표를 직접 까봐서 찾음: PyMuPDF의 기본 단어
인식 임계값이 이 말뭉치의 정당화(justify)된 한글 본문 간격보다 느슨해서, 진짜
띄어쓰기 자리(문자 간 간격 ~4~6pt)를 못 잡고 여러 단어를 하나로 붙여버리고
있었음. 실측해보니 문자 간격이 **단어 내부(~0pt, 종종 겹침) vs 단어 경계
(~4~6pt)로 뚜렷하게 두 무리로 나뉘어 있어서**(중간값 2~3pt 거의 없음) 간격을
직접 재는 것만으로 복구 가능함을 확인 → `scripts/reextract_pdf_text.py` 작성.

### 검증 완료
한국철도공사 2020(`dec56dc84bfe3a6c`) 문서의 "1. 감사배경 및 목적" 문단을 이
로직으로 재추출한 결과가 **현재 DB 원문과 글자 하나 안 틀리고 일치**함(직접
비교). 다단 순서 문제였던 "◯1 음주관리...◯5 조작판..." 구간도 1→2→3→4→5
순서로 완전히 정상화됨. 원래 띄어쓰기 문제 없던 한국공항공사 문서, 정상
단일컬럼 문서(한국관광공사 등)로도 회귀 테스트 — 둘 다 문제없음 확인(단,
완전정당화된 줄에서 공백이 두 번 들어가는 사소한 버그 하나 발견해서 같이 수정).

### 아직 프로토타입 — DB 반영 전 남은 일
- **이 세션은 DB 쓰기 권한이 없어서 함수만 작성, 실제 반영 코드는 없음.**
- 5개 문서로만 검증함 — 2,111건 전체 적용 전에 더 넓은 샘플로 재검증 필요
  (특히 `gap_threshold=2.5pt`가 다른 폰트/기관 문서에도 맞는지 확인 필요).
- 줄바꿈이 원래보다 세밀하게 쪼개지는 경우가 있음(내용 손실은 없음, 프론트
  문단 합치기 로직이 흡수하는지 실제 렌더링으로 확인 필요).

### ⏭️ 다음 세션은 여기부터
1. `scripts/reextract_pdf_text.py`를 2,111건 후보 중 더 많은 샘플(예: 20~30건)
   로 돌려서 사람이 직접 원문과 비교 검증 — 특히 `gap_threshold` 값이 다른
   기관/폰트에서도 유효한지 확인.
2. 검증 통과하면 DB 반영 스크립트 작성 — 반드시 백업(`scripts/
   backup_before_fix.py`류) 먼저, 그 다음 반영, 그 다음 재임베딩 사이클
   (HNSW 인덱스 임시 DROP → 작업 → VACUUM → 재생성 순서 지킬 것).
3. README.md Railway 백엔드 URL 갱신 — 계속 미착수, 또 바뀜
   (`auditweb-production-1bc4`), 커스텀 도메인 검토 권장(반복 문제).

## ✅ PDF 다단 레이아웃 전수조사 완료 — 30,120건 중 2,111건(약 7%) 확인 (2026-08-14, 5차)

### 전수조사 결과
사용자가 `scripts/audit_pdf_column_layout.py`를 Colab에서 실제 DB(PDF 소스 30,120건)
대상으로 실행 완료. **2,111건(약 7.0%)이 다단(2단) 레이아웃 의심으로 확인됨.**
체크포인트 파일(`column_layout_checkpoint.jsonl`, 사용자 Colab 로컬)에 문서별
`{id, institution, year, flagged(페이지 인덱스), npages}` 전체 결과 저장되어 있음.

### 검증 — 의심 패턴 2건 직접 확인, 둘 다 진짜(오탐 아님)로 판명
결과 목록을 훑어보다가 오탐 의심 패턴 2가지 발견해서 실제 원본 PDF 5건을 직접
열어서 확인함(Railway 백엔드 도메인이 또 바뀌어 있었음 —
`auditweb-production-1bc4.up.railway.app`로 확인, README 갱신 필요 목록에 계속 누적됨):
- **"`pages=[0]`만 단독" 패턴** (표지 페이지 오탐 의심) — 한국자산관리공사 2017
  (`23144a2a2cc93a55`), 한전KDN 2017(`d75ff7836b2615cd`) 확인 → **둘 다 진짜**.
  표지가 따로 없고 첫 페이지부터 바로 좌우 2단 서술문이 시작하는 문서였음.
- **"전체/대부분 페이지가 다 찍힌" 패턴** (표 많은 재무제표류 오탐 의심) —
  한국가스공사 2021(`8f694a3d5bb45942`, 10페이지 전부), 영화진흥위원회
  2023(`a33f561a8d67e1ff`, 12페이지 중 10페이지) 확인 → **둘 다 진짜**. 목차·본문·
  표 페이지 다 실제로 처음부터 끝까지 좌우 2단으로 조판된 문서였음(표가 우측
  칸에 섞여 나오는 경우도 정상적으로 같이 잡힘).
- 기존에 알고 있던 표 오탐 1건(한국소비자원 `3053fd66660671f3`, "[공직기강 점검
  2회 적발자 현황]" 표)은 이번 결과에도 그대로 있음 — 이건 이미 알려진 좁은
  범위의 예외로, 전체 결과를 왜곡할 만큼 흔하지는 않은 것으로 판단.
- **결론**: 2,111건은 대체로 신뢰할 수 있는 숫자. 대량 오탐 우려는 기우였음.

### 스크립트 버그 2건 수정(같은 세션, 실행 전에 발견)
- `detect_multicolumn_pages()`가 `doc.close()` 뒤에 `len(doc)` 호출해서 모든
  문서가 "document closed" 에러로만 기록되던 버그 — 로컬 재현 후 수정.
- 순차 처리라 30,120건 기준 몇 시간 걸릴 속도였던 것을 `ThreadPoolExecutor`
  병렬 처리(기본 16개 동시)로 개선, 진행 로그도 200건→20건 간격으로 단축.
- 둘 다 `main`에 배포 완료.

### ⏭️ 다음 세션은 여기부터
1. **재추출 파이프라인 설계** — 8/14 3차에 확인한 대로, PyMuPDF 기본 추출은
   컬럼 순서는 잘 잡지만 문서에 따라 띄어쓰기가 깨지는 경우가 있음(철도공사는
   깨짐, 공항공사는 안 깨짐). 2,111건 전체에 일괄 적용하기 전에 문서별(또는
   최소 기관별) 띄어쓰기 검증이 필요함 — 단어 간격(bbox gap) 기준으로 공백을
   직접 삽입하는 후처리 로직을 pdfplumber나 PyMuPDF `"dict"` 모드로 구현하는
   방향 검토.
2. **2,111건 전체를 다시 봤을 때의 우선순위** — 페이지 수가 많이 걸린 문서
   (예: 그랜드코리아레저 11개 페이지, 한국장학재단 15개 페이지 등)부터 먼저
   손보면 체감 개선폭이 클 것으로 보임.
3. DB 반영 + 재임베딩 사이클 필요(규모 크므로 별도로 시간 잡을 것, HNSW
   인덱스 임시 DROP → 작업 → VACUUM → 재생성 순서 지킬 것 — 8/14 이전 볼륨
   부족 사고 대응 항목 참고).
4. README.md Railway 백엔드 URL 갱신 — 계속 미착수, 이번에 또 바뀜
   (`auditweb-production-1bc4`). 도메인이 세션마다 계속 바뀌는 게 반복 문제라,
   여유 있으면 Railway 커스텀 도메인 설정을 검토할 만함.

## ✅ PDF 다단 레이아웃 조사 스크립트 작성 + 새 변종("2-up 인쇄") 발견 (2026-08-14, 4차)

### 새로 발견 — "2-up 인쇄" 변종 (한국공항공사 2023 특정감사(19), 125a97b031fd8fe9)
사용자가 붙여준 텍스트가 "너무 이상하다"고 제보 → 원본 PDF(공개 CDN에서 직접
다운로드)를 PyMuPDF로 열어보니, 8/14 3차에서 확인한 철도공사 사례(①~④ 왼쪽
칸/⑤~⑧ 오른쪽 칸 **번호 목록**)와는 다른 변종이었음: **PDF 페이지 한 장이 실제
문서 2쪽(홀수쪽/짝수쪽)을 나란히 붙인 "2-up" 인쇄 형태**(파일은 6장인데 페이지
번호는 "-1-, -3-, -5-...-9-"만 찍혀 있음 — 실제론 10쪽 분량). 목록 항목이 아니라
**문단 전체**가 좌우로 뒤섞여서 철도공사 사례보다 훨씬 심각하게 깨져 있었음.

이 문서는 **PyMuPDF 기본 `get_text()`만으로 완벽하게 재추출됨** — 컬럼 순서도
정확하고(왼쪽 절반 전체 → 오른쪽 절반 전체), 지난번 철도공사 문서와 달리
**띄어쓰기도 안 깨짐**(원본 PDF 인코딩 품질 차이로 추정 — PDF마다 다름, 재추출
파이프라인을 실제로 쓰려면 문서별 검증이 필요하다는 뜻).

### 완료 — 레이아웃 실태 조사 스크립트 (`scripts/audit_pdf_column_layout.py`)
raw_text 기반 패턴(숫자·문장잘림 등)으로 뒤섞임을 역추적하는 대신, **원본 PDF의
텍스트 블록 좌표를 PyMuPDF로 직접 분석**해서 2단/다단 여부를 판정하는 스크립트
작성 — 실제 PDF 3종(철도공사=참, 공항공사=참, 한국관광공사=거짓)으로 반복
검증하며 임계값 조정:
- 문장급 블록(너비 150pt+, 텍스트 15자+)만 걸러써서 표 셀 조각 오탐 방지 1차 시도
- 그래도 표(여러 컬럼이 폭넓게 퍼진 형태, 한국소비자원 "[공직기강 점검 2회
  적발자 현황]" 사례)에서 오탐 발견 → 좌우 클러스터가 세로로 100pt 이상 퍼져
  있어야(표 한 행이 아니라 진짜 문단 흐름) 인정하는 조건 추가로 개선(완전히
  제거되진 않음 — 스크립트 자체 주석에 한계로 명시해둠)
- 이 세션에서 확보한 실제 source_url 17건(전부 PDF)으로 시범 실행: **2건 진짜
  다단 레이아웃 확인**(철도공사, 공항공사), 표로 인한 오탐 1건, 그 외 정상
  판정 — 매우 작은 표본(비무작위, 검색 쿼리로 우연히 모은 문서들)이라 정확한
  비율은 아니지만 "드물지 않다"는 정도의 감은 확인됨
- **이 세션은 Railway DB 접근이 없어서 전체 72,913건을 돌리지 못함** — 스크립트는
  Colab에서 `DATABASE_URL` 붙여서 실행하도록 작성(체크포인트 저장/재개 포함,
  PDF 다운로드가 문서당 1번씩 필요해서 전체 실행 시 시간이 꽤 걸릴 수 있음)

### ⏭️ 다음 세션은 여기부터
1. `scripts/audit_pdf_column_layout.py`를 Colab에서 실제 DB로 실행해서 정확한
   영향 범위(몇 건인지) 파악.
2. 후보 목록이 나오면 표로 인한 오탐 섞여있을 수 있으니 몇 건 사람이 직접 확인.
3. 재추출 파이프라인 설계 — PyMuPDF 기본 추출이 문서마다 띄어쓰기 깨짐 여부가
   달라서, 문서별 검증(또는 띄어쓰기 자동 보정 로직) 필요. DB 반영 + 재임베딩
   사이클도 필요(규모 크면 별도로 시간 잡을 것).

## ✅ 제목 헤딩 대괄호 유실 수정 + PDF 2단 컬럼 재추출 조사 착수 (2026-08-14, 3차)

### 완료 — 제목 굵게 안 되는 버그 수정 (PR #8, 배포 완료)
사용자 제보("제목 볼드체로 안 되어있고 크기도") → 건강보험심사평가원 문서 다수로
확인: "제 목】: 직원의 의무 위반"처럼 원본 "【제 목】: ..."의 여는 대괄호가 추출
단계에서 유실된 문서가 있었음. `textutils.py`의 `extract_title()`은 8/13에 이미
고쳤지만 프론트 `TITLE_RE`(굵게+큰 제목 스타일 담당)는 안 고쳐져 있었음 — 여는/닫는
대괄호를 전부 optional로 바꿔서 수정, 21개 기존 검증 문서로 회귀 없음 확인 후 배포.

### 확인만 함 — 인명 마스킹이 부서명과 같은 placeholder("[부서]") 사용
사용자가 같은 스크린샷에서 지적: "□□부 ○○○이"(부서+사람 각각 다른 기호로 마스킹된
원본)가 raw_text에는 "[부서]부 [부서]이"로 **부서명과 사람 이름이 똑같이 "[부서]"로
치환**되어 있음 — 문장이 "[부서]이 초과근무를 했다"처럼 부자연스럽게 읽힘. 확인 결과
이 마스킹은 이 저장소(`audit_web`)의 코드가 하는 게 아니라 **원본 데이터 준비
단계(README 표현으로 "원본 수집, 이 저장소 밖")에서 이미 이렇게 치환되어 들어온
것** — `raw_text` 자체에 이미 "[부서]"로 박혀 있어서 이 저장소 코드로는 부서명/
사람이름을 구분해서 되돌릴 방법이 없음(원문에 남아있던 원래 마스킹 기호 □□/○○○
정보 자체가 소실됨). **손 못 댐, 원본 데이터 준비 파이프라인 쪽에서 다뤄야 함.**

### 착수 — PDF 2단 컬럼(2-column) 읽기순서 뒤섞임, 재추출로 해결 가능성 확인 중
사용자가 "2-2/2-3 다음 사례 내용 소실" 다음으로 제보한 별개 문제: 원문이 2단
레이아웃(예: 한국철도공사 2020 복무감사, ①~④ 왼쪽 칸/⑤~⑧ 오른쪽 칸)인데 현재
`raw_text`엔 줄 단위로 왼쪽·오른쪽이 번갈아 섞여 저장되어 있음(예: "◯1 음주관리...
◯5 조작판 취급..."이 한 줄에 붙음). 사용자가 "원본 PDF 재추출"(가장 정확한 방법)을
선택 → 조사 착수:
- 해당 문서 원본 PDF를 jsdelivr CDN(`source_url`, 공개)에서 직접 다운로드해서 확인
  가능함을 확인 (`haechyaning-commits/data` 저장소는 이 세션 GitHub 접근 범위 밖이지만,
  공개 CDN 링크라 `curl`로 바로 받아짐).
- **PyMuPDF(`pymupdf`, Python)로 재추출한 결과, 이 문서에 한해 별도 로직 없이
  `page.get_text()` 기본 동작만으로 왼쪽 칸 전체 → 오른쪽 칸 전체 순서로 정확히
  나옴** — PyMuPDF가 내부적으로 텍스트 블록을 좌표 기반으로 묶어서 읽기순서를
  잡아주는 덕분으로 보임(`get_text("blocks")`로 블록별 bbox 확인, 왼쪽 블록 x0≈40~60,
  오른쪽 블록 x0≈465~480로 명확히 분리되고 블록 순서 자체가 이미 왼쪽 전체→오른쪽
  전체로 나옴).
- **그러나 트레이드오프 발견**: PyMuPDF 기본 추출은 **한글 단어 사이 공백이 없음**
  (예: "임직원의복무의무위반, 특정비위사실" — 현재 DB에 저장된 raw_text는
  "임직원의 복무의무위반, 특정비위사실"처럼 공백이 정상적으로 들어있음). 즉 현재
  파이프라인이 쓰는 원래 추출 도구(이 저장소 밖, 무엇인지 미확인)는 띄어쓰기는
  잘 처리하지만 컬럼 순서를 못 잡고, PyMuPDF는 컬럼 순서는 잘 잡지만 띄어쓰기가
  깨짐 — 단순 도구 교체로는 한쪽을 고치면 다른 쪽이 깨짐. **다음 단계**: PyMuPDF의
  `get_text("dict")`/`"rawdict")`나 `pdfplumber`(설치 완료, `cffi` 누락 문제도 해결함)
  로 단어(span) 단위 bbox를 직접 얻어서, 페이지를 좌/우 컬럼으로 나눈 뒤 각 컬럼
  안에서 단어 간 x-간격 기준으로 공백을 직접 삽입하는 커스텀 후처리가 필요해 보임
  — 컬럼 순서(블록 재배열)와 띄어쓰기(gap 기반 삽입) 둘 다 직접 구현하는 방향.
- **아직 안 한 것**: 이 방식이 실제로 몇 건에 적용 가능한지(2단 레이아웃 문서
  전수조사), 재추출 파이프라인 실제 구현, DB 반영(이 세션은 Railway DB 쓰기 권한
  없음 — 읽기 전용 공개 API만 사용 중), 재임베딩 사이클. 규모가 크고 원본 PDF
  전체(21.8GB 저장소)에 접근해야 정확한 영향 범위를 알 수 있어서, 다음 세션에서
  이어가려면 최소한 "2단 레이아웃 의심 문서 목록"부터 좁혀야 함.

### ⏭️ 다음 세션은 여기부터
1. 2단 컬럼 재추출 파이프라인 프로토타입 이어서 만들기(위 항목 참고) — pdfplumber로
   단어 bbox 기반 컬럼 분리 + 공백 삽입 로직 구현, 여러 문서로 검증.
2. 영향받는 문서 수 파악 — 전수조사는 Railway DB 직접 접근이 있어야 정확함(이 세션은
   공개 API로 문서 하나씩만 확인 가능했음).
3. 인명/부서명 마스킹 정보 소실 문제는 원본 데이터 준비 파이프라인(저장소 밖) 담당자
   확인 필요 — audit_web 저장소 범위 밖으로 결론.

## ✅ 원문 렌더링 개선(각주/불릿 글리프/보안문구/라벨) + 배포 설정 버그 발견 (2026-08-14)

### 배포 설정 버그 발견(코드는 안 고침 — Vercel 대시보드 권한 없음)
사용자가 "재배포했다"며 스크린샷과 실제 비교를 요청 → 배포된 프론트 번들을 직접
받아서 확인해보니 `VITE_API_BASE_URL`(Vercel 환경변수)이 백엔드(Railway)가 아니라
**프론트 자기 자신(`audit-web-phi.vercel.app`)으로 빌드되어 있었음** — `/search`,
`/documents/{id}`, `/health` 전부 실제로 호출해보면 JSON이 아니라 그냥 SPA
`index.html`(200)이 돌아옴, 즉 지금 배포본은 검색/상세 둘 다 완전히 안 됨. Railway
도메인도 또 바뀌어 있었음(사용자가 알려준 현재 값: `auditweb-production-2911.
up.railway.app`, `/health` 정상 확인). **다음에 할 일**: Vercel 프로젝트 설정에서
`VITE_API_BASE_URL`을 이 Railway 주소로 바꾸고 재배포(이 저장소 코드로는 못 고침).
README의 Railway URL도 이 값으로 갱신 필요(전에도 몇 번 바뀜, 계속 반복되는 문제라
가능하면 커스텀 도메인이나 Railway 프로젝트 설정에서 도메인 고정을 검토해볼 만함).

### 원문 렌더링 개선 (`frontend/src/pages/DetailPage.jsx`, 데이터는 안 건드림)
사용자가 원본 PDF ↔ 웹페이지 스크린샷을 여러 장 비교해서 12개 문제를 한 번에 제보.
이번 세션은 위 배포 버그 때문에 브라우저로 직접 눈으로 확인은 못 했지만, Railway
백엔드 주소를 알아낸 뒤로는 **`/documents/{id}`로 스크린샷 속 문서들의 실제
raw_text를 직접 가져와서(국제회의 보조금 건 `a85748231dd23bc7`, 한전KPS 태양광
경상정비 건 `09db923059dc1c8b` 등) 진짜 데이터로 규칙을 만들고 검증**함(splitIntoBlocks
로직만 떼어내 Node로 직접 돌려봄, 18개 문서로 회귀 확인).

- **각주가 소제목처럼 굵게 나오던 문제 수정**: "87,818,181원1)"처럼 붙어 나온 각주
  참조 번호를 문서 전체에서 먼저 스캔해두고, 그 번호로 시작하는 줄이 "직전이 아직
  안 끝난 본문 문단"일 때만 각주로 분리(작게 표시, 목차 제외). 진짜 목록 헤딩("1)
  태양광...")은 항상 상위 헤딩 바로 다음에 오므로 이 조건에 안 걸림 — 실제 문서로
  확인함. 단, 각주가 불릿(□/○) 문단 바로 뒤에 오는 경우는 일부러 못 잡게 남겨둠(그
  경우까지 허용하면 진짜 소제목과 번호가 겹칠 때 오히려 헤딩을 잘못 죽이는 사고가
  실제로 재현돼서 — "2) 단기노무원 출퇴근 허위입력 관련" 같은 진짜 헤딩이 죽는 걸
  실제 데이터로 확인하고 되돌림). 원문 자체의 순서(각주가 문장 중간에 끼어드는 것)는
  텍스트 추출 단계 문제라 여전히 못 고침 — 스타일만 바로잡음.
- **"q"/"m" 불릿 인식**: □/○가 PDF 폰트 글리프 매핑 문제로 "q"/"m"으로 추출된 줄을
  불릿으로 인식하고, 화면엔 원래 기호로 치환해서 보여줌.
- **반복되는 문서보안 상용구 축소**: "본 문서의 감사요지 및 귀책내용이 누설되어...
  요구합니다."가 페이지마다 반복 삽입되는 걸(문서 하나에 10번 넘게 확인) 캡션 스타일
  (작게)로 낮춤 — 지우지는 않음.
- **"[관련자 명세]" 표도 접어서 보기**: 기존엔 [표]/[그림]류만 접혔는데, 실제
  문서로 확인해보니 "[관련자 명세]" 표도 셀 구분이 소실된 채 통짜 텍스트로 노출되고
  있었음 — 같은 방식으로 접음.
- **상단 필드 라벨 인식 확대**: 관계부서/소관부서/조치부서/감사명/관련자(콜론
  있을 때만)/일련번호/내용(단독줄) 추가. 콜론 없이 공백만으로 라벨을 구분하는
  문서("소관부서 [부서]사업소")도 커버하되, 라벨 뒤에 조사가 바로 붙는 본문
  문장("관련자 T은 2019...")과 안 겹치게 구분자(콜론 또는 공백) 필수화.

### 이번엔 손 안 댄 것 (실데이터로 확인은 했지만 범위 밖으로 판단)
- **표 셀 복원**: `[관련자 명세]` 등 표를 접어서 숨기는 것까지만 함 — 표 안 내용(셀
  구분)은 추출 단계에서 이미 소실된 상태라 이 저장소 프론트 코드로는 복원 불가.
- **문서 분리 경계 버그로 보이는 사례 1건 확인**: 사용자가 "2-2/2-3 항목이 웹페이지엔
  안 보인다"고 제보한 문서(`c1b8ac66979ce69b`, 한국토지주택공사 "시간외근무 부적절")를
  직접 조회해보니, raw_text가 실제로 **"(2-2) [ 경고 ] 사항"이라는 다음 사례의 제목
  한 줄만 남긴 채 865자에서 뚝 끊겨 있었음**(내용 없음, 프론트 렌더링 문제가 아니라
  raw_text 자체가 이 상태). 같은 "(N-M) [처분유형] 사항" 꼬리표가 여러 기관 문서에서
  반복 확인돼서 우연이 아니라 구조적 패턴으로 보이는데, 이 사례 분리(splitting)가
  일어나는 단계가 이 저장소 스크립트(`build_final_dataset.py` 등)보다 앞선, 저장소
  밖의 원본 파싱 단계로 보여서 여기서는 원인 특정도 수정도 못함. 다음에 조사하려면
  같은 (source_file, 인접 case_number) 쌍의 raw_text를 여러 건 직접 비교하는 스크립트
  가 필요.

### ⏭️ 다음 세션은 여기부터
1. Vercel `VITE_API_BASE_URL` → `auditweb-production-2911.up.railway.app`로 교체 +
   재배포(위 "배포 설정 버그" 참고) — 이거 없으면 지금 배포본은 검색도 상세페이지도
   안 뜸.
2. README.md Railway 주소 갱신.
3. "(N-M) [처분유형] 사항" 문서 분리 경계 버그 — 위 항목 참고, 원인 조사 필요.
4. 표 셀 복원은 여전히 별도 대형 프로젝트(원본 PDF 레이아웃 인식 재추출 필요).
5. **"[관련자 명세]"/"현황/내역/명세/명단/실태" 표 캡션 인식 + 표 안 날짜 셀 보호를
   위에서 이미 반영함** — 아래 "라벨 인식 확장" 항목과 같은 파일(`HEADING_LABEL_PATTERNS`
   등)을 건드려서 병합 충돌 났었음(2026-08-14 같은 날 각각 다른 세션에서 작업),
   `git merge`로 충돌 해결 후 두 작업 다 반영된 채로 병합함 — 서로 다른 정규식이라
   겹치는 부분 없이 공존.

## ✅ Railway 볼륨 부족 대응 + 라벨 인식 확장(전수조사 기반) (2026-08-14)

### 오늘 한 일 요약
1. **Railway `postgres-volume` 77% 경고 대응** — 사용자가 대시보드에서 발견. 진단부터 시작:
   - `pg_database_size`로 실측: `railway` DB 2,485MB (documents 192MB + chunks 2,284MB,
     그중 인덱스만 1,105MB), 나머지 template DB는 정상(수 MB대) — orphaned DB 없음 확인.
   - dead tuple 비율(10~11%)은 정상 범위라 bloat 문제가 아님 확인. `VACUUM (VERBOSE,
     ANALYZE)`은 예방 차원으로 실행(0%까지 정리됨), Railway Query 탭이 트랜잭션으로 감싸서
     `VACUUM`/`ALTER SYSTEM` 둘 다 거기서 안 돌아가길래 Colab psycopg2
     `autocommit=True`로 우회.
   - `pg_ls_waldir()`로 WAL이 1,008MB(거의 `max_wal_size` 기본값 1GB 꽉 채움) 차지하는 거
     확인 — 최근 대량 UPDATE(재임베딩 등)로 쌓인 것으로 추정. `ALTER SYSTEM SET
     max_wal_size = '256MB'` + `pg_reload_conf()` + `CHECKPOINT`로 낮춰봤으나 **WAL이
     1,008MB에서 안 줄어듦** — replication slot(`pg_replication_slots` 비어있음),
     `wal_keep_size`(0), `archive_mode`(off) 다 확인했지만 전부 정상이라 원인 특정 못함.
     사용자가 "돈 안 들일거야"라고 해서 유료 볼륨 증설 대신, **다음 대량 재임베딩 작업
     때 HNSW 인덱스를 미리 DROP → 작업 → VACUUM → 인덱스 재생성** 순서를 지키는 무료
     대응책으로 정리하고 넘어감(8/10 사고 때 이미 검증된 패턴). WAL이 왜 정확히
     max_wal_size 근처에서 안 줄어드는지는 미해결 — 다음 세션에서 필요하면 이어서 조사.
2. **원문 잘림/표 컬럼 뒤섞임/라벨 이어붙임 3종 — Railway DB 67,751건 전수조사 완료**
   (`scripts/audit_truncated_documents.py`, `scripts/audit_table_column_interleave.py`
   실행 + 아래 3번 조사 신규 진행, 전부 읽기 전용, Colab에서 psycopg2로 실행):
   - **원문 잘림**: 초기 판정(1,160건, 1.71%)의 상당수가 "주의/불가/효과/증가" 등 조사
     "의/가/과..."로 끝나는 게 아니라 그 자체로 완결된 단어라서 오탐이었음(특히 "~주의"로
     끝나는 감사처분 표현이 관형격 조사 "의"로 오인됨) — 이 오탐 패턴 제외하고
     **416건(0.61%)**로 정제. 남은 목록은 실제로 "및/은/는/에"처럼 조사로 끝나 문장이
     끊긴 것들 위주로 훨씬 정확함(예: `1ca9faabf1df243f` 최초 발견 사례 포함).
   - **표 컬럼 뒤섞임**: 기존 스크립트(서울대학교치과병원 "감사결과 처분서" 3단 표 양식만
     겨냥)로 **49건** 확인 — 전부 서울대학교치과병원(2024~2026)·한국소비자원·
     한국세라믹기술원 소수. 새로 발견된 "2단 번호 리스트" 변종은 이번엔 조사 안 함(별도
     탐지 규칙 필요, 여전히 미해결).
   - **라벨 한 줄 이어붙임**: 처음 가설("여러 라벨이 줄바꿈 없이 한 줄에 이어붙음")은
     완전히 틀렸음(정밀 재조사 결과 0건) — 원래 발견 사례(한국환경산업기술원 2019)를 다시
     보니 라벨들이 실제로는 줄바꿈으로 이미 분리돼 있었음. **진짜 원인은 "부서명" 같은
     라벨 단어 자체가 `HEADING_LABEL_PATTERNS`에 아예 없어서 인식 실패**하는 거였음 —
     방향을 바꿔 "줄 맨 앞 `[라벨]` 형태 전체 목록화"(2,895종) → "그중 뒤에 내용이
     바로 붙어서 인식 실패하는 것만 추출"로 재설계, 영향 문서 수까지 정확히 집계함.
3. **위 3번 조사 결과로 `HEADING_LABEL_PATTERNS` 확장 — 배포 전, 코드 수정 완료**
   (`frontend/src/pages/DetailPage.jsx`):
   - 캡션형 그룹(표/그림/별표/붙임/별첨/참고/서식/양식)에 **"사진"**(`[사진 1]` 등,
     269건 영향) 추가.
   - 완결형 라벨 신규 그룹 추가 — 영향 문서 수 큰 순: 소관부점의견(824건)/통보(796건)/
     관련자(601건, 기존 패턴으로 1,287건은 이미 인식되고 있었음)/모범사례(351건)/
     조치할사항(101건, 547건은 기존 패턴으로 이미 인식)/현지조치(151건)/행정상조치(149건)/
     부서주의(144건)/부서명(22건, 이 조사를 시작하게 만든 라벨) 등 총 20개 키워드.
     겹치는 라벨(개선요구/개선, 관련자의견/관련자 등)은 긴 쪽을 먼저 둬서 짧은 쪽이 먼저
     매칭돼 나머지 글자 때문에 전체 매칭 실패하는 일 없게 순서 조정.
   - Node/esbuild로 실제 정규식 직접 검증(매칭돼야 하는 것 22개 + 매칭되면 안 되는 것
     5개, 전부 통과 — `[부서]`/`[코드]` 같은 익명화 placeholder는 여전히 안 걸림 확인,
     `esbuild --bundle=false`로 문법 오류도 확인). **아직 배포(main 병합) 안 함** —
     다음 세션에서 실제 커밋/푸시/배포 진행할 것.
   - "[시정(회수)]"/"[시정(기타)]"/"[통보(모범사례)]"처럼 대괄호 안에 괄호가 또 있는
     복합 변형은 이번엔 제외(건수 적고 별도 검증 필요).

### ⏭️ 다음 세션은 여기부터
1. **위 라벨 패턴 확장 커밋 + 배포** — 코드 수정은 끝났고 정규식 직접 검증도 했지만,
   아직 git commit/push 안 함. 다음 세션에서 커밋 메시지 정리해서 push, Vercel 배포
   확인, 가능하면 영향받은 문서 몇 건 상세페이지에서 실제로 눈으로 확인할 것.
2. **표 컬럼 뒤섞임 새 변종("2단 번호 리스트") 탐지 규칙** — 여전히 미해결, 위 STATUS.md
   이전 항목(8/13 2차, 7번) 참고.
3. **숫자 중복 collapse 가드 버그 수정** — 계속 미착수, 이전 항목(8/13 2차, 5번) 참고.
   재임베딩 작업 전에는 위 1번 항목의 "HNSW 인덱스 임시 DROP → 작업 → VACUUM → 재생성"
   순서를 반드시 지킬 것(볼륨 여유 없음).
4. **WAL이 max_wal_size 낮춰도 안 줄어드는 원인** — 오늘 못 찾음, 급하지 않으면 넘어가도
   되지만 재임베딩처럼 대량 쓰기 작업 전엔 볼륨 게이지 미리 확인할 것.
5. **README.md의 Railway 백엔드 URL 갱신** — 계속 미착수(8/13 2차, 4번부터 이어짐).

## ✅ 원본 파일 링크 + 실배포 + 탭 타이틀 버그 수정 (2026-08-13, 2차)

### 오늘 한 일 요약
0. **꺾쇠괄호(`<...>`) 표 캡션 인식 추가 — 배포 완료** — 사용자가 원본 PDF ↔ 웹페이지
   렌더링 스크린샷을 나란히 비교해서 발견("<☆☆☆☆☆본부 직장내 괴롭힘 관련자 현황>"
   캡션이 있는 표). 기존엔 `【...】`/`[표 N]` 스타일만 캡션으로 인식해서 이 줄이 앞
   서술 문단이랑 통째로 이어붙어 보였음. 기존 `/^【.+】/`(줄 전체를 캡션으로 인정)와
   완전히 같은 근거로 `/^<.+>$/`를 `HEADING_LABEL_PATTERNS`에 추가 — 일반 문장이 줄
   전체를 `<>`로 감싸는 경우는 사실상 없어 오탐 위험 낮다고 판단, 실제 예시 텍스트로
   `classifyLine` 로직 직접 검증 후 배포, Vercel 반영까지 확인함. **표 셀 구조 자체는
   추출 단계에서 이미 소실된 상태라 복원 못 함** — 캡션만 분리되고 표 내용(성명/나이
   등 실제 값)은 여전히 한 줄로 이어짐, 아래 5·6번(표 컬럼 뒤섞임 조사)과 같이 다뤄야
   완전히 해결됨.
0-1. **소관기관/조치기관/조치기한 라벨 + `[표-1]` 하이픈 캡션 인식 — 배포 완료** —
   역시 사용자가 원본 PDF ↔ 웹페이지 스크린샷 비교로 발견. 두 가지:
   - `제목/소관기관/조치기관/조치기한/내용` 표준양식("시정, 통보" 처분서)의
     소관기관/조치기관/조치기한 라벨이 기존 "관계기관" 패턴과 달라서 전혀 인식 안
     되던 것 — 관계기관과 동일한 방식으로 패턴 추가.
   - `[표-1] 50만원 이상 비품 구매내역`처럼 표 키워드-번호 사이가 **하이픈**인 경우
     기존 정규식(공백만 허용)이 못 잡던 것 — `[\s-]*`로 하이픈도 허용하도록
     `HEADING_LABEL_PATTERNS`·`TABLE_CAPTION_RE` 둘 다 수정. 이제 `[표-N]` 뒤 표
     데이터도 기존 "표/그림 데이터 접어서 보기" 동작이 정상 적용됨.
   - 둘 다 실제 문장으로 `classifyLine`/`TABLE_CAPTION_RE` 직접 검증 후 배포,
     Vercel 반영 확인함.
1. **원본 파일(GitHub) 하이퍼링크 — 배포 완료** — 어제/오늘 1차 항목 1번.
   - `textutils.build_source_url()` — `documents.source_file`에서 `data_repo/` 접두어를
     떼서 `raw.githubusercontent.com/haechyaning-commits/data/main/{경로}` URL로 변환,
     `DocumentDetail.source_url`로 노출.
   - `scripts/backfill_source_file.py` 추가(`backfill_audit_type.py`와 동일 패턴) —
     사용자가 Railway Query 탭에서 `ALTER TABLE documents ADD COLUMN IF NOT EXISTS
     source_file TEXT;` 실행 후 이 스크립트를 Colab에서 돌려 76,454건 백필 완료.
   - 상세페이지에 "원본 파일 보기" 링크 버튼(신뢰도 배지 옆, `source_url` 있을 때만).
   - `main`에 병합 배포 후 확인: `/documents/{id}` 500 없이 `source_url` 정상 응답,
     실제 반환된 raw.githubusercontent.com 링크도 200 OK로 열림(한국조폐공사 2016
     종합감사(27).pdf로 직접 확인) — 기능 end-to-end 완료.
   - **후속 수정**: 사용자 질문("다운로드가 나아 바로 보기가 나아?")으로 raw.
     githubusercontent.com이 보안상 모든 파일을 `application/octet-stream` +
     `nosniff`로 내려줘서 PDF/HWP 둘 다 무조건 다운로드였다는 걸 헤더로 직접 확인함 —
     `cdn.jsdelivr.net/gh/haechyaning-commits/data@main/`(GitHub 미러, 프론트가 폰트
     로딩에 이미 쓰던 CDN)로 base URL 교체. jsdelivr는 확장자 기준 실제 MIME 내려줘서
     PDF는 `application/pdf`로 와서 클릭 시 새 탭에서 바로 보임(직접 헤더 확인), HWP는
     jsdelivr에서도 `application/octet-stream`(브라우저에 렌더러가 없어서 CDN 상관없이
     항상 다운로드 — 정상, 못 고치는 부분). `textutils.build_source_url()`의
     `_SOURCE_REPO_RAW_BASE`만 교체, 경로 인코딩 로직은 동일.
2. **실배포 진행** — `main`에 병합해서 Vercel/Railway 자동배포 트리거함. 처음엔
   source_file 커밋을 제외한 안전한 것만 먼저 올렸다가(위 1번 백필이 끝나기 전이라),
   백필 완료 확인 후 source_file 커밋도 별도로 병합해서 총 3번 배포함. 확인된 것:
   - Vercel(`audit-web-phi.vercel.app`) 새 빌드 정상 반영(`/cases/` 라우트 번들에 포함 확인).
   - **Railway 도메인이 또 바뀌어 있었음** — README의 `auditweb-production-0dab`는 죽은
     도메인(엣지 404 `Application not found`), 실제는 `auditweb-production-573d`.
     README 갱신 필요(다음 세션 할 일에 추가).
   - `curl`로 `/health`, `/search`, `/documents/{id}` 직접 호출해서 새 필드(`title`)가
     실제로 나오는지 확인함 — Playwright로 브라우저까지 띄워보려 했으나 이 세션(코드
     샌드박스) 프록시 환경에서 Chromium이 `ERR_CONNECTION_RESET`으로 계속 실패해서
     API 레벨 확인까지만 함(브라우저 스크린샷 검증은 못 함).
3. **실사용 중 탭 타이틀 버그 발견 + 수정 + 재배포** — 배포 직후 사용자가 상세페이지를
   열어보다가 발견. 원인: `한국환경산업기술원 2019` 문서의 raw_text가
   `"제 목] 직장 내 괴롭힘\n [부 서 명] [부서]팀..."`으로 시작(원문 추출 단계에서 맨 앞
   여는 대괄호 `[`가 유실된 것으로 보임, "[제목] ... [부서명] ..." 대괄호-라벨 양식).
   `extract_title()` 정규식이 "목" 다음 구분자(공백/콜론)가 있는지 안 따지고 아무 문자나
   바로 캡처해버려서, 탭 타이틀에 `"] 직장 내 괴롭힘 - 공공감사데이터 검색"`이 그대로
   노출됨. `(?:[:：]\s*|\s+)`로 구분자 필수 조건 추가해서 구분자 없으면 매칭 자체가
   실패 → `None`(프론트가 기관명 등으로 폴백)으로 안전하게 처리하도록 수정, `main`에
   즉시 재배포해서 반영 확인 완료(Monitor로 `title:null` 될 때까지 폴링 확인).
4. **새로 발견한 별개 이슈(안 고침, 사용자 지시로 여기 기록만)** — 위 3번 문서처럼
   "[라벨] 내용 [라벨2] 내용2"를 **줄바꿈 없이 한 줄에 이어붙이는 양식** 문서는
   `DetailPage.jsx`의 `HEADING_LABEL_PATTERNS`가 이 양식을 전혀 인식 못 해서 전체가
   문단 구분 없이 통짜 텍스트로 보임("문단 나눠지는 것도 이상해" 피드백). 같이 확인한
   다른 `title: null` 문서 2건(코레일테크 "조사결과보고서", 한국산업안전보건공단
   "주 의 요 구")은 애초에 "제목" 라벨이 없는 **정상적으로 다른 양식**이라 이 버그와는
   무관 — 즉 이 특정 "대괄호 라벨 한 줄 이어붙임 + 첫 대괄호 유실" 조합이 실제로 몇 건
   있는지는 DB 직접 조회(`raw_text LIKE '%] %'` 류) 없이는 정확히 파악 불가(이 세션은
   DB 자격증명 없음). 이 코드베이스는 헤딩 정규식 하나 고칠 때마다 실제 문서로 여러
   차례 검증해온 이력이 있어서(과거 오탐 사고 있었음), 샘플 없이 지금 손대는 건 위험
   판단 — 다음 세션에서 Railway Query로 건수부터 확인하고 진행할 것.
5. **숫자 중복 collapse 가드의 실제 버그 발견 + 정확히 진단함(안 고침)** — 사용자가
   실사용 중 "2222222222222222200000000...2222222...5555555..." 처럼 극심하게 중복된
   문서를 발견(원래 "2025년"으로 추정). `fix_text_corruption.py`의
   `_collapse_digit_pass`는 "서로 다른 숫자 2종류 이상이 한 매치 안에 섞여 있을 때만
   collapse"하는 가드가 있는데(순수 마스킹 "0000" 보호 목적), **`DIGIT_CHAIN_RE`가
   같은 숫자가 4자리 넘게(예: 16~18자리) 반복되면 `\1{1,3}`(최대 4자리 단위) 반복이
   경계에서 나머지가 남아 다음 숫자 종류로 안 이어지고 매치가 숫자 종류별로 뚝뚝
   끊김** → 각 매치 안엔 항상 숫자 1종류만 남아서 가드가 무조건 "마스킹"으로 오판,
   진짜 심한 중복(2~4배가 아니라 16~18배급)일수록 오히려 절대 안 고쳐지는 역설적
   버그(실제 파이썬으로 재현 확인함, `scripts/fix_text_corruption.py`
   `_collapse_digit_pass`/`DIGIT_CHAIN_RE`). "2200224" 같은 2~4배 중복(코멘트의 원래
   가정)은 정상적으로 잘 고쳐짐 — 이 버그는 훨씬 심한 중복 사례에서만 발생.
   **재현**: `DIGIT_CHAIN_RE.finditer("22222222222222222000...")` 하면 숫자종류별로
   별개 매치 5개가 나옴(하나로 안 이어짐). 고치려면 가드 로직을 "매치 하나" 기준이
   아니라 "숫자 종류가 바뀌는 지점"을 넘나들며 판단하도록 바꿔야 함 — 정규식만 고치면
   될지, 별도 로직이 필요할지는 다음 세션에서 설계 필요. **고치면 이 corpus 전체
   재검사(`apply_fix`) + 바뀐 chunk 재임베딩(GPU, Colab) 사이클을 또 한 번 돌려야 함**
   (이번 세션의 재임베딩 34,764건과 같은 규모의 작업이 될 수 있음).
6. **"문단 중간에 짤림"/"2)부터 시작함" 제보 — 이미 추적 중인 카테고리로 확인됨** —
   사용자가 실사용 중 "1) 없이 2)부터 시작하는 원문", "리스트 항목 4)/5) 사이에 엉뚱한
   텍스트('근무중인자로서,') 끼어들고 중간에 짤림" 두 건 제보함. 직접 코드는 안
   고쳤지만, git 로그 확인해보니 **이미 직전 세션에서 이 클래스의 문제를 위한 읽기전용
   조사 스크립트가 만들어져 있었음**: `scripts/audit_truncated_documents.py`(원문
   잘림 전수조사), `scripts/audit_table_column_interleave.py`(표 컬럼 뒤섞임 —
   PDF 다단(멀티컬럼)/표 레이아웃 추출 시 열 순서가 섞이는 문제로 추정, "근무중인자로서,"
   처럼 문맥에 안 맞는 조각이 리스트 항목 중간에 끼어드는 것과 정확히 일치하는
   증상). 즉 **새로 발견한 게 아니라 이미 인지·조사 중인 이슈** — 다음 세션에서 이
   두 스크립트를 먼저 돌려서 규모부터 파악하고 고칠 방법 설계할 것.
7. **표 컬럼 뒤섞임 — 새 변종 확인, 기존 조사 스크립트로는 못 잡음** — 사용자가 원본
   PDF ↔ 웹페이지 스크린샷으로 확실한 사례 제시: 2단(2-column) 번호 리스트(왼쪽 칸
   1~3번, 오른쪽 칸 4~6번)가 PDF 추출 시 줄 단위로 나란히 읽혀서 "1. 소요판단...
   4. 손망실 보고..."처럼 한 줄에 섞여 들어감. `scripts/audit_table_column_interleave.py`
   내용을 `git show origin/main:...`으로 직접 확인해보니, **이 스크립트는 "연번
   지적사항 처분" 3단 표(서울대학교치과병원 등 "감사결과 처분서" 양식) 딱 하나만
   정확히 겨냥한 것**이고("1차 통계적 휴리스틱은 오탐 너무 많아 폐기"라고 스크립트
   주석에 명시돼 있음), 이번에 발견한 "2단 번호 리스트" 유형은 완전히 다른 양식이라
   전혀 안 잡힘. 즉 표 컬럼 뒤섞임은 **양식마다 다른 여러 변종이 있는 문제**로 확인됨
   — 원문 렌더링(프론트) 수정으로는 해결 불가(raw_text 자체가 잘못된 순서로 저장돼
   있음), 원본 PDF를 레이아웃 인식하며 재추출하거나 최소 이 새 양식 전용 탐지 규칙을
   실제 문서 다수로 새로 설계해야 함 — 오늘 한 라벨/캡션 인식 수정과는 급이 다른
   별도 프로젝트로 다뤄야 함.

### ⏭️ 다음 세션은 여기부터
1. **"[라벨] 한 줄 이어붙임" 문단 분리 이슈** — 위 4번 참고. Railway Query로 건수 확인
   먼저, 그 다음 `HEADING_LABEL_PATTERNS`에 안전하게 추가할 방법 검토.
2. **숫자 중복 collapse 가드 버그 수정** — 위 5번 참고. 로직 재설계 + corpus 재검사 +
   재임베딩(GPU) 사이클 필요 — 규모가 크므로 별도로 시간 잡고 진행.
3. **원문 잘림 / 표 컬럼 뒤섞임 조사 스크립트 실행 + 새 변종 대응** — 위 6·7번 참고.
   `scripts/audit_truncated_documents.py`, `scripts/audit_table_column_interleave.py`
   이미 만들어져 있음(둘 다 아직 실행 결과 확인 안 됨) — 단, 후자는 "연번 지적사항
   처분" 양식 하나만 겨냥해서 이번에 발견한 "2단 번호 리스트" 변종은 못 잡음. 별도
   탐지 규칙 설계 필요(원본 PDF 재추출까지 고려해야 할 수도 있는 큰 작업).
4. **README.md의 Railway 백엔드 URL 갱신** — `auditweb-production-0dab` (죽음) →
   `auditweb-production-573d` (현재).
5. **law.go.kr 스타일 반영 계속 다듬기**, **URL 구조 개선 마무리**(공유 미리보기
   메타태그, 브라우저로 직접 눈으로 확인) — 아래 1차 섹션 항목 그대로 이어서.

## ✅ 재임베딩 완료 확인 + URL 구조 개선 1단계 (2026-08-13)

### 오늘 한 일 요약
1. **제목 콜론 통일 재임베딩(34,764건) 완료 확인** — 어제 항목 1번. Colab 체크포인트
   끝까지 갔고 DB 반영 셀도 다 돌았음, 샘플 norm 확인(0.999850, 정상 범위)까지 끝.
   (중간에 검증 코드 자체 버그(`pgvector` 신버전에서 `SELECT` 결과가 numpy 배열이 아니라
   `Vector` 객체로 와서 `np.linalg.norm(np.array(sample))`이 TypeError 내는 문제)를
   `scripts/reembed_changed_chunks.py`에서 `.to_numpy()` 쓰도록 고침 — 실제 DB 반영
   자체는 이 버그와 무관하게 이미 끝나 있었음.)
2. **URL 구조 개선 1단계** — 어제 항목 2번 착수.
   - 백엔드: `textutils.extract_title()` 추가(raw_text 첫 줄 "제목 : ..."에서 파싱),
     `/search`·`/documents/{id}` 응답에 `title` 필드로 노출.
   - 프론트: `/cases/:id/:slug` 라우트 추가(`caseUrl.js`). **`/cases/기관명/연도/제목`처럼
     세그먼트를 쪼개진 않고 `:id`(실제 조회 키)+`:slug`(기관명-연도-제목 이어붙인 표시용
     장식, 파싱 안 함) 구조로 감** — 중복 기관/연도/제목 조합이 있어도 조회가 항상 id
     하나로만 결정돼서 충돌 걱정이 없어서 이 방식으로 택함. `/documents/:id`는 그대로
     남겨둠(예전 링크/북마크 안 깨짐) — DetailPage가 문서 로드 후 `history.replace`로
     조용히 새 URL로 교체함.
3. **탭 타이틀 반영** — `useDocumentTitle.js` 추가, 상세페이지 진입 시 브라우저 탭
   타이틀을 `{제목} - 공공감사데이터 검색`으로 바꿈(제목 파싱 실패한 소수 문서는
   기관명으로 폴백, 둘 다 없으면 기본 타이틀 유지). 페이지 이동(언마운트) 시 원래
   타이틀로 되돌림. **메타태그(og:title 등, 카톡/슬랙 공유 미리보기용)는 아직 안 함**
   — SPA라 서버사이드 렌더링 없이는 크롤러가 못 읽어서 별도 검토 필요(SSR 안 붙이고
   할 거면 각 사례별 정적 meta 태그를 미리 만들어두는 정도밖에 방법이 없음).
4. **실배포 확인 안 함** — Vercel에서 구 URL(`/documents/:id`) 공유 링크가 새 URL로
   잘 리다이렉트되는지, 검색결과 카드 클릭 시 새 URL로 바로 뜨는지 로컬 build만 확인,
   실제 브라우저로 눈으로 본 적은 없음.

### ⏭️ 다음 세션은 여기부터
1. **원본 파일(GitHub) 하이퍼링크** — 표/그림이 텍스트로 안 남는 문제의 근본 해결책으로
   요청함. 확인된 것: 원본 PDF/HWP가 `haechyaning-commits/data`(공개, 21.8GB, 클론은
   너무 커서 안 됨 — GitHub API로만 구조 확인함) 리포에 있고, `source_file` 경로에서
   `data_repo/` 접두어만 떼면 실제 경로와 정확히 일치함(예:
   `자체감사파일1/한국조폐공사_2016년 종합감사(27).pdf`). 남은 작업: `documents`에
   `source_file`(또는 GitHub 경로) 컬럼 추가 + 백필(`embed_ready_v2.jsonl`에서
   document_id별 source_file 매핑, audit_type 백필과 같은 방식) + 상세페이지에
   `raw.githubusercontent.com/haechyaning-commits/data/main/{경로}` 링크 버튼 추가.
2. **law.go.kr 스타일 반영 계속 다듬기** — 세리프 제거, 검색창 모서리, 목차 사이드바
   반응형 브레이크포인트, 검색결과 밀도 등 실사용 피드백대로 계속 손보는 중이었음.
3. **URL 구조 개선 마무리** — 위 "오늘 한 일 요약" 3·4번 참고(공유 미리보기 메타태그,
   실배포 확인).

## ✅ 복구 완료 + 데이터 품질 2차 수정 + UI 재설계 (2026-08-12)

어제(08-11) 끊겼던 재임베딩부터 이어서 전체 복구 마무리, 그 다음 텍스트 품질을 실제
문서로 하나씩 검증하며 계속 수정, 마지막엔 law.go.kr 스타일 UI 반영까지 진행.

### ⏭️ 다음 세션(내일)은 여기부터
1. **제목 콜론 통일 재임베딩(34,764건) 완료 확인** — `documents.raw_text`는 이미 반영
   확인함(콜론 잘 붙어있음), 근데 `chunks.embedding` 쪽 재임베딩은 오늘 세션 끝날 때까지
   진행 중이었을 수 있음. Colab 체크포인트(`reembed_checkpoint.jsonl`)가 끝까지 갔는지,
   DB 반영 셀까지 다 실행됐는지부터 확인. (안 끝났으면 체크포인트가 있으니 이어서 하면 됨)
2. **URL 구조 개선** — 지금 `/documents/{해시}`인데, law.go.kr처럼 `/cases/기관명/연도/
   제목` 형태로 바꾸고 싶어함. 막힌 지점: API에 "제목"이 별도 필드로 없음(원문 텍스트
   안에만 있음) — 백엔드에서 raw_text 첫 줄("제목: ...")을 파싱해서 새 필드로 내려주는
   작업부터 필요. 프론트 라우팅도 `/documents/:id` -> `/cases/:id/:slug` 식으로 바꾸고
   기존 링크 안 깨지게 처리 필요.
3. **원본 파일(GitHub) 하이퍼링크** — 표/그림이 텍스트로 안 남는 문제의 근본 해결책으로
   요청함. 확인된 것: 원본 PDF/HWP가 `haechyaning-commits/data`(공개, 21.8GB, 클론은
   너무 커서 안 됨 — GitHub API로만 구조 확인함) 리포에 있고, `source_file` 경로에서
   `data_repo/` 접두어만 떼면 실제 경로와 정확히 일치함(예:
   `자체감사파일1/한국조폐공사_2016년 종합감사(27).pdf`). 남은 작업: `documents`에
   `source_file`(또는 GitHub 경로) 컬럼 추가 + 백필(`embed_ready_v2.jsonl`에서
   document_id별 source_file 매핑, audit_type 백필과 같은 방식) + 상세페이지에
   `raw.githubusercontent.com/haechyaning-commits/data/main/{경로}` 링크 버튼 추가.
4. **law.go.kr 스타일 반영 계속 다듬기** — 오늘 1차 반영 후 실사용 피드백으로 세리프
   제거, 검색창 모서리, 목차 사이드바 반응형 브레이크포인트, 검색결과 밀도를 이미
   되돌림/조정함. 추가로 더 손볼 곳 있으면 이어서.

### 오늘 한 일 요약

**1) 재임베딩 마무리 + 인덱스**
- 어제 끊긴 재임베딩(34,409건) 이어서 완료, DB 반영까지 끝. HNSW/GIN/document_id
  인덱스 생성 완료(공유메모리 에러는 `max_parallel_maintenance_workers = 0`으로 해결,
  이후 Railway Query 탭 자체의 짧은 실행 타임아웃 문제는 Colab에서 직접 psycopg2로
  돌려서 우회).
- DB 반영 방식을 `executemany`(1건씩 왕복) 대신 `execute_values`(여러 건 한 왕복)로
  바꿔서 훨씬 빠르게 만듦 — 388건/34,764건짜리 2차 수정 때는 이 빠른 버전만 씀.

**2) 백엔드/프론트 재배포**
- Railway 백엔드 서비스가 없어진 상태였어서 GitHub 연결로 새로 생성. 도메인이 배포
  중간에 한 번 더 바뀜(`...-30d9` → `...-7469`, Public Networking 도메인 재생성 때문으로
  추정) — 바뀔 때마다 Vercel `VITE_API_BASE_URL` 갱신 + 재배포 필요했음.
- CORS(`FRONTEND_ORIGIN`)도 값 반영 후 재배포 안 하면 안 먹는 문제 있었음(환경변수는
  재시작 시점에 읽음) — 재배포로 해결.
- `frontend/src/pages/SearchPage.jsx`의 `YEAR_COUNTS`를 실제 쿼리 결과로 채움(2016~2026,
  총 67,751건 중 year NULL 2건 제외 67,749건 반영). 정적 값이라 데이터 바뀌면 수동 갱신
  필요 — 나중에 API로 바꾸는 것도 고려해볼 만.

**3) 프론트엔드 UI 버그/피드백 대응 (여러 라운드)**
- 검색 중 히어로(통계카드+그래프)가 로딩 스켈레톤이랑 같이 뜨던 버그 → `results===null
  && !loading` 조건으로 수정
- 레이아웃 폭(880/1000px)이 넓은 화면에서 너무 좁아 보이던 문제 → 확대
- 검색결과 미리보기가 `left(text, 200)`로 문장 중간에서 뚝 잘리던 문제 →
  `backend/app/textutils.py`(`build_preview`)로 문장/어절 경계에서 자르고 "…" 붙이게 함
- 하이라이트가 조사/어미("때" 등)까지 잡아서 정작 중요한 키워드가 안 보이던 문제 →
  `highlight.jsx`에 불용어 목록 추가, 긴 단어부터 매칭
- 자유형 요약이 실패 캐싱돼서 계속 안 나오던 문제 → 파싱 관대화(정확히 4줄 아니어도 됨)
- 재검색 placeholder 문구 조정
- `<html lang="en">` → `lang="ko"`, 404 catch-all 라우트 없어서 빈 화면 뜨던 버그 수정,
  히어로 메인 검색창 키보드 포커스 표시 누락(접근성) 수정

**4) 원문 구조화 렌더링 (제일 여러 번 손봄, 실제 문서로 계속 재현/검증)**
- `frontend/src/pages/DetailPage.jsx`의 `renderRawText`가 원문을 문단/헤딩/불릿/캡션/
  표 데이터 블록으로 나눠서 렌더링하도록 여러 차례 다듬음:
  - PDF 줄바꿈(페이지 폭 기준)을 그대로 블록화하면 한 문장이 조각조각 끊겨 보이던 문제
    → 헤딩/불릿/빈줄 전까지는 문단으로 이어붙임
  - "○" 불릿을 헤더 취급해서 굵게 만들었더니 문장이 반쪽만 굵게 보이던 문제 → 불릿은
    문단 구분 신호로만 쓰고 안 굵게
  - "(현황)" 같은 괄호 단독 라벨이 아무 패턴에도 안 걸려서 문단에 흡수되던 문제 → 패턴 추가
  - "※"/"*" 각주가 줄바꿈만으로 나열될 때 한 문단으로 뒤섞이던 문제 → 불릿 신호에 추가
  - 표/그림 캡션(`[표 N]`/`【표 N】`/`[별표 N]`) 뒤에 셀 구분 없이 남은 텍스트를 접힌
    `<details>` 박스로 분리 — "자료: ... 재구성" 출처 표기 뒤에 헤딩/불릿 없이 실제
    문장이 바로 이어지는 경우, 표 블록이 그 내용까지 삼켜버리던 심각한 버그를 "자료:"/
    "출처:" 경계 인식으로 수정
  - **회귀 버그**: 대괄호 캡션 패턴(`/^\[.{1,12}\]/`)이 "[표 1]"뿐 아니라 익명화
    placeholder "[부서]"로 시작하는 일반 문장까지 전부 헤딩으로 오인식 — 표 블록도 이것
    때문에 행마다 끊겨서 조각났었음. 캡션 키워드(표/그림/별표/붙임/참고/서식/양식)로
    한정해서 수정.
  - 매번 짐작으로 정규식 고치는 대신, 라이브 API에서 실제 문제 문서를 찾아 정확한 줄바꿈
    구조를 확인하고 재현 테스트한 뒤 수정하는 방식으로 진행(오탐 여러 번 잡음)

**5) 감사종류(audit_type) 추가**
- 원본 파일명(`기관명_연도_감사종류(번호).확장자`, `source_file` 필드)에서 감사종류
  파싱 — 처음엔 패턴을 짐작("기관_연도_감사종류" 언더스코어 2개)했다가 실제 데이터로
  틀렸다는 걸 확인, 진짜 패턴("기관명_연도년 감사종류(번호)", 언더스코어 1개 + 공백
  구분)으로 다시 짬. `scripts/textparse.py` 신규.
- `documents.audit_type` 컬럼 추가 + Colab 백필 스크립트(`scripts/backfill_audit_type.py`,
  DRY_RUN 안전장치)로 67,751건 전부 반영(종합감사 42,571 / 특정감사 13,500 / 복무감사
  8,100 / 성과감사 2,284 / 재무감사 1,242 / 기타 소수).
- 검색결과 카드 + 상세페이지에 표시.

**6) 텍스트 오염 2차 수정**
- 괄호/인용부호 중복("｢｢...｣｣" 같은 bold_dup의 괄호 버전) 발견 → `fix_duplicated_brackets`
  추가, 276개 문서/388개 청크 반영 + 재임베딩 완료.
- 제목 라벨("제목 XXX" 콜론 없음, "제 목 XXX"도) 통일 → `normalize_title_colon` 추가,
  33,707개 문서/34,764개 청크 대상 — **재임베딩 완료 여부 내일 확인 필요**(위 참고).
- 단독 한글 글자 중복("목목", "조조" 등, 연쇄 안 된 경우) 실태조사
  (`scripts/audit_isolated_duplicates.py`, 읽기 전용) — 33,944개 문서에서 350종 발견됐는데,
  "공공"(8,298건)/"지지"/"매매"/"주주"/"의의"/"정정" 등 대부분 정상 단어이거나 복합어
  경계("구성성분"→"성성" 등)에서 우연히 생기는 패턴으로 확인됨. 형태소 분석 없이는
  진짜 오류와 구분 불가 판단 → **자동 수정 보류**, 사용자 승인하에 안 건드리기로 결정.

**7) law.go.kr 스타일 UI 반영**
- 사용자가 law.go.kr(국가법령정보센터) URL 두 개 보여주며 참고 요청 → 4가지 방향(밀도,
  조문 목차 네비게이션, URL 구조, 타이포그래피/색상) 확인 후 실제 사례 데이터로 아티팩트
  시안 제작, 승인받고 반영:
  - 색상 토큰 KRDS 밝은 블루(#256ef4) → 절제된 진남색(#1a4d8f), 카드 그림자/둥근모서리 축소
  - 검색결과: 카드 그리드 → law.go.kr류 목록형(번호·본문·감사종류 3열 행)
  - 상세페이지: 헤딩 3개 이상 문서에 좌측 목차 사이드바 추가(클릭 시 해당 조항으로 스크롤)
  - 문서 제목을 다른 헤딩보다 크게 구분 표시
- **반영 후 실사용 피드백으로 일부 되돌림**: 히어로/상세제목 세리프체가 어색하고
  시스템별로 이상하게 보임 → 제거(고딕 유지). 검색창 모서리가 너무 각짐(6px) →
  12px로 완화. 목차 사이드바가 800~1000px대 창 폭에서 본문을 짓누르던 실제 버그
  (브레이크포인트 760px) → 1040px로 올려서 해결. 검색결과 목록이 "글씨만 빽빽하다"는
  피드백 → 패딩 확대, 감사종류 다시 색 pill로, 번호를 원형 컬러 배지로, 미리보기 2→3줄.

**8) 기타**
- GitHub `data` 리포(21.8GB) 구조를 API로 확인(클론은 시간 초과로 불가) — 원본파일
  하이퍼링크 기능 준비 작업.
- Playwright로 실제 스크린샷 검증 시도했으나 이 샌드박스의 프록시 설정과 호환 안 됨
  (사이트 종류 상관없이 `net::ERR_CONNECTION_RESET`) — 이후 디자인 점검은 코드/CSS
  직접 검토 + 사용자 육안 확인으로 진행.

---

## 🚨 Railway DB 실수로 삭제 → 전체 복구 진행 중 + UI 대량 손질 (2026-08-11, 배포 완료 이어서)

### ⏭️ 다음 세션(내일)은 여기부터
**진행 순서(1~5까지는 이미 완료, 6번 재임베딩만 진행 중 상태로 오늘 끊김):**
1. ~~스키마 생성~~ ✅
2. ~~원본 데이터 적재(72,913건/96,355건)~~ ✅
3. ~~텍스트 오염 수정(documents 28,027건/chunks 34,727건)~~ ✅ — DB 반영까지 완료, 안전함
4. ~~재추출 불가 문서 제거(75건 — 이번엔 5,162건이 아니라 75건만 나왔는데, 이유는 아래
   "문서 개수 관련 참고" 항목 참고, 버그 아님)~~ ✅ — documents 67,751건, chunks 89,301건으로
   확정됨(예전 정상 상태와 정확히 일치)
5. ~~인덱스는 아직 X (아래 8번에서 진행)~~
6. **재임베딩 — 오늘 세 번째 시도 중 끊김.** 체크포인트 기능을 추가해뒀으니(아래 "재임베딩
   반복 크래시" 참고) 내일은 **`scripts/reembed_changed_chunks.py` 최신 버전**(체크포인트
   지원) 그대로 셀에 붙여넣어 실행 — 중간까지 됐던 건 자동으로 건너뛰고 이어서 함.
   입력 파일(`reembed_input.jsonl`)과 체크포인트(`reembed_checkpoint.jsonl`)는 Google
   Drive(`MyDrive/audit_project/`)에 있음 — `INPUT_PATH`를 이 Drive 경로로 직접 지정해서
   실행할 것 (로컬 `/content` 복사 불필요)
7. → 재임베딩 끝나면 **인덱스 생성**: Railway Query 탭에 `scripts/schema_indexes.sql` 실행
   (공유메모리 에러 뜨면 `SET max_parallel_maintenance_workers = 0;` 먼저 실행)
8. → **최종 확인**: documents 67,751건, chunks 89,301건 카운트 재확인
9. → **백엔드(FastAPI) 재배포 필요** — 새 DB(`DATABASE_URL`)로 연결, `OPENAI_API_KEY`/
   `FRONTEND_ORIGIN` 환경변수도 다시 설정. Railway 도메인이 또 바뀔 수 있음(이번 사고 전에도
   한 번 바뀐 적 있었음)
10. → Vercel `VITE_API_BASE_URL`을 새 백엔드 도메인으로 갱신 + 재배포
11. → **연도별 문서 수 쿼리** 돌려서 `SELECT year, count(*) FROM documents GROUP BY year
    ORDER BY year;` 결과를 `frontend/src/pages/SearchPage.jsx`의 `YEAR_COUNTS` 배열에 채우기
    (지금은 빈 배열이라 `YearChart` 컴포넌트가 화면에 안 뜨는 상태 — 채우면 바로 나타남)
- → **6번 이슈(4줄 요약 안 됨) 재확인** — DB가 완전히 새로 적재된 거라 `summary_failed` 캐싱
  문제는 자동으로 해결된 상태. `OPENAI_API_KEY`만 맞으면 정상 작동할 것

### 재임베딩 반복 크래시 (오늘 세 번 걸림, 다음엔 체크포인트로 방지됨)
- 1차: `!python scripts/reembed_changed_chunks.py`로 서브프로세스 실행 → `google.colab.userdata`가
  Colab 커널과 통신 못 해서 DB 반영 직전에 크래시 → 34,727건 임베딩(약 1시간) 통째로 날아감
- 2차: 같은 실수(`!python`)로 또 크래시, 또 날아감
- 3차(오늘 마지막 시도): 셀에 직접 붙여넣어서 정상 진행 중이었으나, 사용자가 시간 제약으로
  컴퓨터를 꺼야 해서 중간에 끊긴 상태로 오늘 세션 종료
- **대응**: `scripts/reembed_changed_chunks.py`에 체크포인트 저장/재개 기능 추가(커밋
  `63f306b`) — 배치마다 계산된 벡터를 Drive에 즉시 append 저장, 재실행 시 이미 된 것은
  자동 스킵. 앞으로 이 문제로 시간 날릴 일 없음
- **교훈**: 이런 장시간 GPU 스크립트는 반드시 ①노트북 셀에 직접 붙여넣기(`!python` 금지)
  ②체크포인트를 Drive(로컬 `/content` 아님)에 저장, 이 두 가지를 기본값으로 할 것

### 문서 개수 관련 참고 (헷갈렸던 부분, 결론은 문제없음)
텍스트 수정 단계(`fix_text_corruption.py`)를 돌렸을 때 "documents 전체 67,826건 중..."으로
찍혀서, 분명 72,913건을 적재했는데 왜 67,826건인지 세션이 완전히 해명은 못 했음. 재추출
불가 문서 제거 단계에서 마저 75건이 추가로 빠지면서 최종 67,751건으로 맞아떨어졌는데, 이는
예전(사고 전) 최종 확정치와 정확히 일치함. classify() 로직이 텍스트 내용 기준 결정론적이라
그렇게 수렴한 것으로 추정 — 몇 건이 정확히 어느 시점에 빠졌는지 로그로 재구성은 안 했지만,
최종 숫자가 목표치와 정확히 일치하므로 실질적 문제는 없다고 판단하고 넘어감.

### 사고 경위
Railway 백엔드 서비스를 "비용 관리 목적으로 지워도 되냐"는 질문에 세션이 "예전에 하시던
패턴(백엔드 서비스만 삭제, Postgres는 유지)이면 괜찮다"고 답했는데, **실제로는 Postgres DB
자체를 지우고 다시 "Add"로 새로 만드신 상태**였음 — 확인 없이 답한 세션 쪽 실수. 그 결과
67,751건 문서 + 96,355건 청크(벡터 포함) 데이터가 전부 날아감.

### 복구 경로 확인
- 로컬 CSV 백업(전에 진단용으로 만들었던 것) 있는지 Drive 전체 검색해봤으나 **없음** —
  다른 프로젝트(`dacon_medical`) 파일만 발견됨
- 원본 소스 파일(`documents_final.jsonl`, `chunks_final.jsonl`, 임베딩)은 Google Drive에
  그대로 남아있어 **전체 파이프라인 재실행으로 복구 가능**하다고 판단, 처음부터 재실행 시작

### 복구 파이프라인 (오늘 실행한 순서, 다음에 또 필요하면 참고)
0. Colab GPU 런타임으로 먼저 시작 (중간 재시작 방지 — 안 그러면 로컬 임시파일 날아감,
   실제로 한 번 이렇게 날려서 재임베딩을 처음부터 다시 함)
1. Drive 마운트 + 레포 클론 + `DATABASE_URL`을 Colab 시크릿에서 로드
2. Railway Query 탭에 `schema_tables.sql` 실행 (테이블만, 인덱스는 맨 마지막)
3. `python scripts/load_to_postgres.py` — 원본 72,913건/96,355건 그대로 적재
4. `python scripts/fix_text_corruption.py` — 1차+2차 텍스트 오염 수정 통합 적용
   (이번엔 documents 28,027건 / chunks 34,727건 수정 대상 — 예전 개별 라운드 합계랑
   정확히 똑같진 않은데, 로직이 통합돼서 그런 거라 정상 범위로 판단)
5. 재임베딩(`reembed_changed_chunks.py` 내용을 **노트북 셀에 직접 붙여넣어** 실행) — 이거
   34,727건, 배치당 약 6.5~7초라 총 60~70분 정도 소요

### 이 과정에서 겪은 사고들 (전부 해결, 다음에 또 겪으면 바로 인지할 것)
- **Public Networking 꺼짐** — 새로 만든 Postgres는 기본적으로 외부 접속(Colab 등)이 막혀있음.
  Railway Postgres 서비스 → Settings → Networking → **"Add Public Access"** 눌러야
  `DATABASE_PUBLIC_URL`이 생기고 외부 연결 가능해짐
- **`vector type not found`** — `CREATE EXTENSION IF NOT EXISTS vector;`를 안 돌린 상태에서
  적재 스크립트를 돌려서 발생. `schema_tables.sql`을 먼저 전체 실행해야 함
- **`relation "documents" does not exist`** — 확장만 만들고 테이블(`CREATE TABLE`)은 아직 실행
  안 한 상태였음. `schema_tables.sql` 전체를 다 돌려야 함
- **`AttributeError: 'NoneType' object has no attribute 'kernel'`** (재임베딩 1차 시도) —
  `reembed_changed_chunks.py`를 `!python scripts/...py`로 서브프로세스 실행해서, 그 안에서
  부르는 `google.colab.userdata.get()`이 Colab 프론트엔드(커널)에 접근을 못 해 실패함. 이
  스크립트는 원래 주석에도 "노트북 셀에 파일 내용 그대로 붙여넣어 실행"이라고 적혀 있었는데
  세션이 `!python`으로 잘못 안내함 → **34,727건 임베딩(약 60분 분량)이 서브프로세스 메모리와
  함께 통째로 날아가서 처음부터 재실행**해야 했음. 교훈: 이 스크립트류는 반드시 셀에 직접
  붙여넣어서 실행할 것, `!python`으로 돌리지 말 것
- **`reembed_input_2.jsonl` FileNotFoundError** — `fix_text_corruption.py`가 기본값으로
  `reembed_input.jsonl`(2 없음)에 저장하는데, `reembed_changed_chunks.py`는
  `reembed_input_2.jsonl`을 찾음. 파일명이 안 맞아서 `!cp reembed_input.jsonl
  reembed_input_2.jsonl`로 이름 맞춰줘야 함 (스크립트 두 개 사이 네이밍 불일치, 코드 수정은
  안 하고 우회만 함 — 나중에 여유 있으면 스크립트 쪽 정리해도 됨)

### 같은 시간에 진행한 프론트엔드 UI 개선 (DB 복구랑 별개로, 코드 작업이라 기다리는 동안 병행)
사용자 피드백 기반으로 여러 라운드 진행, 전부 커밋+push 완료(`main` 반영도 끝):
1. **검색 결과 카드 정리** — 신뢰도 배지를 목록에서 제거(상세페이지엔 유지, TOP 순위랑
   같이 있으면 혼란 줬음), 기관명/연도 분리 표시(긴 기관명이 연도까지 잘라먹던 문제 해결),
   미리보기 6줄→4줄
2. **백엔드 미리보기 텍스트 소스 변경** — `raw_text` 맨 앞 150자(정형화된 서류 양식 헤더라
   검색어와 무관한 내용만 보였음) 대신 **실제 매치된 청크(`chunks.text`)** 사용하도록
   `repository.py` SQL 수정. DB 복구 끝나고 재배포하면 적용됨
3. **히어로 영역 "전체화면일 때 너무 비어보임" 개선** — 배경 장식(블롭 등) 대신 콘텐츠
   자체를 키우는 방향으로 결정:
   - `.hero-inner` 720px → 880px, 제목/부제 폰트를 `clamp()`로 화면 폭에 비례해서 확대
   - 스탯카드 3개 추가(67,751건 데이터 / 벡터+키워드 검색 결합 / AI 4줄 요약 자동생성) —
     패딩/아이콘도 넓은 화면에서 소폭 확대
   - **연도별 막대그래프 컴포넌트(`YearChart.jsx`) 준비** — 미리보기에서 4가지 스타일(세로
     막대/가로 막대 리스트/영역그래프/스파크라인) 보여주고 세로 막대(A안)로 확정. 데이터
     없으면 null 반환해서 화면에 안 뜨는 안전장치 넣어둠 — DB 복구 끝나면 연도별 카운트
     쿼리 결과를 `SearchPage.jsx`의 `YEAR_COUNTS`에 채우기만 하면 바로 나타남
4. **검색창 placeholder 문장형으로 변경** ("예: 출장비 부당 집행 (검색창 포커스는 / 키)"
   → "예: 직장 상사가 부하 직원을 지속적으로 괴롭혀서 신고하고 싶어요", 단축키 안내는
   placeholder에서 제거)
5. **예시 칩 키워드+문장형 혼합**으로 변경 (전부 키워드였던 것에서)
6. **로고/타이틀 클릭 시 진짜 랜딩화면으로 초기화** (`useSearchState.resetSearch` 추가 —
   예전엔 URL만 "/"로 바뀌고 검색 결과 상태가 안 지워져서 그대로 남아있었음)
7. **헤더 좌우로 확장** (860px → 1400px, 전체화면일 때 로고/타이틀(좌)·BETA+다크모드(우)가
   자연스럽게 벌어지게)
8. **상세페이지**: 검색 컨텍스트 안내("'검색어'와 유사해서 노출된 사례") + 원문 하이라이트
   추가, 원문(raw_text) 가독성 개선(이중스크롤 제거, 폰트/줄간격 확대)
9. **원본 파일(HWP) 링크 연결은 보류** — DB에 원본 URL/파일 참조 필드 자체가 없어서 지금
   데이터로는 불가능, 하려면 별도로 원본 소스에 사례별 URL이 있는지부터 확인 필요

### 확인/완료된 것
- `claude/violence-document-search-08aegl`, `claude/frontend-work-7qu7t9` 원격 브랜치 —
  이미 삭제되어 있음 확인 (별도 조치 불필요)

## 🎉 4주차 배포 완료 — 프론트엔드 Vercel 배포 + 실서비스 최종 확인 (2026-08-11)

**development-plan.md 4주차 목표(배포) 완료.** 사용자가 Vercel 대시보드를 직접 조작하고, 이
세션은 절차 안내 + `vercel.json` 준비(코드) + curl로 실데이터 검증을 담당하는 방식으로 진행.

### 겪은 문제 + 해결 (전부 이번 세션에 처리됨)
- **GitHub 계정 꼬임** — 레포 소유 계정(`haechyaning-commits`)과 평소 쓰던 Vercel 로그인
  계정(GitHub `eemiso` 연결)이 서로 달라서 Import 화면에 `audit_web`이 안 보였음. 해결: Vercel
  드롭다운의 **"Switch Git Provider"**로 연결된 Git 계정을 `haechyaning-commits`로 전환 →
  바로 해결됨 (`+Add GitHub Account`로 두 번째 계정을 추가하는 방식은 이 Vercel 계정 구조상
  안 먹혔음 — 개인 Vercel 계정은 Git 연결이 1개로 고정되는 듯)
- **상세페이지 새로고침 404** — `react-router-dom` 클라이언트 라우트(`/documents/:id`)용
  `vercel.json` rewrite 설정이 없어서 발생. `frontend/vercel.json` 신규 추가
  (`{"rewrites": [{"source": "/(.*)", "destination": "/index.html"}]}`), 처음엔 작업 브랜치에만
  커밋했다가 Vercel이 `main` 기준으로 배포한다는 걸 뒤늦게 확인 → **`main`에 직접 push**
  (커밋 `74b5192`, 사용자 명시적 승인 받고 진행)
- **Railway 백엔드가 완전히 죽어있었음** — 지난 세션 종료 후 방치된 상태였는지 `/health`가
  Railway 엣지 레벨 404(`Application not found`, `x-railway-fallback: true`)를 반환.
  재배포 진행하며 연쇄적으로 문제 3개 발견/해결:
  1. `DATABASE_URL`에 진짜 값 대신 `호스트:포트` 같은 **한글 플레이스홀더 텍스트가 그대로
     들어있었음** (`ValueError: invalid literal for int(): '포트'`) → Colab 시크릿에 저장된
     검증된 값으로 교체
  2. 그다음엔 `DATABASE_URL` 자체가 비어있는 상태로 배포됨 → 다시 채워 넣음
  3. 그래도 `unknown type: public.vector` 재발 → Colab에서 직접 그 값으로 연결해서
     `pg_extension`에 `vector` 있는지, `documents` 카운트가 67,751건 맞는지 실측 검증(정상
     확인) → 그런데도 Railway에서 계속 같은 에러 → 결국 도메인 라우팅 문제였음이 다음
     항목에서 드러남
  4. 앱 자체는 정상 기동(`Uvicorn running on 0.0.0.0:8080`)됐는데도 도메인이 계속
     `Application not found` → 예전에도 겪었던 "도메인이 서비스에 아직 안 붙음" 문제로 판단,
     **도메인 삭제 후 재생성**으로 해결 → 새 도메인
     `auditweb-production-0dab.up.railway.app` 발급됨 (기존 `...22b3...`는 이제 죽은 URL)
- **도메인이 바뀌어서 Vercel `VITE_API_BASE_URL` 재설정 필요** — 새 Railway 도메인으로
  업데이트 후 Vercel 재배포(환경변수는 빌드 타임에 박히는 값이라 저장만으론 반영 안 됨,
  redeploy 필수)

### 최종 검증 (curl로 직접 확인, 전부 통과)
- `GET /health` → `{"status":"ok"}`
- `GET /search?q=출장비` → 실제 사례(전남대학교병원 "출장비 중복 수급" 등) 정상 반환
- CORS: `Origin: https://audit-web-phi.vercel.app` 헤더로 요청 시
  `access-control-allow-origin` 정상 응답
- Vercel 빌드 산출물(JS 번들)에 새 Railway 도메인이 정확히 박혀있는지 직접 grep으로 확인
- SPA 라우팅: `/documents/123` 새로고침 시 200 (더 이상 404 아님)

### 배포 URL (최신)
- 프론트엔드: https://audit-web-phi.vercel.app
- 백엔드: https://auditweb-production-0dab.up.railway.app (도메인 바뀐 것 주의 — 옛날
  `...22b3...` URL은 더 이상 안 씀)

### README.md 갱신
- 프론트엔드/백엔드 배포 링크를 위 최신 URL로 갱신 완료

### 다음
- [ ] README.md 남은 `[ ]` 항목(왜 만들었나 / 스크린샷 / 검색 품질 검증 / 한계와 다음 계획 /
      비용) — 사용자의 개인 경험·실측치 필요, 여전히 미착수
- [ ] `claude/violence-document-search-08aegl`, `claude/frontend-work-7qu7t9` 원격 브랜치 정리
      (main에 이미 흡수됐지만 아직 안 지워짐)
- [ ] Railway 도메인이 이번처럼 또 죽을 수 있으니, 다음에 이 문제 마주치면 "앱 로그부터
      확인(정상 기동됐는지) → 정상인데도 404면 바로 도메인 재생성"으로 시간 절약할 것

---

## 🚀 Railway 재배포 + 실데이터 재검증 + PR #5(프론트엔드) main 병합 (2026-08-10, 코드 샌드박스 세션)

**이 세션은 Railway/DB 자격증명이 없는 코드 샌드박스라 재배포 자체는 사용자가 직접 진행**,
이 세션은 절차 안내 + 재배포 후 실데이터 검증 + PR 병합만 수행.

### 완료
- Railway 재배포 절차 안내(Root Directory=`backend`, `DATABASE_URL`/`OPENAI_API_KEY`/`PORT=8080`
  환경변수, 도메인 타겟 포트 등 과거 트러블슈팅 내용 포함) → 사용자가 직접 재배포 진행
- **실데이터 재검증 완료**: `https://auditweb-production-22b3.up.railway.app`
  - `GET /health` → `{"status":"ok"}`
  - `GET /search?q=출장비` (키워드 검색), `GET /search?q=상사가 부하직원 괴롭힘` (의미기반 검색 →
    "직장 내 괴롭힘" 관련 실제 사례들과 정확히 매칭) 둘 다 실제 라이브 DB 기준 정상 응답 확인
  - (참고: 이 세션의 headless 브라우저는 아웃바운드 프록시 특성상 외부 사이트 TLS 접속이
    `ERR_CONNECTION_RESET`으로 막혀서 실제 화면을 직접 스크린샷하지는 못함 — `curl`로 받은 실제
    JSON 응답을 별도로 렌더링해서 시각 확인용 이미지로 대체함. 다음에 또 이 문제 마주치면
    "브라우저 트래픽만 프록시에서 리셋되고 curl/node http는 정상" 패턴으로 인지하고 바로 우회할 것)
- **PR #5(`claude/frontend-work-7qu7t9`, 3주차 프론트엔드 구현) → `main` 병합 완료**
  (충돌 없음, 병합 커밋 `79e17e4`). 병합 직후 Railway가 `main` 추적 중이라 자동 재배포 트리거됨 →
  잠깐 전체 엔드포인트 502(`Application failed to respond`) 떴다가 재기동 후 정상 복구 확인
  (재배포 중 일시적 502는 정상 — 당황하지 말 것)
- 브랜치 정리 상태 확인: `claude/violence-document-search-08aegl`, `claude/frontend-work-7qu7t9`
  둘 다 이제 `main`의 조상 커밋(고유 커밋 0개) — 원격에서 삭제해도 안전. 단, 이 세션은 git-level
  push 권한이 막혀있어(`git push --delete` 403) 실제 삭제는 아직 못함 — 다음에 대시보드에서
  수동 삭제하거나 push 권한 있는 세션에서 처리
- README.md 갱신: 백엔드 API 배포 링크 채움(`/health` 등), "로컬에서 실행하기" 섹션을
  `backend/README.md`/`frontend/README.md` 링크로 대체(이미 각 폴더에 상세 실행법이 있어서 중복 작성 안 함)

### 다음
- [ ] 프론트엔드 Vercel 배포 (Root Directory=`frontend`, `VITE_API_BASE_URL` 환경변수, 배포 후
      Railway `FRONTEND_ORIGIN`에 Vercel 도메인 추가) — 사용자가 내일 진행 예정
- [ ] Vercel 배포 URL 나오면 실제 화면으로 재검증(이번엔 진짜 스크린샷 가능한지 다시 시도)
- [ ] README.md 프론트엔드 배포 링크 채우기
- [ ] `claude/violence-document-search-08aegl`, `claude/frontend-work-7qu7t9` 원격 브랜치 삭제
- [ ] README.md 남은 `[ ]` 항목(왜 만들었나/스크린샷/검색 품질 검증/한계와 다음 계획/비용)은
      사용자의 개인 경험·실측치가 필요해서 이 세션이 임의로 채우지 않음 — 직접 채우거나 내용
      불러주면 정리해서 반영 가능

## 🎨 검색/상세페이지 UI 대규모 개선 (2026-08-10, 실제 백엔드 연동 후 이어서)

**Railway 백엔드(CORS 수정 후)와 연결해서 프론트엔드 실데이터 검증 완료 → 이어서 사용자
피드백 받아가며 UI/UX 대규모 개선.** 아래 "3주차 프론트엔드" 항목(최초 구현)의 후속 작업.

### 실제 백엔드 연동 검증
- Railway backend를 `claude/frontend-work-7qu7t9` 브랜치 기준으로 재배포(CORS 미들웨어
  포함된 버전) → 실제 67,751건 데이터로 검색/상세페이지 전부 확인. 단어검색/문장검색/
  의미기반검색("상사가 부하직원 괴롭힘" → "직장 내 괴롭힘" 사례 매칭 등) 다 정상 동작
- 배포 중 겪은 문제: 처음엔 main 브랜치로 배포해서 CORS 코드가 없어 막힘 → 브랜치를
  `claude/frontend-work-7qu7t9`로 바꿔 재배포하니 해결
- 하드코딩된 문서 총 건수(72,913→67,751) 표기 오류 발견 — 수정하려던 순간 다른 세션에서
  이미 동일 수정을 push해둔 걸 발견, 그쪽 커밋으로 병합

### UI/UX 개선 (사용자 피드백 반영, 여러 라운드)
1. **헤더 상시 검색창** — 검색 상태를 `App.jsx` 레벨(`useSearchState` 훅)로 끌어올려서
   헤더 검색창과 히어로 검색창이 상태를 공유. 랜딩화면(검색 전)엔 히어로만, 결과화면/
   상세페이지엔 헤더 검색창만 보이도록 상호배타적으로 노출(`HeaderSearch.jsx` 신규)
2. **검색결과 2열 그리드 + 페이지네이션** — 백엔드 `/search` limit 10→20으로 확대, 프론트는
   2열×5줄(10건)씩 페이지네이션(URL `&page=` 동기화, 페이지 전환 시 결과 상단으로 스크롤)
3. **카드 크기 확대** — 세로(패딩 18→26px)·가로(컨테이너 최대폭 860→1120px) 둘 다 키워서
   페이지네이션 하단에 남던 큰 공백을 최소화
4. **상세페이지 원문 우선 표시** — 예전엔 요약 자동생성을 기다린 후 원문은 토글을 눌러야
   보였는데, 지금은 원문이 즉시 표시되고 요약은 "4줄 요약보기" 버튼을 눌러야 생성됨(요약
   생성/조회 백엔드 API 분리가 필요했음, 아래 참고). 상세페이지 컨테이너 폭은 820px로
   검색결과(1120px)와 별도 유지(읽기 편하게)
5. **관련 사례 섹션 제거** — 상세페이지의 "같이 검색된 관련 사례" 카드 목록을 삭제(관련
   사례 재검색 로직도 같이 제거), "← 검색 결과 전체 보기" 링크만 남김
6. **자유형 4줄 요약 추가** — 기존 구조화 요약(지적사항/원인/조치/결과 박스)은 그대로 두고,
   그 아래에 완전히 새로운 프롬프트로 항목 틀 없이 자유롭게 뽑은 4줄 요약을 문단 형태로
   추가 표시. "요약보기" 버튼 한 번에 구조화+자유형 둘 다 생성(`asyncio.gather`로 동시
   호출해서 지연시간을 순차 호출 대비 절반으로 줄임)
7. **랜딩화면 수직 중앙 정렬** — data.go.kr 스타일 참고해서 히어로(검색창)를 뷰포트 세로
   중앙에 배치(`min-height: calc(100svh - 90px)` + flex 중앙정렬). 하단 통계/그래프 추가는
   이번엔 보류하기로 함(사용자 결정)

### 백엔드 변경 (요약 기능 리팩터링)
- `GET /documents/{id}` — 더 이상 요약을 자동생성하지 않고 캐싱된 값만 반환(원문을 지연
  없이 바로 보여주기 위함)
- `POST /documents/{id}/summary` (신규) — "요약보기" 버튼 클릭 시에만 호출됨. 구조화 요약과
  자유형 요약 중 아직 캐싱 안 된 것만 `asyncio.gather`로 동시 생성, 한 응답으로 같이 반환
- `documents` 테이블에 `summary_freeform`(TEXT), `summary_freeform_failed`(BOOLEAN) 컬럼
  추가 — **라이브 DB에도 반영 완료**(사용자가 Colab에서 `ALTER TABLE` 직접 실행)
- CORS `allow_methods`에 `POST` 추가(기존엔 `GET`만 허용돼 있었음)
- `/search` limit 10 → 20

### 검증
- `npm run build`/`npm run lint` 매 변경 단계마다 통과 확인
- Playwright + 로컬 mock 백엔드로 전체 플로우(헤더 검색창 노출/숨김, 그리드+페이지네이션,
  상세페이지 원문/요약 분리, 자유형 요약, 관련사례 제거, 랜딩 중앙정렬)를 스크린샷으로
  하나씩 확인
- **이번 UI 변경분 자체는 아직 실제 Railway 백엔드로 재검증 안 함**(mock 서버로만 확인) —
  다음 세션에서 실제 배포 후 최종 확인 필요

### 다음
- [ ] 실제 Railway 백엔드 재배포해서 이번 UI 변경분(요약 API 분리, 자유형 요약 등) 최종 재검증
- [ ] 4주차 배포 시 `VITE_API_BASE_URL`(프론트), `FRONTEND_ORIGIN`(백엔드) 환경변수 설정
- [ ] README.md 스크린샷 자리에 실제 화면 캡처 채워넣기
- [ ] (보류) 메인화면 하단 통계/그래프 — 백엔드 집계 엔드포인트 설계부터 필요, 나중에 논의

---

## 🎨 3주차 프론트엔드(검색+상세페이지) 구현 (2026-08-10, 조기 착수)

**development-plan.md 3주차 목표(원래 일정 8/18~8/24)를 미리 착수해서 완료.** React(Vite)로
검색페이지+상세페이지를 새로 구현하고, data.go.kr(공공데이터포털)이 실제로 쓰는 KRDS(정부디자인
시스템) 컬러 토큰을 참고해 "관공서/금융권" 톤으로 디자인함. PR #5.

### 구현 범위
- **검색페이지** — 히어로 배너, 검색창(아이콘 + placeholder), 예시/최근 검색어 칩, 데이터 규모
  통계 문구, 결과 카드(기관·연도·신뢰도 배지 + 순위 배지 + 검색어 하이라이트), 스켈레톤 로딩,
  빈 결과 상태, 카드 페이드인 애니메이션
- **상세페이지** — 4줄 요약(AI 생성 안내 배지 + 복사 버튼 + 번호 매긴 2x2 그리드), 원문
  펼쳐보기 토글, "같이 검색된 관련 사례" 섹션, 스크롤 위로가기 버튼
- **전역** — URL 쿼리(`?q=`) 동기화로 검색 링크 공유/새로고침 대응(상세페이지도 쿼리를 이어받아
  새로고침해도 관련 사례를 다시 채움), 다크모드 명시적 토글(localStorage 저장), sticky
  헤더(스크롤 시 축소), `/` 키보드 단축키로 검색창 포커스, 최근 검색어(localStorage)
- **백엔드** — `CORSMiddleware` 추가(`backend/app/main.py`): 로컬 Vite 포트(5173) 기본 허용 +
  `FRONTEND_ORIGIN` 환경변수로 배포된 프론트 origin 추가 가능

### 디자인 결정 과정
처음엔 미니멀 유틸리티 스타일로 시작했는데 "프론트는 디자인이 중요한데 너무 단조롭다"는
피드백을 받음 → data.go.kr을 레퍼런스로 요청받아 확인해보니 해당 사이트가 KRDS(대한민국
정부디자인시스템)를 쓰고 있었고, 실제 primary 컬러가 `#256ef4`라는 걸 그 사이트 CSS 소스에서
직접 확인해서 그대로 채택(사이트 자체는 봇 차단으로 스크린샷은 못 찍었지만 CSS/HTML은 받아짐).
디자인 방향이 여러 차례 바뀔 수 있어서, 매번 정적 HTML 목업으로 스크린샷 먼저 보여주고 승인받은
다음에만 실제 코드에 반영하는 방식으로 진행 — 코드를 먼저 갈아엎고 되돌리는 시행착오를 피함.

### 검증
- `npm run build`, `npm run lint`(oxlint) 통과
- 로컬 mock API 서버 + Playwright로 실제 브라우저에서 검색 → 로딩 스켈레톤 → 결과 → 상세 →
  관련 사례 → 다크모드 → 복사 버튼 → 스크롤까지 전체 플로우 직접 확인
- **실제 백엔드(Railway DB)와는 미연동 확인** — 이 세션에 `DATABASE_URL`/`OPENAI_API_KEY`가
  없어서 진짜 72,913건 데이터로는 테스트 못 함. 데이터 품질 2차 수정(위 항목 참고)이 아직 진행
  중이라 지금 연결해도 의미가 크지 않다고 판단해서 보류하기로 함.

### 다음 (→ 전부 위 "검색/상세페이지 UI 대규모 개선" 항목에서 이어서 진행됨)
- [x] 데이터 품질 2차 수정 + DB 반영 끝남 → 실제 백엔드와 연결해서 재검증 완료
- [ ] 4주차 배포 시 `VITE_API_BASE_URL`(프론트), `FRONTEND_ORIGIN`(백엔드) 환경변수 설정
- [ ] README.md 스크린샷 자리에 실제 화면 캡처 채워넣기

---

## ✅ 데이터 품질 사고 대응 완료 (2026-08-07 발견 → 2026-08-10 마무리)

**검색 결과 미리보기에서 텍스트 오염 발견 → 원인 진단 → 수정 → 라이브 DB 반영 → 실제
검색 결과로 재확인 → 잔여 이슈 2개 추가 발견 → 2차 수정 코드 작성 → 2차 수정 DB 반영
+ 재임베딩 + HNSW 인덱스 재생성 → 재추출 불가 문서 건수 급증(1,765 예상 vs 5,162 실제)
원인 조사(버그 아니라 진짜 노이즈였음 확인) → 삭제 실행(documents 5,162건 + chunks
7,054건 제거) → **Railway에 backend 재배포해서 라이브 검색 최종 확인까지 완료.**
`22002244년도` → `2024년도`, "표" placeholder 잡음, 글자 중복 등 모든 증상 해결 확인됨.**

### ⏭️ 다음 세션은 여기부터
이번 사고 대응은 끝났음. 3주차 프론트엔드(위 "🎨 3주차 프론트엔드" 항목 참고)는 이미 조기
착수해서 구현까지 끝난 상태 — 남은 건 실제 백엔드(Railway, 재배포 필요)와 연결한 재검증뿐.
지금 백엔드는 Railway에 배포된 상태일 수 있음 — 계속 켜둘 필요 없으면 비용 관리를 위해
서비스 삭제할 것(코드는 GitHub에 그대로 있어 언제든 재배포 가능).

### Railway 재배포 중 겪은 문제들 (해결됨, 다음에 또 배포할 때 참고)
- **Root Directory 미설정** — 모노레포라 `Settings → Root Directory`를 `backend`로 안 잡으면
  `Procfile`/`requirements.txt`를 못 찾음
- **`DATABASE_URL` DSN 파싱 에러** (`scheme is expected to be either "postgresql" or "postgres", got ''`)
  — `${{Postgres.DATABASE_URL}}` 참조 문법이 서비스 이름 불일치 등으로 안 먹었음. 해결:
  Postgres 서비스 Variables/Connect 탭에서 완성된 연결 문자열을 직접 복사해서 하드코딩
- **`unknown type: public.vector`** — 위 에러를 고친 뒤에도 발생, 원인은 **엉뚱한(비어있는)
  DB에 연결**되고 있었던 것 (프로젝트에 Postgres 서비스가 여러 개 있었거나 참조가 다른
  DB를 가리킴). 해결: 이미 검증된 값(Colab 시크릿에 저장해둔 `DATABASE_URL`)을 그대로
  복사해서 사용 — 확실한 값이 있으면 그걸 기준으로 맞추는 게 제일 빠름
- **PORT 불일치로 "Application failed to respond"/도메인 라우팅 에러** — Railway가 PORT를
  자동으로 못 잡아준 이력이 있어(2주차 때도 겪음) `PORT` 변수를 `8080`으로 직접 설정 +
  도메인 생성 시 타겟 포트도 `8080`으로 맞춰야 함
- **"Not Found: The train has not arrived at the station"** — 도메인이 서비스에 아직
  라우팅이 안 된 상태(앱 크래시와는 다른, Railway 엣지 쪽 문제). 재배포 직후엔 1~2분 정도
  기다리거나 도메인을 삭제 후 재생성하면 해결됨

### 재추출 불가 문서 급증(1,765 → 5,162) 원인 조사 결론 (2026-08-10)
처음엔 "2차 수정의 글자/숫자 중복 collapse가 부풀려진 분량을 걷어내서 새로 걸렸다"는
가설을 세웠으나, `documents_final.jsonl`(수정 전 원본)과 직접 비교해보니 그 가설은 거의
틀렸음:
- 5,046건(`table_placeholder_only`) 중 4,969건은 **수정 전 원본부터 이미** 300자 미만
  — 2026-08-07 원래 진단(1,674건)이 raw 길이만 보고 과소 추정했던 것뿐, 이번 수정과 무관
- 나머지 77건은 원본이 겉보기엔 1만~1.7만 자였지만, 실제로 열어보니 **90% 이상이 HWP
  내부 XML 태그/속성값**(`hp:run hp:linesegarray ... textpos="0" vertpos="1000" ...`)이고
  진짜 한글 내용은 200~300자 안팎뿐이었음 — `strip_hwpml_leak`이 태그를 걷어내고 실제
  분량을 정확히 드러낸 것이지, 버그로 내용을 지운 게 아님. `TAG_ATTR_RE`/`TAG_NAME_RE`
  매칭 비율을 실측(90.7%)해서 확인.
- **교훈**: raw 텍스트 길이만으로 "내용이 충분한지" 판단하면 안 됨 — 태그 노이즈가 낀
  문서는 길이가 부풀려져서 과소 진단으로 이어짐. 앞으로 비슷한 판정을 할 땐 노이즈 제거
  후 길이를 기준으로 삼을 것.

### DB 반영 중 겪은 사고 2건 (해결됨, 다음에 또 나올 수 있으니 기록)
- **디스크 부족(`DiskFull: could not extend file`)** — 2차 수정 반영 + 재임베딩 UPDATE를
  거듭하면서 dead tuple이 쌓여 Railway 볼륨이 꽉 참. `chunks.embedding`에 걸린 HNSW
  인덱스가 UPDATE마다 그래프를 다시 써야 해서 특히 크게 기여한 것으로 추정(실제로 인덱스를
  `DROP INDEX`하니 바로 이어서 진행됨). 조치: ①`chunks_embedding_hnsw_idx` 임시 삭제
  ②`documents_backup_20260807`/`chunks_backup_20260807` 백업 테이블 삭제(1차 수정이 이미
  검증됐다고 판단해서 지금 지움 — **더 이상 롤백 불가**) ③`VACUUM` ④UPDATE 이어서 진행
  ⑤끝나고 HNSW 인덱스 재생성. `schema.sql` 원래 주석에도 있던 "인덱스 걸어두고 대량
  쓰기하면 디스크 문제" 교훈이 INSERT뿐 아니라 UPDATE에도 그대로 적용된 사례.
- **공유메모리 부족(`could not resize shared memory segment`)** — 인덱스 재생성 시
  16차 때와 동일한 문제 재발(병렬 빌드가 컨테이너의 작은 `/dev/shm` 한도를 넘음). 조치:
  `SET max_parallel_maintenance_workers = 0` 후 재시도 → 해결(직렬이라 더 오래 걸림).

### 발견 경위
검색 API가 실데이터로 잘 동작하는 걸 확인한 뒤(2주차), 실제 검색 결과 미리보기를 보다가
"제제목목목목", "관관관관계계계계" 처럼 글자가 반복되거나 `hp:run hp:linesegarray ...`
같은 HWP 내부 XML 마크업이 그대로 노출되는 문서를 발견. `raw_text`를 만든 파싱 단계
(Colab 노트북, 이 레포 밖 — audit_cases_final_v2.jsonl 생성 단계)에서 생긴 문제로,
이미 Postgres에 적재까지 끝난 상태였음.

### 발견된 문제 유형 (전체 72,913건 기준)
1. **글자 중복(bold_dup)** — 1,516건. 원본에서 볼드체가 획을 겹쳐 그리는 방식으로
   렌더링된 경우, 추출기가 겹친 획을 각각 별개 글자로 읽음. 정규식으로 완전 자동 수정
   가능함을 검증(수정 후 같은 패턴 재탐지 0건).
2. **HWP XML 누출(hwp_leak)** — 표(테이블) 파싱 실패 시 HWPX 내부 마크업이 텍스트로
   그대로 남음. `hp:t` 뒤에 표 셀 내용이 남아있어 태그만 벗겨내면 대부분 복원 가능
   (표의 행/열 구조는 잃지만 검색엔 지장 없음).
3. **표 placeholder만 남음** — 27,128건 중 1,674건은 표 내용이 통째로 "표"라는
   단어 하나로 치환되고 그걸 빼면 내용이 사실상 없음(전체 300자 미만). 텍스트 치환으로
   복구 불가 — 재추출 필요.
4. **짧은 문서(non-fallback)** 101건, **인코딩 깨짐**(치환문자 섞임) 12건 — 역시
   재추출 필요.
5. (표 서식 붕괴, 완전 중복 등 추가로 의심했던 패턴은 실제로는 대부분 오탐 —
   테이블 점선 채움, `AAA`/`○○○`류 익명화 마스킹으로 확인되어 폐기)

→ 1+2(총 1,516건 문서 / 4,770건 청크)는 **자동 수정 로직 확정** →
`scripts/fix_text_corruption.py`. 3+4(총 1,765건, 2.4%)는 정규식으로 못 고치는
영역이라 **재추출 대상 목록으로 별도 분리**, 이번 수정 범위 밖.

### 1차 반영 후, 실제 "폭력" 검색 결과로 재확인하다 추가로 발견한 것
- **숫자 중복** — 1)번과 같은 원인인데 숫자는 처음부터 collapse 대상에서 뺐었음
  (생년월일/주민번호 익명화 마스킹 "0000년", "000000"까지 지워버리는 오탐 때문).
  실제로 인천교통공사 사례에서 연도가 `22002244년도`로 안 고쳐진 채 남아있는 걸
  발견 → 마스킹은 "같은 숫자만" 반복되고 버그는 "서로 다른 숫자가 섞여서" 반복된다는
  차이로 구분해서 `fix_duplicated_digits` 추가.
- **표 placeholder 잡음** — 표 내용이 사라진 자리에 남는 "표"라는 단어는 검색
  화면에서 표를 렌더링할 방법이 없으니 어차피 정보가 없음 → `strip_table_placeholder`로
  해당 줄만 제거(내용을 복원해주는 건 아니고 잡음만 치움).

### 처리 과정에서 잡은 사고 (반영 전에 잡아서 다행이었던 것들)
- HWP 태그 제거 로직 초안이 `\s+`로 전체 공백을 뭉갬 -> **관련 없는 문서 72,511건까지
  줄바꿈이 다 눌리는 사고**로 이어질 뻔함. `hp:` 태그가 아예 없는 문서는 건드리지 않는
  가드를 추가하고, 반영 전 로컬 CSV에서 "수정 후 바뀐 문서 수가 기대치(~1,500)와
  맞는지" 재검증하는 절차를 거쳐서 잡아냄.
- 글자 중복 collapse가 중복 배수가 애매하게 안 떨어지는 경우(예: 13배) 한 번에 안
  끝나고 2배로 남는 걸 발견 -> **더 이상 안 바뀔 때까지 반복 적용**(`max_passes`)으로 해결.
- Railway 유료 사용량 문제로, 반복적인 진단 쿼리를 DB에 직접 돌리는 대신 **`documents`/
  `chunks`를 CSV로 한 번에 내려받아 로컬(pandas)에서 반복 검증**하는 방식으로 전환.

### 진행 상황
- [x] 1차 수정(글자중복+HWP누출): `documents.raw_text`(1,516건)/`chunks.text`(4,770건)
      반영 + 라이브 재검증(0/0)
- [x] 1차 수정: 바뀐 chunk 4,770건 BGE-m3 재임베딩(Colab) 후 `chunks.embedding` UPDATE 완료
- [x] "폭력" 검색 실제 호출로 1차 수정 확인 — 원래 깨졌던 두 사례(한국보훈복지의료공단
      "성성성성희희희희롱롱롱롱"→"성희롱", 인천교통공사 "민민간간위위탁탁업업무무"→
      "민간위탁업무") 정상 출력 확인. 이 과정에서 잔여 이슈 2개(숫자 중복, 표
      placeholder 잡음) 추가 발견 → 위 "1차 반영 후 추가로 발견한 것" 참고
- [x] 2차 수정 로직(`fix_duplicated_digits`, `strip_table_placeholder`) 작성 + 커밋(`2d49b1b`)
- [x] 2차 수정으로 바뀐 chunk 재임베딩+반영 스크립트(`scripts/reembed_changed_chunks.py`)
      작성 + 커밋(`5df6f67`)
- [x] **재추출 필요 문서 처리 방침 결정 (2026-08-10)** — 재추출 안 하고 DB에서 아예
      제외(삭제)하기로 확정. 이유: 재추출은 원본 재파싱까지 필요해 비용이 크고, 애초
      예상 규모(2.4%)라면 검색 커버리지에 미치는 영향이 작다고 판단. 삭제 대상 판정
      기준(3가지)과 실행 스크립트: `scripts/remove_unrecoverable_docs.py` — 안전을
      위해 조회(`find_unrecoverable`, 삭제 없음)와 삭제(`delete_unrecoverable`)를
      분리, manifest 파일로 건수 확인 후에만 삭제하도록 만듦
- [x] **2차 수정 DB 반영 완료** (documents/chunks 텍스트 업데이트, 디스크 부족 2번
      겪었지만 해결 — 아래 "DB 반영 중 겪은 사고 2건" 참고)
- [x] 2차 수정으로 바뀐 chunk 16,855건 재임베딩(Colab GPU) 후 `chunks.embedding`
      UPDATE 완료
- [x] `chunks_embedding_hnsw_idx` 재생성 완료 (반영 과정에서 임시로 삭제했었음)
- [x] **재추출 불가 문서 건수 급증 원인 조사 완료** — `find_unrecoverable()` 결과 예상
      (1,765건)의 3배인 5,162건이 나온 원인을 `documents_final.jsonl`(DB 적재 전 원본)과
      직접 비교해서 확인. 결론: 버그 아님 — 대부분(4,969건)은 원래 진단이 raw 길이만 보고
      과소 추정했던 것, 나머지 77건은 원본이 90% 이상 HWP XML 태그 노이즈였던 게 걷히면서
      드러난 것. 자세한 내용은 위 "재추출 불가 문서 급증 원인 조사 결론" 참고
- [x] **재추출 불가 문서 최종 삭제 실행 완료 (2026-08-10)** — `delete_unrecoverable()`로
      documents 5,162건 + chunks 7,054건 제거. 총 문서 수 72,913 → 약 67,751건
- [ ] "폭력" 검색으로 2차 수정까지 최종 확인 (Railway 재배포 vs 로컬 확인 방법 미정)
- [ ] README.md 등 문서의 총 건수 표기(72,913건) 갱신
- [ ] 삭제 실행 후 총 문서 수 표기(README.md 등, 현재 72,913건으로 적혀있음) 갱신
- [x] ~~참고: 반영 전 `documents_backup_20260807`, `chunks_backup_20260807` 테이블로
      백업해둠~~ → 2026-08-10 디스크 부족 해결 과정에서 삭제함, 더 이상 존재하지 않음
- 참고: 반영 전 `documents_backup_20260807`, `chunks_backup_20260807` 테이블로 백업해둠
  (안전 확인되면 삭제 예정 — 아직 지우지 말 것)

---

## 🎉 2주차 목표 달성 (2026-08-07, 1주차 이어서 같은 날)

**1주차(DB) 완료에 이어, 2주차(검색 API)까지 같은 날 완료.** development-plan.md 2주차 목표
("Postman이나 브라우저 주소창으로 검색 API를 호출하면 결과가 나온다")를 실제 배포된 서버 +
실제 72,913건 데이터로 달성.

### 1주차 (완료)
1. [x] Railway Hobby 업그레이드 + 새 Postgres 서비스 생성
2. [x] 데이터 적재 완료 — documents + chunks(벡터 포함) 전체
3. [x] 인덱스 생성 완료 — HNSW/GIN/btree 3개
4. [x] 1주차 체크포인트 통과 — 벡터/키워드 검색 SQL로 직접 검증

### 2주차 (완료) — `backend/` 폴더
1. [x] FastAPI 프로젝트 세팅 — `db.py`(asyncpg 비동기 연결), `repository.py`(RRF+dedup SQL),
   `embedding.py`(BGE-m3 쿼리 인코딩), `summary.py`(온디맨드 4줄 요약), `main.py`(라우팅)
2. [x] 검색 API(`/search`) 구현 — RRF만 사용, 리랭커는 `repository.rerank()`에 no-op 스텁으로
   자리만 마련(스트레치 목표, architecture.md §3.4)
3. [x] 상세 API(`/documents/{id}`) 구현 — 온디맨드 요약 생성 + DB 캐싱(§4.5), 실패 시
   `summary_failed` 캐싱으로 재시도 비용 방지(§4.6)
4. [x] 로컬 서버 기동 + 3개 엔드포인트(`/health`, `/search`, `/documents/{id}`) 합성 데이터로
   실동작 검증 (요약 API 실패 시 우아하게 처리되는 것까지 확인)
5. [x] Railway에 백엔드 배포(PORT 변수 수동 설정 필요했음) → **실제 72,913건 데이터로
   Swagger UI에서 `/health`, `/search` 성공 확인** → 확인 후 비용 관리를 위해 서비스 삭제
   (코드는 GitHub에 그대로 있어 재배포 언제든 가능)

### 참고 — 개발 중 정한 것들
- DB 드라이버: psycopg2(동기) → **asyncpg(비동기)**로 전환 — FastAPI의 비동기 장점을 실제로
  살리기 위함(CPU 연산인 임베딩만 `asyncio.to_thread`로 스레드에 위임)
- `requirements.txt`: 버전 전부 실제 설치+import 테스트를 통과한 값으로 고정, torch는
  CPU 전용 빌드(Railway는 GPU 없음, 안 쓰는 CUDA 라이브러리 제외해서 배포 용량 절감)
- Railway 비용 관리: Usage → Set Limits에서 Compute Hard limit($10)/Soft limit($7) 설정

### 다음 — 3주차
- 프론트엔드(검색페이지 + 상세페이지) 구현 (development-plan.md 3주차)

---

## 2026-08-07 (16차 — 인덱스 생성 시도, 참고용 용어 정리)

### 진행 요약 (포괄적으로만)
- Railway Data 탭(웹 대시보드)에서 인덱스 생성 SQL 실행 시 두 가지 문제 발생: ①공유메모리 부족 에러
  (병렬 빌드가 컨테이너의 작은 `/dev/shm` 한도를 넘어서 발생) ②대시보드 자체의 실행시간 제한으로
  인한 timeout
- 대응: 병렬 빌드를 끄는 옵션(`max_parallel_maintenance_workers = 0`) 적용 + 웹 대시보드 대신
  Colab에서 직접 연결해 실행하는 방식으로 전환 (오래 걸리는 작업은 웹 UI보다 스크립트 연결이 안전)

### 참고 — 인덱스 3종 요약
- **HNSW**: 벡터(의미) 유사도 검색용 — "비슷한 뜻" 찾기를 빠르게 함
- **GIN**: 키워드(텍스트) 검색용 — "정확한 단어 포함" 찾기를 빠르게 함
- **btree**: 일반 조인/조회용 — 청크→문서 연결을 빠르게 함
- 셋 다 없어도 검색 자체는 되지만, 96,355건 규모에서는 없으면 눈에 띄게 느려짐

---

## 2026-08-06 (9차 — preflight_check.py가 실전에서 진짜 버그 잡아냄)

**실제 `documents_final.jsonl`(72,913건)로 `preflight_check.py`를 처음 돌려봄 — 바로 문제 2건 발견.**

### 진짜 버그: `build_final_dataset.py`의 parse_tier 정규화 누락
- 증상: `parsing_quality`가 `standard`/`partial`/`fallback`이 아니라 `fallback_split1of2`,
  `standard_split1of3` 같은 값으로 88건 남아있었음 (CHECK 제약 위반 → 적재 시 멈췄을 것)
- 원인: 같은 (source_file, case_number) 그룹 안에 **같은 등급인데 split된 레코드(idx 있음)와
  안 된 레코드(idx=None)가 섞여 있는 경우**, `else` 분기를 타면서 `chosen[0]`이 하필 split
  레코드면 원본 `parse_tier`(접미사 안 지워진 값)가 그대로 저장됨. `if` 분기(순수 split
  병합)만 `best_base`로 정규화하고 있었고 `else` 분기는 빠져있었음
- 재현: 합성 데이터로 정확히 이 상황(같은 그룹에 `fallback`과 `fallback_split1of2` 혼재)을
  만들어서 버그 확인 → 수정(`else` 분기도 `best_base`로 정규화) → 같은 재현 케이스로 재검증,
  정상 값(`fallback`)으로 나오는 것 확인
- **사용자가 해야 할 일**: Colab에서 `build_final_dataset.py` 최신 버전으로 재실행해서
  `documents_final.jsonl`/`chunks_final.jsonl` 다시 생성 필요

### 버그 아님으로 판명: `year` 필드가 문자열
- `year`가 72,911/72,913건에서 문자열(`"2019"` 등)이었지만, 실측 확인 결과 **Postgres/psycopg2가
  숫자 문자열은 자동으로 int로 캐스팅**해서 문제없음 (로컬 DB에 직접 넣어서 확인)
- 그래도 방어 코드는 추가: `load_to_postgres.py`에 `_parse_year()` 헬퍼 — 혹시 진짜 파싱 안 되는
  값(빈 문자열, "2020년" 등)이 하나라도 있으면 그 배치 전체가 죽는 대신 해당 값만 NULL로 대체하고
  경고 출력
- `preflight_check.py`도 "정수 타입 아니면 전부 경고"(72,911건 떠서 노이즈만 컸음)에서
  "진짜 파싱 안 되는 것만 경고"로 개선

### 다음에 할 일
1. Colab에서 `build_final_dataset.py` 재실행 (parse_tier 버그 수정된 최신 버전)
2. `preflight_check.py` 재실행해서 "치명적 문제 없음" 확인
3. `load_to_postgres.py` 실행

## 2026-08-06 (8차 — DB 적재 전 마지막 보강: 배치 커밋 + 사전 점검 스크립트)

DB 적재(`load_to_postgres.py`) 실행 직전에, float16 벡터 저장 검증 + 코드 보강 2건 진행.
(중간에 사용자 지적으로 미승인 변경을 한 번 되돌렸다가, 승인받고 다시 반영함 — 앞으로도
실제 파일 수정 전에는 먼저 여쭤보고 진행하는 것으로 함)

### 확인만 하고 코드는 안 건드린 것
- **float16 벡터가 pgvector에 안전하게 저장되는지 실측 검증** — 로컬 Postgres에 실제로
  float16 numpy 배열을 삽입 후 다시 읽어서 원본과 코사인 유사도 비교 (거의 1.0, 손실 없음
  확인). `load_to_postgres.py`는 수정 불필요.

### 코드 수정 1 — `load_to_postgres.py`: 배치 커밋
- 기존: `documents`(72,913건), `chunks`(96,355건, 벡터 포함이라 더 무거움) 각각 통째로
  하나의 트랜잭션 — Railway Public Network 연결이 도중에 끊기면 그 지점까지 넣은 것 전부
  롤백됨
- 변경: 5,000건 단위로 나눠서 커밋 + 진행 로그 출력. 끊겨도 이미 커밋된 배치는 남고,
  재실행하면 `ON CONFLICT DO NOTHING`으로 이어서 진행됨
- 로컬 Postgres로 정상 동작 확인 + 재실행 시 중복 안 생기는 것(idempotency)도 확인

### 코드 수정 2 — `scripts/preflight_check.py` (신규)
- DB 연결 없이 로컬 파일만으로 사전 점검: document_id FK 무결성, `parsing_quality` CHECK
  제약 위반 여부, `year` 타입, id 중복, 임베딩 누락 청크 수
- 일부러 문제 3개(잘못된 parsing_quality, year가 문자열, FK 깨진 청크) 주입한 가짜 데이터로
  테스트 → 전부 정확히 잡아내는 것 확인. 정상 데이터로는 통과하는 것도 확인
- **다음에 실제로 할 일**: Colab에서 `python preflight_check.py` 먼저 돌려서 "치명적 문제
  없음" 나오면 그다음에 `load_to_postgres.py` 실행

## 2026-08-06 (7차 — "4줄 전부 미기재" 처리 확정 + 카드 미리보기 모순 발견/수정)

### "4줄 전부 미기재" 최종 확정 (architecture.md §4.6, v11)
- 실측: 35건 중 1건(2.9%, `partial` 등급) → 72,913건 기준 95% 신뢰구간으로 최소 약 53건 ~ 최대 약 10,877건 예상. 0건은 아닐 게 거의 확실해서 안전장치 필요하다고 최종 판단
- **처리**: 1차 생성 실패(4줄 전부 탈출구 문구) → 재시도 1회 → 그래도 실패하면 `summary_failed=TRUE` 저장하고 원문만 반환
- **UI**: 새 화면 상태 안 만들고 기존 신뢰도 배지 체계에 "요약 어려움 — 원문 참고 필요" 티어만 추가
- **실패 캐싱**: `scripts/schema.sql`에 `documents.summary_failed BOOLEAN NOT NULL DEFAULT FALSE` 컬럼 추가 — 이거 없으면 진짜 요약 불가능한 문서는 조회될 때마다 매번 API 2번(1차+재시도)씩 낭비됨. 로컬 Postgres에서 재적용 테스트 완료 (컬럼 정상 생성 확인)
- **미룬 것**: 원문 인용 폴백(quote extraction) — 엣지케이스(3% 미만) 대비 API 호출 추가 비용이 안 맞아 스트레치로 이연

### 부수적으로 발견한 진짜 버그: 검색 카드 미리보기가 v9 이후로 깨져 있었음 (architecture.md §8.3, v12)
- §4.6 반영하려고 §8.3(검색 카드 설계)을 다시 보다가 발견: 카드 미리보기가 "4줄 요약 1번째 줄"에 의존하고 있었는데, **v9에서 요약을 배치→온디맨드로 바꾼 뒤로는 상세페이지 클릭 전까지 요약 자체가 존재하지 않음** — 그대로 뒀으면 검색 결과 카드 대부분이 빈 미리보기로 떴을 상황
- 카드에서도 온디맨드 생성을 걸면 검색 1회당 최대 10번 LLM 호출 → NFR2(1~2초) 예산 초과 위험
- **수정**: 카드 미리보기는 LLM 없는 `raw_text` 발췌로 되돌림, AI 4줄 요약은 상세페이지(단건 클릭) 전용으로 명확히 분리
- 교훈: 설계를 한 부분 바꾸면 그 설계에 의존하던 다른 부분도 같이 점검해야 함 — v9 커밋 때 §8.3까지 같이 안 봤던 게 원인

## 2026-08-06 (6차 — 임베딩 품질 검증, 벡터DB 선택 근거 문서화, 파이프라인 숫자 정리)

### 임베딩 품질 검증 완료 (Colab에서 실측)
```
shape: (124066, 1024)
dtype: float16
NaN 있는지: False
전부 0인 벡터 수: 0
벡터 크기(norm) 평균/표준편차: 0.9999997 / 0.00017351884
```
- **norm이 거의 정확히 1.0** → 벡터가 L2 정규화되어 있음이 실측으로 확인됨. 이건 5차에서 걱정했던
  "배치 임베딩(`FlagEmbedding`)과 온라인 쿼리 예시 코드(`sentence-transformers`)가 라이브러리가 달라
  정규화 방식이 어긋날 수 있다"는 우려를 상당 부분 해소함 — `FlagEmbedding`도 기본적으로 정규화된
  벡터를 내놓는다는 게 확인됨 (다만 두 라이브러리가 완전히 동일한 벡터공간을 내놓는지는 검색 API
  실제 구현 때 한 번 더 확인 권장)
- **의미 유사도 테스트도 통과**: 임의 청크 하나를 기준으로 코사인 유사도 상위 5개를 뽑아보니, 자기 자신(1.000) 다음으로
  다른 기관의 비슷한 양식(감사결과처분요구서) 문서들이 0.86~0.90대 유사도로 잡힘 — 임베딩이 의미적으로
  잘 작동하고 있다는 증거

### 벡터DB 선택 근거 보강: LangChain + FAISS 검토 후 기각
`docs/architecture.md` §5.6 ADR 표에 아래 행 추가 (근거는 §5.7과 동일 맥락이지만, "FAISS를 구체적으로
검토했었다"는 사실 자체가 문서에 없어서 명시적으로 추가함 — 포트폴리오 인터뷰에서 "왜 FAISS 안 썼냐"
질문 대비):
- FAISS는 순수 벡터 검색 속도는 더 빠르지만(인메모리 C++), 이 프로젝트 규모(청크 96,355개)에서는
  pgvector HNSW로도 충분히 빠르고, 진짜 지연시간 병목은 벡터 검색이 아니라 리랭커(크로스인코더)임
- FAISS를 쓰면 메타데이터/키워드검색/요약캐싱용 Postgres가 어차피 또 필요해져서, "벡터DB 왕복 + RDB
  왕복" 2번이 되는 반면 pgvector는 DB 왕복 1번으로 끝남 — 이 프로젝트 규모에선 이 차이가 알고리즘
  자체 속도차보다 체감에 더 영향을 줌
- LangChain은 이 프로젝트의 커스텀 검색 로직(RRF 융합 + document 단위 dedup + 리랭커 재채점)이 일반적인
  Retriever/Chain 추상화에 깔끔히 안 맞아서 채택 안 함

### 파이프라인 숫자 계보 정리 (그동안 여러 번 나온 숫자들, 한 곳에 정리)
| 단계 | 파일 | 건수 | 단위 | 비고 |
|---|---|---|---|---|
| 0. 원본 수집 | (이 저장소 밖) | 약 80,000건 | API 레코드 | 사용자 기억 기준, 이 저장소로는 확인 불가 |
| 1. 파싱 | `audit_cases_final_v2.jsonl` | 143,160줄 | 파싱 레코드 | 중복/분할(`_splitNofM`)/overview 포함 |
| 2. 청킹 대상 | `embed_ready_v2.jsonl` | 76,454건 | 고유 문서 | 0→1, 1→2 사이 정확한 사유는 미확인 |
| 3. 청킹 결과 | `embed_ready_v2.jsonl` | 124,066개 | 청크 | **임베딩은 이 전체를 대상으로 함** |
| 4. 1차 정리 | `build_final_dataset.py` (audit_cases 쪽) | 78,697건 | 고유 문서 | overview 4,723건 제외, extraction_failed만 있는 234건 제외, split 병합 |
| 5. 최종 매칭 | `documents_final.jsonl` | **72,913건** | 최종 문서 | 4번과 2번을 (source_file, title)로 매칭한 교집합 |
| 5-1. 최종 청크 | `chunks_final.jsonl` | **96,355건** | 최종 청크 | 위 72,913건에 속한 청크만 |

## 2026-08-06 (5차 — 임베딩 완료 + 4줄 요약을 배치→온디맨드로 재설계)

### 🎉 임베딩 완료
`embed_chunks.py`로 124,066개 청크 전부 임베딩 완료 확인 (`체크포인트 저장: 124066/124066건 완료`,
개수 검증 assert 통과). `embeddings_v2/embeddings.npy` + `chunk_ids.jsonl` 준비 끝남.
**다음 단계: `load_to_postgres.py`로 실제 DB 적재** (Railway 스키마는 이미 적용되어 있음, 3차 항목 참고).

### 설계 변경: 4줄 요약을 "배치 사전생성" → "상세 API 온디맨드 생성 + DB 캐싱"

사용자가 "검색 결과 여러 개 중 실제로 클릭해서 보는 건 일부인데, 72,913건 전체를 미리
요약해두는 게 비효율 아니냐"는 질문 → 맞는 지적이라 설계를 바꿈.

- **이전 설계**: Colab에서 72,913건 전체를 배치로 미리 요약 → DB에 채워 넣기 (스모크 테스트 실측 기준 약 7.2시간, $15 고정 비용, 체크포인트/재개 인프라 별도 필요)
- **바뀐 설계**: 상세 API가 문서 조회 시 `summary_point`가 NULL이면 그 자리에서 OpenAI 호출 → 결과를 DB에 저장(캐싱) → 반환. 이미 채워져 있으면 API 호출 없이 DB 값만 반환
- **왜 나은가**: 실제로 조회된 문서에 대해서만 비용 발생, 7시간짜리 배치+체크포인트 인프라 자체가 통째로 불필요해짐, 반복 조회 시 요약 문구가 매번 바뀌는 것도 방지(캐싱이라 최초 1회만 생성)
- **바뀌지 않은 것**: 프롬프트 설계(1~3줄 탈출구 포함, §4.1~4.2)와 `scripts/summary_smoke_test.py`는 그대로 유효 — "이 프롬프트가 실제로 잘 작동하는지" 사전 검증 용도로 계속 씀. 배치 실행 자체만 안 하는 것

### 수정한 문서
- `docs/architecture.md` → v9: §3.7 다이어그램(4줄요약을 오프라인 배치에서 상세 API 경로로 이동), §4.5 신규(온디맨드+캐싱 동작 방식), §4.4/§5.5/§5.6/§5.7/§6 관련 서술 전부 갱신
- `docs/development-plan.md` → v3: 1주차 "4줄 요약 배치 실행" 항목 제외, 2주차 "상세 API 구현"에 온디맨드 생성+캐싱 포함하는 걸로 명시
- `README.md`: 기술스택 표, 주요 설계 결정 목록, 비용 섹션 갱신

### 스모크 테스트 실측 결과 (참고용으로 계속 유효)
- 35건 실제 API(gpt-4o-mini) 테스트: 35/35 성공, 포맷 위반 1건(원인 미확인 — 다음에 확인 필요)
- 건당 평균 입력 948토큰 / 출력 105토큰 / 1.77초 / 약 $0.0002
- standard 등급은 1~3줄 미기재 사용 0% (모델이 게으르게 답한 흔적 없음, 좋은 신호)

## 2026-08-06 (3차 — Railway DB 스키마 적용)

### 완료
- **Railway Postgres 서비스 생성** — Public Networking 활성화 (`DATABASE_PUBLIC_URL` 확보, Colab에서 쓸 때는 `DATABASE_URL` 환경변수명으로 매핑해서 사용)
- **`scripts/schema.sql`을 Railway에 실제 적용** — Railway 대시보드 Query 탭에서 실행, `documents`/`chunks` 테이블과 인덱스 5개 생성 확인 완료 (`information_schema.tables`, `pg_indexes`로 검증)

### DB 스키마 요약 (테이블은 만들었고, 데이터는 아직 안 들어간 상태)

**`documents`** — 사례 1건 = 행 1개, 상세페이지용
| 컬럼 | 용도 |
|---|---|
| `id` (PK) | 사례 고유번호 |
| `institution`, `year` | 기관명, 감사연도 |
| `raw_text` | 원문 전체 (상세페이지 "펼쳐보기") |
| `parsing_quality` | standard/partial/fallback — 신뢰도 배지용, CHECK 제약 있음 |
| `summary_point/cause/action/result` | 4줄 요약 (아직 비어있음, 상세 API에서 온디맨드 생성+캐싱으로 채워질 예정 — 5차 항목 참고) |

**`chunks`** — 문서를 쪼갠 검색 단위
| 컬럼 | 용도 |
|---|---|
| `id` (PK), `document_id` (FK → documents.id) | 청크 식별 + 소속 문서 |
| `text` | 청크 원문 |
| `embedding vector(1024)` | BGE-m3 임베딩 벡터 (현재 Colab에서 생성 중) |
| `tsv` | 키워드 검색용, `text` 저장 시 Postgres가 자동 생성(GENERATED ALWAYS) |

**인덱스 5개** (검색 속도용 — 없으면 검색할 때마다 테이블 전체를 훑어야 함)
- `chunks_embedding_hnsw_idx` (HNSW, `embedding`) — 벡터 유사도 검색
- `chunks_tsv_gin_idx` (GIN, `tsv`) — 키워드 검색
- `chunks_document_id_idx` (btree, `document_id`) — 청크→문서 조인
- pgvector 확장이 내부적으로 생성하는 인덱스 등

이 벡터 검색 + 키워드 검색 결과를 RRF로 합쳐서 최종 Top-10을 뽑는 게 검색 로직의 핵심 (architecture.md 참고).

### 다음
- 예상 건수: `documents` 72,913건, `chunks` 96,355건 (build_final_dataset.py 최신 실행 결과 기준)
- 임베딩(`embed_chunks.py`, 124,066개 중 진행 중) 완료 대기
- 완료되면 `load_to_postgres.py`로 실제 데이터 적재 → 1주차 체크포인트(SQL로 유사 사례 순위 확인)

## 2026-08-06

### 완료
- **데이터 정리** — 원본 `audit_cases_final_v2.jsonl`(143,160줄)에 중복/분할/비-사례 레코드가 섞여 있던 걸 발견하고 정리
  - 같은 사례가 여러 번 파싱된 것(순수 중복), 원문이 길어 여러 조각으로 쪼개진 것(split), 실제 지적사항이 아닌 "감사개요" 레코드(overview) 세 가지가 섞여 있었음
  - `scripts/build_final_dataset.py`로 재현 가능하게 정리 — 최종 **72,913건**의 documents/chunks 확정
- **DB 스키마 확정** — `scripts/schema.sql` (documents, chunks 테이블 + HNSW/GIN 인덱스)
- **DB 적재 스크립트 준비** — `scripts/load_to_postgres.py` (documents_final.jsonl + chunks_final.jsonl + 임베딩 파일 조인해서 적재)
- **요약 배치 스모크 테스트 스크립트 준비** — `scripts/summary_smoke_test.py` (DRY_RUN 모드 포함, documents_final.jsonl 기준으로 필드명 맞춤)
- **Drive 정리** — 중복/백업 파일 삭제로 ~5.6GB 확보
- Colab 임베딩 스크립트 개선안 준비 (chunk_ids.jsonl을 체크포인트마다 저장하도록 — 이번 세션 코드 블록에 있음, 다음 재시작 때 반영 필요)

### 진행 중 (Colab, Google Drive `MyDrive/audit_project/`)
- BGE-m3 임베딩: `embed_ready_v2.jsonl`(124,066개 청크) 기준으로 계속 진행 중, 8/6 기준 19,200개대
- 1만 건대 스냅샷 확보함: `embeddings_v2/embeddings_10k_snapshot.npy` + `chunk_ids_10k_snapshot.jsonl` (19,200개)

### 이어서 진행 (같은 날, 코드 샌드박스 세션 — Drive/Railway 접근 불가 환경)
> 이 세션은 Google Drive(Colab 데이터)와 Railway에 접근할 수 없어서, 실제 72,913건 데이터로는
> 아무것도 못 돌렸음. 대신 **로컬에 Postgres 16 + pgvector를 직접 설치하고, 실제 스키마를 흉내낸
> 합성 데이터로 스크립트 4개를 전부 실행해서 코드 자체의 버그를 미리 잡는 작업**을 했음.

- **버그 발견 및 수정**: `scripts/build_final_dataset.py`가 `overview`는 제외하면서 `extraction_failed`는
  제외하지 않고 있었음 — 한 사례(case_number)의 모든 레코드가 파싱 실패(`extraction_failed`)면
  그대로 `documents_final.jsonl`에 `parsing_quality: "extraction_failed"`로 흘러들어감.
  - 이건 (1) `summary_smoke_test.py` 주석("overview/extraction_failed는 이미 제외됨")과 실제 동작이
    어긋나고, (2) `schema.sql`의 `CHECK (parsing_quality IN ('standard','partial','fallback'))`을
    위반해서 **DB 적재(`load_to_postgres.py`)가 그 문서에서 통째로 실패**하는 문제였음
  - 합성 데이터로 재현 확인 후 수정 완료 (그룹 내 최고 등급이 `extraction_failed`면 스킵 + 카운트 로그 추가)
  - 실제 데이터로 이 스크립트를 다시 돌릴 때는 이 수정이 반영된 버전으로 돌려야 함 (Colab에 반영 필요)
- **검증 완료 (합성 데이터 기준, 로컬 Postgres+pgvector)**:
  - `schema.sql` 클린 실행 확인 (테이블/HNSW/GIN 인덱스 전부 생성됨)
  - `build_final_dataset.py` → `load_to_postgres.py` 전체 파이프라인 end-to-end 통과 (documents/chunks 적재, 임베딩 조인 정상)
  - 벡터 유사도 검색 SQL(`ORDER BY embedding <=>`) + documents JOIN 정상 동작 확인 — 1주차 체크포인트의 "SQL로 순위 확인"이 메커니즘 상 작동함을 확인 (실제 데이터의 순위 품질 자체는 아직 미확인)
  - `summary_smoke_test.py` DRY_RUN 실행 확인 — 합성 documents_final.jsonl(standard 25 / partial 12 / fallback 7)로 35건 층화 샘플링, 동시성, 포맷 검증, 저장, 리포트까지 전부 정상 동작 (실패 0건, 포맷 위반 0건)

### Colab 임베딩 체크포인트 FileNotFoundError 수정 (같은 날, 2차)
- 사용자가 Colab에서 체크포인트 저장 로직 추가 후 실행하다가
  `os.replace(tmp_path, EMBEDDINGS_PATH)`에서 `FileNotFoundError` 발생시킴
- **원인**: `tmp_path = EMBEDDINGS_PATH + ".tmp"` (예: `"embeddings.npy.tmp"`)는 `.npy`로 안 끝나서,
  `np.save()`가 자동으로 `.npy`를 붙여 실제로는 `"embeddings.npy.tmp.npy"`라는 파일이 생성됨.
  그 직후 `os.replace`가 찾는 `"embeddings.npy.tmp"`는 존재한 적이 없어서 죽음 — 체크포인트를
  저장하려는 시도마다 100% 재현되는 버그. 저장 도중 6,400건 단위 진행상황이 매번 유실됐을 것.
- **수정 + 저장소에 반영**: `scripts/embed_chunks.py`로 새로 커밋 — 임시 파일명을 처음부터
  `.npy`로 끝나게 바꾸고(`EMBEDDINGS_PATH.replace(".npy", ".tmp.npy")`), 실행 시작 시 이전 실행이
  남긴 stray 임시 파일도 정리하도록 추가. 모델 로딩 없이 체크포인트 저장/재개 로직만 격리해서
  로컬에서 재현 테스트 → 수정 후 정상 동작 확인 (`.tmp.npy` 잔여 파일 없이 `embeddings.npy` +
  `chunk_ids.jsonl`만 남음, resume 시 개수도 정확히 로드됨)
- **미확인**: 사용자의 실제 Google Drive에 이번 버그로 생긴 `embeddings.npy.tmp.npy` 같은
  잘못된 이름의 파일이 남아있는지는 이 세션에서 Drive 접근이 안 돼서 직접 확인 못함 —
  사용자가 Colab에서 `os.listdir(CHECKPOINT_DIR)`로 직접 확인 필요

### 아직 안 한 것 (다음 세션에서 이어갈 것 — Colab/Railway 실제 환경 필요)
1. **위 버그 수정을 Colab의 `build_final_dataset.py`에도 반영하고 재실행** — 기존 72,913건 중 extraction_failed로만 이루어진 사례가 있었다면 재확인 필요
2. **`scripts/summary_smoke_test.py` DRY_RUN을 실제 `documents_final.jsonl`로 실행** — 코드 자체는 이번 세션에서 검증했지만, 실데이터 분포 기준으로는 아직 안 돌려봄
3. **Railway Postgres 서비스 생성 여부 미확인** — 만들었으면 `DATABASE_URL` 확보 후 `scripts/schema.sql` 실행 → `scripts/load_to_postgres.py` 실행 (스크립트 자체는 로컬에서 검증 끝남)
4. **1주차 체크포인트** — 실제 DB 적재 후 SQL로 유사 사례 순위 확인 (development-plan.md 1주차 목표, 메커니즘은 검증됨)
5. 요약 스모크 테스트 소량 실제 실행 (비용 발생, 몇십 원 수준)
6. **`scripts/embed_chunks.py`로 Colab 임베딩 재개** — Drive에 잘못된 이름으로 남은 파일(있다면) 정리 후 실행
7. *(여유 있으면)* Railway/Vercel 빈 뼈대 배포로 4주차 배포 리스크 선제 검증
8. *(여유 있으면)* BGE-m3 단독 메모리 실측

### 파일 위치 참고
- 이 저장소(`scripts/`): `schema.sql`, `build_final_dataset.py`, `load_to_postgres.py`, `summary_smoke_test.py`, `embed_chunks.py`
- Google Drive `MyDrive/audit_project/`: 원본 데이터, 임베딩 체크포인트, `documents_final.jsonl`, `chunks_final.jsonl` (전부 여기, 이 git 저장소에는 없음)
